# FAISS-индекс генерится инструментами (tools/build_scene_map.py или
# generate_mock_map.py) и не коммитится — версионируем только metadata.json
# (схема + сэмпл). Бинарный индекс не мержится и тяжёлый.
*.index
# Веса MLP-«топографа» — обучаются офлайн, тяжёлые, не мержатся.
*.pt
