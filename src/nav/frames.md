# Рамы и курс: кто чем выровнен, как сшивается, план перехода на курс VINS

Разбор 2026-09-09. Три системы координат, один шов и план заменить компас на VINS
в качестве источника курса. Механика возврата, которая на этом стоит, — `src/mission/rth.md`.

## Соглашение об осях

**ENU** (в нём живёт ROS): x — на восток, y — на север, z — вверх; курс отсчитывается
от оси x против часовой (восток = 0°, север = +90°). Полётник и MAVLink внутри живут в
**NED** (север/восток/вниз, курс от севера по часовой) — перевод делает MAVROS, поэтому
уставки возврата мы кладём в `setpoint_raw/local` прямо в ENU.
⚠️ `/mavros/global_position/compass_hdg` отдаёт авиационный курс от севера по часовой:
`yaw_ENU = 90° − heading`.

## Три рамы

| | начало | курс | чем удерживается |
|---|---|---|---|
| **мир Gazebo** (истина) | центр площадки | ENU | оракул симулятора; **на борту его нет** |
| **кадр EKF** | точка инициализации/арма | **компас** (`EK3_SRC1_YAW=1`) + склонение (`COMPASS_DEC` 0.13 рад ≈ 7.4°, `COMPASS_AUTODEC=0`) | магнитометр, непрерывно |
| **кадр VINS** | точка init (у нас — сразу после отрыва, `/restart` на арме) | **курс первого кадра камеры** | ничем: yaw у монокуляра НЕНАБЛЮДАЕМ |

Наклоны у обоих привязаны к гравитации (акселерометр её видит), масштаб VINS даёт IMU
(замер по полётам 133201/131451: 0.999 и 1.007). Значит рамы различаются **только
курсом и сдвигом** — этого и хватает шву.

## Шов: FrameAnchor

```
vision_pose = Rz(Δyaw)·p_vins + t,     Δyaw = yaw_EKF − yaw_VINS (одна одновременная пара)
```

- Δyaw — это «поворот кадра VINS относительно компаса». **Он не ноль и не должен им быть**:
  при спавне носом на восток Δyaw ≈ 0 (замер: +0.43° / −0.14°), при спавне −169° он ≈ +169°
  (разбор 212409/213830 — тогда трансляционный якорь без поворота разносил LOITER до 15 м/с);
- при латче нулём становится **невязка позиции**, а не угол: `t` выбирается так, чтобы
  отображённая поза совпала с текущей позой EKF;
- дальше Δyaw **заморожен**: и мягкий дожим, и ЖЁСТКАЯ ПОДТЯЖКА двигают только `t`
  (с 2026-09-09, разбор 173415). Угол задаётся ТОЛЬКО при рождении рамы: первый латч,
  `latch_yaw` на открытии моста, перерождение VINS. Раньше подтяжка пересчитывала и
  угол — и это оказалось миной: расход, который её будит, измерен В МЕТРАХ, а цена
  ошибки несимметрична (кривая трансляция = постоянный сдвиг, кривой угол ПОВОРАЧИВАЕТ
  ВСЮ КАРТУ вокруг якоря: 20° на 45 м = 15 м, а при `EK3_SRC1_YAW=6` те же 20° уходят
  прямо в курс полётника);
- **спаривание по штампу ПРОБОВАЛИ И ОТКАЗАЛИСЬ** (`anchor_pair_by_stamp=False`,
  замер `src/lab/anchor_replay.py` по трём прогонам): расход якоря стал ХУЖЕ
  (0.083 → 0.107 и 0.114 → 0.264 м, подтяжек 1 → 3). Поза EKF — **не независимый
  наблюдатель** того же момента, а ОТКЛИК на нашу же публикацию: `vision_pose` уходит
  в полётник со штампом «сейчас», хотя описывает борт на 0.15 с раньше, — значит EKF
  уже сдвинут на тот же лаг, и пара «по приходу» с ним согласована. Сам разъезд пары
  реален (медиана 0.16 м, хвост 0.45 м), но лечится он не здесь, а честным штампом
  `vision_pose` с компенсацией задержки на стороне AP_VisualOdom — и только с
  повторным замером тем же стендом. `PoseBuffer` и ручка оставлены готовыми;
- **угол рамы захватывается ТОЛЬКО НА СПОКОЙНОМ КУРСЕ** (`latch_yaw_wz=15 °/с`,
  `latch_yaw_still=0.5 с`, `nn1/yaw_calm.py`, с 2026-09-10). `latch_yaw` берёт ОДНУ
  мгновенную пару курсов, у которых разная задержка: на вираже их разность = ω·лаг, а
  не поворот кадра, — и с этого же дня исправить её нечем, жёсткая подтяжка угол не
  трогает. Замер Δyaw якоря против ИСТИННОГО поворота кадра на открытии моста:

  | прогон | Δyaw якоря | истинный поворот | ошибка | итог |
  |---|---|---|---|---|
  | 203257 (пилот, латч на курсе **90°**) | +6.0° | +0.4° | **5.6°** | дрейф EKF **0.08 м** |
  | 202510 (пилот) | −2.7° | +1.2° | 3.9° | дрейф 0.81 м |
  | yawab_check5 (реплей, рыскал непрерывно) | +29.5° | +0.1° | **29.4°** | EKF ушёл на **1959 м** |

  ⚠️ Мешает НЕ РАЗВЁРНУТОСТЬ, а ВРАЩЕНИЕ в момент захвата: полёт 203257 латчился на
  курсе 90° от взлётного и дал лучший результат дня. Пилот рыскает НАРОЧНО (так VINS
  быстрее наблюдает курс) и попадает захватом в паузу между взмахами; реплей рыскал
  непрерывно и попал в размах. Пока ждём спокойствия, мост держим ЗАКРЫТЫМ — поза в
  EKF идёт от моста позы бута, дыры нет. Ждём не вечно: `latch_yaw_wait=10 с`, после
  чего берём как есть (чек имеет право ОТЛОЖИТЬ захват, но не запретить полёт);
- **якорь НЕ УЧИТСЯ У EKF ДО ЛАТЧА ДОМА** (`anchor_follow_latched`, с 2026-09-10).
  До латча поза EKF — **не свидетель**: на нашу раму она ещё не пересела, до открытия
  моста её кормил нулями мост позы бута, и уехать она может куда угодно. Задача якоря
  в этой фазе — ОТДАТЬ раму, а не подстроиться под неё. Разбор `yawab_check4`: EKF уехал
  на 50 м САМ (мы в это время слали ему ВЕРНУЮ позу — 3.5 м при истинных 3.2), жёсткая
  подтяжка впитала эти 50 м, после чего мы стали слать 57 м, EKF уехал дальше, следующая
  подтяжка впитала больше — трансляция удваивалась каждые 10 с и дошла до 2 км, кадр
  прыгал, возврат снялся по `jump`. Якорь не сломался — он честно учился у сломанного
  учителя. Сигнал даёт лётная нода топиком `/mission/rth_latched`, флаг ЗАЩЁЛКИВАЕТСЯ:
  залатчились один раз — EKF стал свидетелем до конца полёта, даже если возврат потом
  запретили (рама-то уже наша);
- **якорь не двигается на протухшей одометрии** (`anchor_stale_sec`, 0.3 с): пара
  «поза EKF / поза VINS» спаривается ПО ПРИХОДУ, и если VINS подвис счётом — штампы
  непрерывны, данные целы, гейт правильно молчит, — то свежий EKF сравнивается с
  двухсекундным VINS. В 173415 борт крутился 10 °/с, пара разъехалась на ω·лаг = 20°,
  подтяжка сработала на первом же сообщении после подвиса и защёлкнула этот угол на
  весь полёт (дрейф EKF 13 м против 0.19 м на восточном спавне того же вечера);
- исключение — `latch_yaw()` на первом открытии моста: берём угол, `t = 0` (полётник
  пересаживается на нашу свежую раму, дрейф за время закрытия не усыновляем).

**Наблюдать:** поле `brd=` в `/mission/status` (шестое поле `/nn1/bridge`) — Δyaw в
градусах. Стоит на месте между перелатчами; скачок = перерождение рамы VINS, медленный
ход = расхождение компаса и курса VINS.

⚠️ **На борту абсолютный курс даёт только компас.** Истины Gazebo там нет, а сам якорь
её и не использует — опора у него `ekf_yaw`, то есть компас + склонение. Кривая
калибровка или неверное склонение поворачивают ВСЮ навигацию: относительные задачи
(держать точку, вернуться по своему треку) отработают точно, сломается абсолютика —
карта ориентиров NN1, географические координаты, «лететь на север».

## План: посадить EKF на курс VINS (`EK3_SRC1_YAW=6`)

Зачем: VINS рождается без курса, EKF — с компасным; дальше оба ведут курс приращениями
ОДНОГО И ТОГО ЖЕ гироскопа (VINS кормится тем же `/mavros/imu/data`). Меняем не источник
приращений, а **источник коррекции дрейфа**: компас (абсолютен, но шумит и врёт над
железом) → визуальные фичи (гладко и без помех, но без абсолюта).

### Фазы

```
1. земля / до зрелости   YAW=1 (компас)  — иначе не заармиться, VINS ещё нет
2. созревание VINS       мост открывается, latch_yaw(): Δyaw снят, t=0
3. рама спокойна         YAW 1 → 6  + ЗАМОРОЗКА yaw-части якоря
   (BS_RTH_HOME_SETTLE)   (тот же момент, что RTH-латч: борт висит в круге,
                          позиционного контура в петле нет)
4. полёт                 курс ведёт VINS, Δyaw — константа, якорь следит только за t
5. разрыв                rebirth | sane=false | мост закрыт | поток протух → откат
```

Переключение **не даёт скачка курса**: мы публикуем ориентацию, уже повёрнутую якорем
(`yaw_VINS + Δyaw = yaw_AHRS`), то есть отдаём полётнику его же текущий курс — поводья
передаются в текущей точке.

### Заморозка обязательна

Пока источник — extnav, EKF следует за нами. Если якорь продолжит пересчитывать Δyaw «по
паре поз EKF/VINS», он будет гоняться за собственным хвостом — та же круговая обратная
связь, что уже ловили с трансляцией (полёт 173102). Поэтому в режиме `vins` жёсткая
подтяжка правит только `t`.

### Откат — мягкий, а не мгновенный

Главный риск схемы: пока летим на курсе VINS, компас и VINS расходятся, и при откате
`6 → 1` EKF доберёт всю разницу разом. Сам полётник рывок переживает (`Copter::check_ekf_reset()`
→ `AC_AttitudeControl::inertial_frame_reset()` сдвигает целевую ориентацию, сохраняя
ошибку — борт не крутанётся). А вот НАШИ рамы никто не компенсирует: на `att_yaw` завязаны
`StationFrame` (рама станции в осях курса), ветровой трим WindTrim и деротация IPM —
скачок курса мгновенно повернёт их, и демпфер поедет держать «не ту» точку.

Поэтому откат: **довернуть свой публикуемый курс к компасному за N секунд** (вести Δyaw к
новому значению с большой τ) → и только потом переключить источник; к этому моменту
расхождение нулевое и сбрасывать нечего. Мгновенный откат оставить аварийному случаю
(VINS исчез — доворачивать нечем): тогда обязательно ярус 0 + сброс наших рам.

Для сравнения: **позиционный рывок при первом открытии моста безопасен** и уже проверен
полётами (6.01 м в 131451, 6.39 м в 120819) — позицию в этот момент не читает никто (ярус
0, ALT_HOLD), а `AC_PosControl` при сбросе сам сдвигает уставку.

### Что уже сделано (2026-09-09)

- **`brdn=`** в статусе — ТЕКУЩАЯ разность курсов «AHRS − VINS», считается `ray_tracer`
  на каждой одометрии **даже при закрытом мосте**; `brd=` рядом — залатченная. Их
  разность = уход курса с момента латча, прямо в bag.
  ⚠️ Прогоны 154759/155443 прошли впустую именно на этом поле: читателя написали
  (`ros_telemetry._on_bridge` → `dyaw_now`, HUD, чек зрелости), а ПИСАТЕЛЯ в `ray_tracer`
  — нет, строка осталась шестипольной и `brdn` был пуст всю запись. Формат строки
  `/nn1/bridge` теперь заперт офлайн-тестом `src/nav/test/test_bridge_gate.py` §9
  (сколько полей и что в каждом) — молча разъехаться больше нельзя. В полёте признак
  живого поля один: `brdn=` ЧИСЛО, а не `--`.
  ⚠️⚠️ И СРАЗУ ВТОРАЯ ПРИЧИНА того же симптома, найденная прогоном 164742: поле
  публиковалось как `-`, потому что `att_yaw` в `ray_tracer` был `None` ВСЕГДА —
  подписка на `/mavros/imu/data` стояла с дефолтным RELIABLE, а MAVROS публикует
  BEST_EFFORT. **Несогласованный QoS не даёт ни ошибки, ни варна: подписка просто
  никогда не срабатывает.** Тем же молчанием убивался `latch_yaw()` на первом открытии
  моста (ветка «курса AHRS нет» → `anchor.reset()` → 2 с сырого VINS в EKF — фикс
  `5c1d562` деградировал обратно) и засечка NN1 (`R_enu_body is None`). Лечение —
  `qos_profile_sensor_data` на все входы MAVROS + сторож молчащих входов (ошибка в лог
  через 30 с). Проверка живьём: `ros2 topic info -v <топик>`, сравнить Reliability
  публикатора и подписчика;
- **зрелость по устойчивости Δyaw** (`BS_RTH_DYAW_TOL`, с 2026-09-10 **=0, ВЫКЛЮЧЕН** — замер ниже): пока `brdn` гуляет больше
  допуска, `RthReadiness` не считает VINS зрелым — значит ни дом не латчится, ни мост не
  открывается, ни курс не переключается. Обёртка через ±180° внутри.
  ⚠️ **Чек молчит на вираже** (`BS_RTH_DYAW_WZ=15 °/с`, с 2026-09-09): курс AHRS идёт от
  FCU, курс VINS — из одометрии 10 Гц, и на развороте их разность гуляет РАЗНИЦЕЙ
  ЗАДЕРЖЕК, а не уходом кадра. Замер 171337: размах `brdn` 5.1° при 41.6 °/с против 2.4°
  при 9.5 °/с в одном полёте; закрывший мост выброс случился на 38 °/с. Родня —
  `ipm_wz_gate` деротации: сравнивать имеет смысл только когда сравнение осмысленно.
  ⚠️⚠️ **И ВСЁ РАВНО ВЫКЛЮЧЕН.** Замер 2026-09-10 (размах `brdn` в скользящем окне 3 с):
  164041 — медиана 2.6°, выше допуска 51 % окон, дом залатчен; 184108 — 2.2°, 39 %,
  залатчен; yawab_check3 — **10.6°, 95 %, дом НЕ залатчен** (`rth=lost:timeout`, мост
  весь полёт закрыт по `ripe`). То есть допуск стоял НА УРОВНЕ СОБСТВЕННОГО ШУМА, латч
  проскакивал на редких тихих участках — лотерея, — и однажды чек запретил возврат
  наглухо. Шум есть и при `|ω|=0`, так что гейт виража от него не спасает. Оживлять
  только с допуском выше измеренного шума (≥15°) или по фильтрованному Δyaw — но при
  15° он ловит лишь грубый развал рамы, который и так ловит детектор перерождения;
- **переключение источника** (`BS_EKF_YAW_SRC=vins`, дефолт `compass`): в момент латча
  возврата нода ставит `EK3_SRC1_YAW=6` через ту же очередь параметров с ретраями, что
  и `EK3_SRC1_*`; при пропаже опоры (перерождение / не sane / закрытый мост / протухший
  поток) возвращает `1` (`BS_EKF_YAW_FALLBACK`). Что реально стоит — поле `yawsrc=`.

**Заморозка yaw-части якоря не понадобилась**: при `YAW=6` полётник следует за нашим
курсом, поэтому пересчёт `Δyaw = yaw_EKF − yaw_VINS` на жёсткой подтяжке даёт ТО ЖЕ
число — круговая обратная связь здесь самонейтральна. ⚠️ Это рассуждение, а не замер:
проверить по `brd=` в первом же прогоне с `vins` (значение обязано стоять).

**Откат пока ЖЁСТКИЙ** — мягкий доворот перед переключением остаётся следующим шагом.

### Ручки и наблюдение

`BS_EKF_YAW_SRC=compass|vins` (дефолт `compass`, пока не доказано полётом),
`BS_EKF_YAW_FALLBACK` (мягкий откат вкл/выкл). В статус — `yawsrc=` (что РЕАЛЬНО стоит в
FCU, а не что мы просили). В bag добавить `/mavros/global_position/compass_hdg`: после
переключения `brd` схлопнется по построению, и расхождение можно будет мерить только
против компаса.

### Порядок проверки

1. реализовать при дефолте `compass` — поведение не меняется, код лежит;
2. прогон A (`vins`, штатный спавн): скорость ухода курса VINS против компаса и против
   истины; спровоцировать `/restart` в полёте и измерить рывок отката;
3. прогон B: то же с магнитной аномалией SITL (`SIM_MAG_ALY`, `SIM_MAG_ALY_HGT`) — при
   идеальном компасе выигрыша в симе не видно, видна только цена;
4. прогон C: спавн НЕ на восток (`SPAWN_POSE`) — единственный, который отличает верную
   сшивку от везения (при спавне на восток Δyaw ≈ 0, и поворот с не-поворотом
   неразличимы).

## Что даст абсолютный курс без компаса

Ни монокулярный VINS (севера не видит), ни GPS-курс (GPS нет), ни GSF (нужна скорость от
GPS). Единственный путь — **засечка NN1**: пеленг на известный ориентир даёт направление,
а не только точку. Записано как оставшийся шаг в `src/nav/tools/nn1/nn1_anchor_howto.txt`.
