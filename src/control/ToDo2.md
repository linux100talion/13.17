# src/control — РУЧКИ (что чем крутится)

**Справочник.** Каждая ручка контура: где живёт, что делает физически, чем измерено её
действие, какое значение стоит сейчас. Ветка `nn2_c3_control2`, состояние на 2026-07-29 (после серии H1–H5).

Чем это НЕ является: списком работ (он в `ToDo.md`) и описанием архитектуры (оно в
`architecture.md`). Здесь только органы управления и цена их поворота.

Имена соответствуют 1:1 по трём уровням:
`BS_PITCH_KP` (env для `src/lab/bootstrap_arch2.sh`) = `--pitch-kp` (CLI ноды) =
`pitch_kp` (поле `mission_pkg/config.py`).

⚠️ Уровни покрывают РАЗНОЕ. Не всё, что есть в конфиге, дотянуто до env — а прогоны
идут через env, так что ручка без `BS_`-имени в прогоне недоступна, её значение берётся
из дефолта. Помечено в таблицах:
**env** — есть `BS_`-имя; **cli** — только флаг ноды; **cfg** — только поле конфига
(менять правкой `config.py`).

---

## Цепочка: от движения борта до наклона рамы

Ручки расставлены по звеньям этой цепи. Задержки — замер `back_check.py` по `G1`
(взаимная корреляция, полное окно висения; у прежних прогонов окно обрывалось ударом
о крепость, их числа занижены):

```
борт поехал
   ↓  +1.12 с   ЗРЕНИЕ: медиана по N кадрам           ← BS_*_SMOOTH
перцепт увидел
   ↓  +0.06 с   РЕГУЛЯТОР: PID по кадру                ← BS_*_KP/KI/KD/IMAX/MAX
команда посчитана
   ↓            ПРОВОД: ограничитель скорости          ← BS_SLEW
команда выдана
   ↓  +0.18 с   БОРТ: τ = 0.27 с                       ← НЕ ручка, физика
рама наклонилась
```

Итого 1.54 с. Три четверти этого — сглаживание перцепта.

---

## 1. Зрение (`flow_estimator.py`)

| Ручка | Что делает | Замер |
|---|---|---|
| `BS_ROLL_SMOOTH` / `BS_PITCH_SMOOTH` / `BS_YAW_SMOOTH` **env** | медиана сигнала по N кадрам. Окно = N/20 сек (перцепт идёт 20 Гц), ступень отдаётся за полокна | 25 кадров → задержка 1.12 с, шум 0.30 px. 12 → шум 0.84. 5 → шум 5.06. Шум растёт быстрее √N: медиана по редким кадрам ловит выбросы |
| `*_conf_min` / `*_conf_full` **cfg** | доверие по числу найденных точек: ниже MIN выход гасится в ноль, выше FULL идёт целиком, между — линейно | 0.05 / 0.20; в полёте conf 0.5–0.9, порог не срабатывал ни разу |
| `BS_FLOW_OBS` **env** | поднять зрение БЕЗ демпфера: `/flow_dbg*` пишется, управление не трогается | режим замера перцепта на заданном движении |
| `feat_lo` **cfg** | доля высоты кадра, ВЫШЕ которой детектор не ищет точки для канала СКОРОСТИ (0.667 = только нижняя треть) | 93% точек детектор ставил в полосу горизонта, где поток почти нулевой, а до ближней земли не доходил ни разу. Маска: `S_lat` 0.40 → 2.42 px/(м/с), шум 3.10 → 0.59 м/с. Инструмент — `src/lab/feat_region.py` |
| `kf_min_pts` **cfg** | ниже скольких точек опора считается потерянной и пересевается | 40 из ~150 посеянных |
| `kf_max_step` **cfg** | отсечка выброса подобия, log-единиц за кадр | 0.05 = двадцатикратный запас над физикой; ловит срывы RANSAC. Подняло corr(kf_logs, удаление) с −0.75 до −0.97 |
| `kf_alt_max` **cfg** | опора действительна, пока высота не ушла больше чем на эту долю (в логарифме); ушла — пересев + `kf_valid=False`, регулятор молчит | 0.06 ≈ 6% (на 5 м это 30 см). Без затвора набор/снижение разваливают подобие: `kf_logs` скачет ±0.02–0.04 не показывая движения, `kd` берёт производную дребезга и кладёт раму на 6.9° |
| `cam_tilt` / `kf_tilt_k` **cfg** | компенсация наклона в опорном канале: вычитается `k·log(sin(α+θ)/sin(α+θ_опоры))` | 0.26 рад / 0.05. Дало R² 0.10 → 0.63 и крутизну, которая наконец воспроизводима: 1.35 ± 0.05 и 1.47 ± 0.34 %/м на двух прогонах |

Чем платим за сглаживание — это главный размен контура: `kp·σ` должно остаться в
пределах потолка команды, поэтому снятие сглаживания требует пропорционального
снижения `kp`. Свип `lag_sweep.sh` гонял их по диагонали именно поэтому.

**Не ручка, а свойство:** чувствительность канала к движению. Замеры
открыто-контурные (`L1_scale2ax`, `K1c_slope`: оракул ведёт, зрение смотрит):

| канал | чувствительность | шум в пересчёте на движение |
|---|---|---|
| боковой, весь кадр | `S_lat = 0.40 px/(м/с)` | 3.10 м/с |
| боковой, маска нижней трети | **`2.42 px/(м/с)`** | **0.59 м/с** |
| продольный, покадровый сдвиг | `−0.33 ± 0.26` (не значим) | — |
| продольный, опорный кадр | **`1.35 ± 0.05 %/м`** масштаба | 0.59 м/с |

Геометрия: камера наклонена вниз на 15° (`worlds/iris_cam/model.sdf`), поэтому боковой
ход идёт поперёк оптической оси и сдвигает картинку, а продольный — почти вдоль оси и
даёт расширение поля. Отсюда и разные механизмы у осей: боковая живёт в СДВИГЕ,
продольная — в МАСШТАБЕ, и мерить их одной статистикой нельзя.

---

## 2. Регулятор, по осям (`stabilization.py`, `_FlowDamper1D`)

У каждой оси СВОЙ полный набор, ничего не шарится (см. комментарий в `config.py`).

| Ручка | Что делает | roll | pitch | yaw |
|---|---|---|---|---|
| `KP` **env** | усиление. ⚠️ у roll/yaw — PWM на ПИКСЕЛЬ (скорость), у pitch — PWM на log(масштаба) (ПОЛОЖЕНИЕ, метр = 0.0135) | **16** | **2000** | 6 |
| `KI` **env** | набор УДЕРЖИВАЕМОГО наклона | 3 | **0** | 0 |
| `KD` **env** (у yaw нет: он PI) | опережение на `kd/kp` секунд — компенсация задержки; множит шум | 0 | **1000** | — |
| `IMAX` **env** у roll/pitch, **cfg** у yaw | потолок интегратора, PWM | 150 | 150 | 200 |
| `MAX` **cfg** | потолок всего выхода оси, PWM | 150 | 150 | 150 |
| `OSIGN` **env** | знак ПРОВОДА | **+1** | **+1** | +1 |
| `CMD_GAIN` **env** | velocity-assist: стик [-1..1] → целевой поток в px, демпфер гасит отклонение от него, а не саму команду | 10 | 10 | 10 |

Числа `kp` у осей несопоставимы: у roll/yaw ошибка в пикселях межкадрового сдвига,
у pitch — в логарифме масштаба от опорного кадра, где метр смещения = 0.0135. То есть
`kp=2000` у pitch — это 27 PWM на метр ухода (0.31 м/с² на метр), а `kp=16` у roll —
16 PWM на пиксель, при `S_lat=2.42` это 39 PWM на м/с.

**`OSIGN` — обе горизонтальные оси `+1`, и это стоило дня отладки.**

У **pitch** знак `+1` проверен прогоном `D1_wrongsign`: при `−1` демпфер командовал
−124 PWM на скорости 4.8 м/с вперёд и разгонял на +0.95 м/с² до удара.

У **roll** знак был `−1` и оказался знаком РАЗГОНА. Вскрылось в `H4_mask`: перцепт
видит движение (`flow_lat` 1.5 → 27 px), команда растёт (−26 → −150 PWM), рама
кладётся до −12.9°, борт разгоняется В ТУ ЖЕ сторону до 12.8 м/с — положительная
связь в чистом виде.

Причина — конвенция. Проекция `−vx·sin(курс) + vy·cos(курс)` даёт ось **ВЛЕВО**
(тело в ROS = FLU), а в разборе она звалась `v_right`. Правильно: борт идёт влево →
мир в кадре смещается вправо → `flow_lat > 0` (сходится с открыто-контурным замером
`L1`: `+0.48` на эту ось) → гасить надо креном ВПРАВО, командой ВЫШЕ центра → `+1`.

Почему `−1` «подтверждался» раньше: до маски канал был слеп (`S_lat` 0.4 против
2.42), петля не успевала разойтись за окно висения, а знак проверялся по прогонам,
где крен сам же создавал движение — такие данные о знаке не говорят. Правило,
которое из этого следует: **знак и крутизну мерить только на открыто-контурных
прогонах** (оракул ведёт, зрение смотрит), иначе меряется собственная петля.

**`yaw_ki` держим 0** — интегратор вреден, у `yaw_flow` есть смещение
(`yaw-hold-tuning`), интегратор его копит.

---

## 3. Формирование команды (`control_stack.py`)

| Ручка | Что делает | Замер |
|---|---|---|
| `BS_SLEW` **env** | предел скорости изменения выхода, PWM/сек (0 = выкл) | 100 → команда упирается в потолок 59–65% выборок, выход = треугольная пила со средним ≈ 0. 300 → 54%, задержка контура 0.06 с. **0 → хуже всего**: команда ±98 PWM, борт уехал 5.5 м/с |
| `--pilot-pitch-sign` / `--pilot-roll-sign` **cli** | сырой PWM пульта → намерение; парный к `_PITCH_RC_SIGN` в стеке, вместе = pass-through | −1 / +1 |
| `--pilot-deadzone` / `--pilot-full` **cli** | мёртвая зона и полный ход стика, PWM | 30 / 400 |

Ограничитель ставился, чтобы команда успевала материализоваться (борт выходит на угол
за τ=0.27 с, команда шириной в кадр даёт 11% угла). Свип показал, что он же делает
контур ватным: PID пересчитывает цель быстрее, чем ограничитель к ней едет. При этом
выключать его нельзя — борт сам себе ФНЧ только выше ~1 Гц, ниже дребезг проходит.

---

## 4. Миссия и фазы (`mission_plan.py`, `plan/step.py`)

| Ручка | Что делает |
|---|---|
| `BS_STAB` **env** | какие стабилизаторы в стеке, склейка через `+`: `DpRollHold+DpPitchHold`, `GzPosHold`, `DpYawHold`, `VinsHold`, `manual` |
| `BS_MISSION` **env** | плейлист токенов: `climb<м>`, `hover<сек>`, `mv_fwd/bkwd/right/left<сек>`, `sk_*<τ>`, `land`. Либо имя из `MISSIONS` |
| `BS_MV_LEVEL` **env** | уровень стика для `mv_*` сегментов, [-1..1] (0.3) |
| `BS_ALT` **env** | целевая высота набора, м |
| `BS_THROTTLE_CLIMB` **env** | газ подъёма, PWM (1800 в калибровочных прогонах; на 1650 набор не укладывался в бюджет) |
| `BS_FENCE` **env** | геозабор, м от старта (0=выкл): ушли дальше — Control-шаг прыгает на land. Стендовая страховка по gt, не бортовая функция | 25 при чистом круге 35 м. Без него борт дважды садился в крону дерева, где LAND не засекает касание и висит до исчерпания бюджета — при RTF 0.01 это часы |
| `BS_MODE/ARM/CLIMB/LAND_BUDGET` **env** | бюджеты фаз, sim-сек — не поведение, а когда сдаваться |

**Стабилизаторы висят на ВСЕХ фазах, включая набор и посадку** (`_hold_stack`;
исключаются только зонды, `is_probe`). Замер `G1`: за набор борт получает 3.8 м/с назад
(`∫g·tan(тангаж) = −4.08` при фактическом `+0.12 → −3.64`), потому что на подъёме
`flow_lon` показывает +1.52 px «вперёд», когда борт уже едет назад. Всё висение уходит
на отыгрыш этого подарка. Отключение демпфера на наборе — одна строка.

---

## 5. Gz-hold — оракул, не боевой контур (`GzPosHold`)

`BS_GZ_KP/KD/KI/IMAX/MAX/PSIGN/RSIGN/CMD_GAIN`, челнок `BS_GZ_SHUTTLE_*`.

Держит позицию по ИСТИННОЙ позе из Gazebo. На борту такого нет — это эталон, с которым
сравнивают зрение, и способ занять оси, которые в опыте не участвуют.

---

## 6. Не ручки (менять можно, но это не настройка контура)

| Величина | Значение | Где живёт |
|---|---|---|
| τ — постоянная времени борта по углу | 0.27 ± 0.03 с | физика рамы + ATC-контур ArduPilot |
| k — крутизна канала | 0.0735 °/PWM | `ATC_ANGLE_MAX=30°`, `RC_MIN/MAX=1100/1900` |
| a₁ — ускорение на PWM | 0.0115 м/с²/PWM | из k: `g·tan(k·PWM)` |
| наклон камеры | 15° вниз (0.26 рад) | `worlds/iris_cam/model.sdf:907` — определяет `S_lat`/`S_lon` |
| темп перцепта | 20 Гц | разрешение камеры (`RES=960x540`) и число точек |
| радиус чистой площадки | `make scene-clear R=…` | `scene_objects.py`; при `R=25` ближайший объект в 25.5 м, борт уходит на 33 м → мало, нужен `R=40` |
| `EK3_GBIAS_P_NSE` | 1e-5 | `config/sitl-extra.parm`; вылечил уход оценки по тангажу (0.187 → 0.021 °/с), крен не тронул |

---

## 7. Что стоит сейчас и что это даёт

```
BS_STAB="DpRollHold+DpPitchHold"   BS_MISSION="climb3,hover10,land"
BS_SLEW=300   BS_FENCE=25
BS_ROLL_KP=16    BS_ROLL_KI=3   BS_ROLL_IMAX=150  BS_ROLL_SMOOTH=25  BS_ROLL_OSIGN=1
BS_PITCH_KP=1500 BS_PITCH_KI=0  BS_PITCH_KD=5000  BS_PITCH_SMOOTH=9  BS_PITCH_OSIGN=1
BS_PITCH_CMD_GAIN=0
```

Ручки высоты (внешний контур, `control/altitude.py`; появились после J1b):
`BS_ALT_KP=0.6` (м/с на метр ошибки), `BS_ALT_RATE_MAX=1.2` (потолок командной vz),
`BS_ALT_TOL=0.10` (в допуске контур отдаёт РОВНО центр — в ALT_HOLD центр значит
«держи», и давить в край мёртвой зоны у цели значит гнать дрожь). Пересчёт PWM→vz
(`dz=100`, `span=400`, `rate_full=3.16`) откалиброван замером: +300 PWM = +1.58 м/с.

Снос за 8 секунд висения (окно от выхода на потолок высоты):

| прогон | что менялось | вдоль | вбок | всего | пик v_fwd | пик v_left |
|---|---|---|---|---|---|---|
| `G1` | поток в px, обе оси | −25.7 | +1.4 | 25.7 | 3.78 | 0.51 |
| `H1` | опорный кадр на pitch | +8.5 | +12.8 | 15.4 | 5.52 | 2.00 |
| `H2` | + компенсация наклона | +4.5 | −13.8 | 14.5 | 1.30 | 4.14 |
| `H3` | + два набора точек | +6.4 | −32.3 | 32.9 | 4.38 | 8.12 |
| `H4` | + маска детекта | +17.0 | +27.1 | 32.0 | 5.33 | 9.25 |
| `H5` | + знак крена | +2.3 | −1.0 | 2.5 | 2.49 | 0.33 |
| `H6` | + `kd` 1000→2500 | +3.6 | +1.6 | 3.9 | 1.94 | 0.25 |
| **`H7`** | **+ затвор по высоте** | **+1.4** | **−1.0** | **1.7** | **0.60** | **0.34** |

Обе оси держат одновременно впервые. Рост сноса в H3/H4 — не регресс: это маска и
разделение наборов усиливали сигнал, а вместе с ним и положительную связь по крену,
которая была там с самого начала и до `H4` пряталась за слепотой канала.

**Затвор по высоте (H7) дал больше, чем любая настройка гейнов.** Разбор входа в
висение: борт въезжал в hover со скоростью 1.5 м/с, и вся она была самодельной. На
наборе с 1.5 до 4.8 м геометрия сцены меняется втрое, подобие пересобирается на ходу,
`kf_logs` скачет ±0.02–0.04 не показывая движения — а `kd` берёт производную этого
дребезга (скачок 0.02 за кадр = 1000 PWM), слю-лимит размазывает в устойчивые −70 PWM,
рама кладётся на 6.9° и разгоняет борт. До больших команд борт шёл 0.25 м/с — вот
столько даёт настоящее возмущение.

Проверялась и отброшена другая гипотеза: что высота подмешивается в масштаб напрямую
(дальность ∝ высоте). Коэффициент при `log(высоты)` вышел −0.02, −0.01, +0.00 на трёх
прогонах вместо модельной единицы — точки опоры сидят на дальнем плане, чья дальность
от высоты не зависит. Дело не в СМЕЩЕНИИ сигнала, а в его РАЗВАЛЕ.

Что дал затвор:

```
                вход, м/с   макс уход   пик v_fwd   посадка: ушёл / финиш от старта
H6 (без)          +1.14       6.7 м       1.94         2.2 м / 3.5 м
H7 (с затвором)   +0.60       1.7 м       0.60         1.2 м / 2.5 м
```

На наборе команда ровно ноль, контур включается когда высота устоялась, гасит 0.6 м/с
за три секунды и держит в пределах 1–1.8 м. На снижении — то же самое: вместо борьбы
с разваливающимся сигналом (`pitch_cmd −53.9 ± 61` в H6) контур молчит (`−9.8 ± 34`).

Продольная ось при этом остаётся недодемпфированной (период ~10 с), но амплитуда
теперь такая, что это уже не мешает: подбор `kd` между 1000 и 2500 в разбросе стенда
(±40%) неразличим — чтобы его различить, нужно по три прогона на значение.

Правило, выведенное этой серией и стоившее дороже всего: **знак и крутизну любого
канала мерить только на открыто-контурных прогонах**, где движение задаёт оракул, а
зрение только смотрит (`BS_STAB=GzPosHold` + `BS_FLOW_OBS=1`). Закольцованные данные
на вопрос «что видит перцепт» не отвечают — трижды за день на этом обожглись.

Полная сводка прогонов — `docker/sim/output/lag_sweep.csv`, разбор одного —
`src/lab/back_check.py`, зрение по кадрам — `src/lab/keyframe_track.py` и
`src/lab/feat_region.py`.


## 8. Серия J: опора как ДАТЧИК ПОЛОЖЕНИЯ (висение 40 с, а не 8)

Серия H мерила снос за 8 секунд. На сорока секундах вылезло другое: контур садился
на устойчивое равновесие в 10–13 м, потому что канал линеен только вблизи опоры
(0–2 м читается на 97%, 9–15 м на 30–41%, 15–30 м на 3%). Чем дальше борт, тем
меньше причин его возвращать.

| прогон | что менялось | уход 10/20/30/40 с, м | положение corr / крутизна | скорость corr / СКО |
|---|---|---|---|---|
| `H9s1/s2` | база (длинная опора) | 6/6/11/17 и 12/13/13/15 | +0.42 / 0.31 %/м | — |
| `J1b` | короткая опора + накопитель | 11/24/29/**34** ⛔ забор | +0.74 / 0.61 %/м | — |
| `J1c` | + kd по ОКОННОЙ скорости (1 с) | 5/8/13/**13** и 7/7/7/**7** | — | — |
| `J2` | + внешний контур высоты | 6/9/9/**9** | **+0.86 / 1.50 %/м** | +0.30 / 91.3 |
| `J3` | + окно скорости 2 с | 15/15/15/**15** | +0.55 / 0.72 %/м | +0.34 / **36.6** |
| `J4` | + ω с MAVROS (не gz-LP-5Гц) | 7.7/7.7/7.7/**7.7** | **+0.88 / 1.52 %/м** | **+0.51** / 37.7 |
| `J5` | + ω средняя за кадр, с data_raw | 13/13/13/**13** | +0.84 / 1.24 %/м | +0.50 / **31.4** |

Что сделал каждый шаг:

- **накопитель (J1b)** превратил канал из «датчика вблизи» в счисление: 31 м при
  истинных 28, равновесия больше нет. Ценой автоколебания ±20 м с периодом 22 с —
  П-регулятор по положению на двойном интеграторе с задержкой 1 с обязан звенеть;
- **оконная скорость (J1c)** дала настоящий D-член. Разность соседних кадров
  коррелирует с истинной скоростью на +0.27 (шаг сигнала p95 0.0134 против полезного
  приращения ~0.001), её kd-член бил в потолок 150 PWM на 21% кадров и переворачивал
  знак железной команды 9 раз за 5 с. Автоколебание ушло, борт стал садиться на
  равновесие вместо звона;
- **высота (J2)** вылечила сам канал положения: corr +0.74 → +0.86, крутизна
  0.61 → 1.50 %/м (физика ~1.35). Причина была в перелёте набора: шаг выходил по
  факту пересечения цели с vz=+1.6 м/с, ALT_HOLD гасил 2 с → висели на 5.2 м вместо
  3, а затвор опоры при 5 м это ±0.30 м, туже собственной болтанки — он бил каждые
  2.6 с и ВЫБРАСЫВАЛ накопленное. Счётчики `/flow_dbg4`: было 3 зачтённых сегмента
  против 22 выброшенных, стало 10 против 2;
- **окно 2 с (J3)** срезало шум D-члена в 2.5 раза (СКО 91 → 37, вклад kd 457 → 183
  PWM). Уход при этом вырос — но это разброс стенда: при одном конфиге J1c дал 13.3
  и 7.4 м. Различать 9 и 15 м одним прогоном нельзя, нужно по три.

**Главное открытие серии — не в зрении, а в проводке.** Полётные цифры канала втрое
хуже офлайновых при одном и том же коде. Причина: восприятие брало ω с
`/gz_imu/data_flu`, а этот поток прожат low-pass'ом 5 Гц (фильтр нужен VINS —
срезает лимит-цикл rate-loop ~7.5 Гц, которого камера на 10 Гц не видит). Оценщик
вычитает по ω ВРАЩАТЕЛЬНЫЙ поток, и прожатая ω оставляет остаток вращения, который
уезжает в оценку перемещения. Замер на одном бэге (J3), одни кадры, один код,
разница только в источнике ω (`src/lab/kf_vel_check.py`, ключ `KV_IMU`):

```
                 положение corr | шаг/кадр | скорость corr (окно 2с) | остаток
  MAVROS              +0.89       0.0034          +0.68              1.43 м/с
  /gz_imu/data_flu    +0.61       0.0070          +0.32              3.86 м/с
```

Полётная телеметрия того же прогона: +0.55 / +0.34 — нода получала ровно вторую
строку. Ориентация в фильтре проходит нетронутой, то есть виноват гироскоп, а не
тангаж. Восприятие переведено на `/mavros/imu/data`; это совпадает и с боевым бортом
(на Orin IMU идёт с полётника, `INS_GYRO_FILTER` 20 Гц — вчетверо шире). VINS
остаётся на своём фильтрованном потоке.

J4 подтвердил офлайн-предсказание в полёте один в один (+0.88 против обещанных
+0.89) и дал лучший уход серии — плоские 7.7 м с 10-й секунды. J5 довёл проводку:
ω теперь СРЕДНЯЯ за межкадровый интервал (оценщик умножает её на dt — ему нужен угол
МЕЖДУ кадрами) и берётся с `data_raw` (20.8 Гц против 12.5 у ATTITUDE; камера 19-30).
Это дало ещё −17% шума D-члена (СКО 31.4, вклад kd 157 PWM — впервые на уровне
потолка 150), но корреляцию не подняло: +0.50 против +0.51.

### База K1s: три прогона на одном конфиге

Чтобы вообще было чем мерить настройки, сняли серию из трёх одинаковых прогонов
(`src/lab/hover_series.sh`):

| прогон | уход 10/20/30/40 с, м | положение corr / крутизна | скорость corr / шум |
|---|---|---|---|
| `K1s1` | 10.0/10.4/10.4/10.4 | +0.89 / 1.37 %/м | +0.50 / 38.9 |
| `K1s2` | 5.1/5.1/5.9/12.1 | +0.87 / 1.46 %/м | +0.46 / 30.5 |
| `K1s3` | 13.1/13.1/13.1/13.1 | +0.95 / 1.90 %/м | +0.56 / 42.2 |
| **среднее** | **11.9 ± 1.4 м (12%)** | **+0.90 ± 0.04 / 1.58 ± 0.28** | **+0.51 ± 0.05 / 37 ± 6** |

Разброс оказался ВТРОЕ меньше, чем я предполагал по J4/J5: не «7-13 м шума», а
11.9 ± 1.4 м. Это переворачивает прошлый вывод — **уход 7.7 м у J4 лежит НИЖЕ всех
четырёх прогонов новой проводки** (12.7, 10.4, 12.1, 13.1), то есть усреднение ω за
кадр + `data_raw`, похоже, не помогло, а слегка навредило. Подтверждать надо серией
на прежней проводке (n=1 против n=3 сравнивать нельзя), но версия «это был шум»
теперь не проходит.

Второй вывод серии важнее: **уход больше не ограничен качеством зрения.** У `K1s3`
самый чистый канал серии (corr +0.95, крутизна 1.90 %/м — выше физических 1.35) и при
этом САМЫЙ большой уход 13.1 м. То есть дальше надо крутить регулятор, а не перцепт.

Арифметика по замеренному в ПОЛЁТЕ (а не офлайн): kp=1500 при крутизне 1.58 %/м даёт
23.7 PWM/м → ω_n = 0.52 рад/с (период 12 с). Крутизна канала СКОРОСТИ в полёте
8.3-10.3 m-log/(м/с) — вдвое ниже офлайновых 18.5, поэтому kd=5000 даёт лишь
51 PWM/(м/с) → ζ ≈ 0.56, недодемпфировано. Для ζ ≈ 1 нужно kd ≈ 9000.

### K2s: kd=9000 — гипотеза ОТВЕРГНУТА

| серия | уход за 40 с | пик v_fwd | тангаж СКО | шум скорости |
|---|---|---|---|---|
| `K1s` (kd=5000) | **11.9 ± 1.4 м** (10.4..13.1) | 4.36 ± 0.17 | 8.1 ± 0.8° | 37.2 |
| `K2s` (kd=9000) | 15.4 ± 4.1 м (11.0..18.9) | 4.55 ± 0.51 | 9.0 ± 0.1° | 44.8 |

Разница средних +3.6 м при объединённом СКО 3.0 → t=1.45 против порога 2.78 (n=3+3),
то есть формально НЕ значима. Но улучшения нет ни по одному показателю, а РАЗБРОС
вырос втрое (1.4 → 4.1) и шум D-члена на 20% (37 → 45). Держим kd=5000.

Почему расчёт ζ не сработал: он молча предполагал, что D-член несёт СКОРОСТЬ. В
полёте его остаток 3.1-3.3 м/с — того же порядка, что сама скорость борта, поэтому
рост kd усиливает шум ровно настолько же, насколько демпфирует. **Пока остаток
D-канала не упадёт, гейнами тут ничего не выиграть.**

### K3s: источник гироскопа тоже ни при чём — и это закрывает вопрос про ω

| серия | что стоит | уход за 40 с |
|---|---|---|
| `K1s` | kd=5000, ω с `data_raw` + усреднение | 11.9 ± 1.4 м |
| `K2s` | kd=9000, ω с `data_raw` | 15.4 ± 4.0 м |
| `K3s` | kd=5000, ω с `data` (ATTITUDE) | **11.4 ± 0.5 м** |

- **База (6 прогонов при kd=5000, оба источника ω): 11.6 ± 0.9 м.** Разброс всего 8% —
  стенд куда воспроизводимее, чем казалось по серии J.
- `kd=9000` против этой базы: +3.8 м, t=2.33 против порога 2.31 (df=7) → **значимо
  хуже**. Гипотеза про недодемпфирование окончательно закрыта.
- `data` против `data_raw`: −0.5 м, t=0.56 → **не значимо**. Остаток D-канала при этом
  одинаков при любом источнике (3.0-3.5 м/с), то есть версия про тряску рамы в сыром
  гироскопе ТОЖЕ неверна. Оставляем `data` (заодно это ровно то, что есть на борту).
- **J4 (7.7 м) — одиночный выброс на −4.2σ от базы, а не эффект проводки ω.** Метрики
  канала у него как у всех. Вывод «усреднение ω навредило» снят.

Что осталось единственной необъяснённой разницей: нода считает по **`/image_mono`**
(CUDA-дебайер камеры), а офлайн-переигрывание — по `/image_color`, переведённому в
серый. Остаток D-канала: полёт 3.0-3.5 м/с, офлайн 0.8 м/с. Больше входов, которые
различались бы, нет — ω, тангаж, высота, кадры по времени и темп уже проверены.
Следующий шаг: записать `/image_mono` в бэг и переиграть оба потока одним оценщиком.

Остаток гэпа по скорости (полёт 0.50 против офлайновых 0.68) объяснён не полностью;
кандидаты — сглаживание `pitch_smooth=9` поверх уже усреднённой ω и то, что нода
считает по `/image_mono`, а офлайн-переигрывание по `/image_color`.

Открытым остаётся: `kf_win`/`kf_seg_max`/`kf_alt_max` не выведены в `BS_`-ручки
(живут дефолтами оценщика); болтанка высоты ±0.2 м никуда не ушла — контур убрал
смещение, но не дрожь; `EK3_ABIAS_P_NSE` так и не пробован.

Инструменты серии: `src/lab/kf_vel_check.py` (переигрывание бэга боевым оценщиком —
им опровергнуты версии про медиану и про темп кадров), `src/lab/back_check.py`,
`/flow_dbg3` = (положение, оконная скорость, точек видно), `/flow_dbg4` = (сегментов
зачтено, пересевов с выбросом, отбраковок).


## 9. Ветер: калиброванное возмущение (серия W)

В Gazebo включён боковой ветер (`WIND_SPD`/`WIND_DIR_DEG`/`WIND_FACTOR`, см.
`docker/sim/scripts/sim_up.sh`). Три части, без любой ветра НЕ будет: `<wind>` в мире,
плагин `gz-sim-wind-effects-system` и `<enable_wind>` в звене. Корпус (`base_link`)
живёт в модели из образа `ardupilot_gazebo`, поэтому её копия патчится на старте — тем
же приёмом, что и разрешение камеры.

**Сила калибруется замером, а не формулой.** Ходовое «F = mass·factor·Δv» неверно:
отклик КВАДРАТИЧЕН по factor. Замер тремя прогонами с оракулом (`BS_STAB=GzPosHold`,
ветер 3 м/с; сила = средний крен удержания × g × 1.75 кг):

| factor | крен удержания | сила | что обещала формула |
|---|---|---|---|
| 0.15 | 0.43° | 0.13 Н | 0.68 Н |
| 0.30 | 1.40° | 0.42 Н | 1.35 Н |
| 0.80 | 9.41° | 2.85 Н | 3.60 Н |

F/factor² = 5.8 / 4.7 / 4.5 → **F ≈ 4.6·factor² Н при 3 м/с**, то есть factor
масштабирует СКОРОСТЬ под квадратичным лобовым. Цель — реалистичный бриз (0.55-0.8 Н,
крен 1.8-2.6°; настоящий борт в 3 м/с держит 3-6°), отсюда **factor 0.4**.

### W3s — зрение против ветра 3 м/с

| | уход за 40 с | сила (по среднему крену) | снос по ветру | положение corr |
|---|---|---|---|---|
| база `K1s+K3s` (6 прогонов) | **11.6 ± 0.9 м** | −0.03 ± 0.03 Н | −1.6 ± 2.2 м | 0.80..0.95 |
| `W3s` ветер 3 м/с (3 прогона) | **13.4 ± 4.3 м** | **+0.69 ± 0.03 Н** | **+3.9 ± 1.3 м** | 0.52..0.77 |

- Ветер стоит **+1.8 м** ухода, t=1.03 против порога 2.31 → **не значимо**. Борт
  держится, все три прогона сели сами, гео-забор не срабатывал.
- Сила воспроизводима до 4% (0.69 ± 0.03 Н) — возмущение стало ИНСТРУМЕНТОМ: теперь
  любую настройку можно проверять и в спокойном воздухе, и под калиброванным сносом.
- Снос ушёл ПО ветру (+3.9 м против −1.6 без него) — контур крена его отрабатывает,
  но не до нуля.
- ⚠️ Побочный эффект: канал ПОЛОЖЕНИЯ просел (corr 0.52-0.77 против 0.80-0.95). Борт
  под ветром постоянно висит с креном 2.3°, а компенсация наклона (`kf_tilt_k=0.05`)
  подбиралась на прогонах БЕЗ постоянного крена. Если ветер станет штатным условием,
  коэффициент надо перемерить с креном в модели — по той же методике (открытый контур,
  оракул ведёт, зрение смотрит).
