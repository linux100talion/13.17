# src/control — КАК НАСТРАИВАТЬ ДЕМПФЕР ЗАНОВО (план прогонов по трём осям)

Если бы настройку `Dp*` пришлось начинать с чистого листа — вот порядок прогонов, который
я бы выполнил, и почему именно такой. Порядок выведен из ошибок этой кампании: почти всё
потерянное время ушло на настройку контура против неизмеренного датчика и на выводы по
одиночным прогонам.

Ручки и их текущие значения — `ToDo2.md`. Механизм звона тангажа — `ToDo3.md`. Впрыск
команд — `control.md`. Здесь только МЕТОДИКА.

**Чем прогоны запускались** — `swips/cmd.md` (готовые строки, дисциплина, разбор,
индекс серий). Мета каждого отснятого прогона — `swips/<NAME>.env`.

> ⚠️ **СТАТУС ПОСЛЕ КАМПАНИИ D2s–F1** (разбор — `ToDo5.md`). Продольная ось больше НЕ
> позиционная: `DpPitchHold` (сигнал `kf_logs`, логарифм масштаба) заменён на
> `DpPitchRate` (сигнал `ipm_vfwd`, МЕТРЫ В СЕКУНДУ из выпрямленной полосы земли).
> Поэтому:
> - **фаза 5 в старом виде мертва** — она настраивает петлю второго порядка по положению,
>   которой больше нет. Что вместо неё — «Фаза 5′» в конце документа;
> - **гейны тангажа несопоставимы** со старыми: `pitch_kp=1500` жил в log-единицах,
>   `pitch_rate_kp=200` — в PWM на м/с. Переносить числа между ними нельзя;
> - **всё остальное (правила, фазы 0–4, 6–8, «чего НЕ делать») в силе** и подтвердилось
>   ещё раз: почти всё потерянное время снова ушло на настройку против неизмеренного
>   датчика и на выводы по одиночным прогонам.

## ПРОТИВ ВЕТРА НУЖЕН ИНТЕГРАТОР (свип `B`, 2026-08-11)

Один П-член против ПОСТОЯННОЙ силы приходит в равновесие на ненулевой скорости:
`v_уст = a_ветра/(g·kp)`. Наклон при этом выходит ровно правильный, но контур может
получить его ТОЛЬКО из остаточного сноса — это не настройка, это устройство П-члена.
Интегральный член набирает тот же наклон накопителем, и остаток упирается уже не в
ветер, а в сдвиг канала зрения.

Свип `BS_ROLL_RATE_KI` при ветре 3 м/с под 98° (боком), `kp=30`, n=3 на точку:

| `roll_rate_ki` | `vбок` м/с | уход, м | висел, с | внутри 10 м | `v_кон` м/с |
|---|---|---|---|---|---|
| 0  | 0.92 0.60 0.61 | ~22 | 19–29 | 27–28 % | 0.66 |
| 5  | 0.19 0.13 0.22 | 6.9–7.7 | 39 | 97–100 % | 0.27 |
| 15 | 0.07 0.07 0.10 | 2.2–5.6 | 38–39 | 100 % | 0.11 |
| **30** | **0.02 0.03 0.00** | **2.0–3.8** | **38–39** | **100 %** | **0.05** |
| 50 | 0.01 0.02 0.01 | 2.8–3.4 | 38–39 | 100 % | 0.09 |

Без стабилизации вообще — 3.3–3.5 м/с. Полная цепочка: **3.5 → 0.71 → 0.02 м/с**.

⚠️ **Мерить под ветром надо `vбок`, а не уход в метрах.** Забор 25 м цензурирует выборку:
при `ki=0` все три прогона обрубались на 19–29 с, и любая плохая настройка даёт
одинаковые «22 м». И не полную скорость: свипится ручка ОДНОЙ оси, а ветер задан в
мировых осях — борт под ним разворачивается, и часть силы уезжает на соседнюю ось с
втрое большим гейном. Прогон `V2s1` по полной скорости выглядел настроенным (1.00 м/с),
а на деле держал ТАНГАЖ (`vвпер` +0.74 против `vбок` +0.13).

**Рабочая точка — `ki = 30`, и выбрана она по ПОЛКЕ, а не по максимуму.** Склон кончается
между 15 и 30: снос падает 0.08 → 0.02 м/с, а дальше на 50 стоит на месте (0.01 — это
уже разрешение метрики, не разница). Расплаты, которую ждали сверху, НЕТ: ни
перерегулирования, ни автоколебаний — `v_макс` на `ki=50` даже ниже (0.85–1.55 против
0.96–1.93). Берём 30 как начало полки: на 50 не лучше, а запас до неизвестной границы
меньше.

**Почему полка, а не оптимум.** Установившийся наклон против ветра задан САМИМ ветром,
а не гейном: `PWMкр` на всех трёх точках 15/30/50 одинаков (~30–35, при `max_pwm=150` —
23 % хода, насыщения нет). `ki` управляет не величиной наклона, а скоростью, с какой
накопитель до него доходит. На `ki=15` дохождение ещё стоит нескольких лишних метров
сноса; на 30 оно перестаёт быть узким местом — и дальше увеличивать нечего.

⚠️ **Остаток теперь в ПЕРЕХОДЕ, а не в установившемся режиме.** `v_кон` 0.05 м/с при
`v_макс` 0.85–1.9 — весь бюджет ухода (3 м) набирается на входе в висение, пока
накопитель пустой. Следующий выигрыш здесь ищется не в `ki`, а в предзаряде/сокращении
окна скорости, и мерить его надо по `v_макс`/`v_нач`, где `vбок` уже ничего не показывает.

⚠️ Отсутствие автоколебаний на `ki=50` — не индульгенция. Контур замкнут через
СГЛАЖЕННУЮ скорость зрения (окно 0.5 с), она и съедает фазу, на которой обычно рвёт.
Тронешь окно (п. про 0.5 → 0.3 с) — верхнюю границу `ki` надо перепроверять.

⚠️ Свип `B3s` до этого уже отлетал ОДИН РАЗ ВПУСТУЮ: `BS_ROLL_RATE_KI` не была
проброшена, прогоны ушли с `ki=0` и дали правдоподобное «интегратор не помогает».
Проводка чинится, но полагаться на внимательность нельзя — перед прогоном ручки
проверяет `src/lab/check_knobs.sh` (встроен в `calib_run.sh`).

## ТАНГАЖ: ТА ЖЕ ПОЛКА, НО ГРАНИЦА СВЕРХУ ЕСТЬ (свип `C`, 2026-08-12)

Тот же вопрос по продольной оси. Ветер 3 м/с **в нос** (`WIND_DIR_DEG=8`; на висении
борт держит курс +8°), `pitch_rate_kp=100`, крен на своём победителе (`ki=30`), n=3.
Точки взяты как `ki ≈ kp` и вдвое выше: единицы у осей разные, переносится не число,
а форма.

| `pitch_rate_ki` | `vвпер` м/с | уход, м | `v_кон` м/с | размах `vвпер` 1-я треть → последняя | `PWMтг` |
|---|---|---|---|---|---|
| 0 | 0.37 0.29 0.23 | 13–15 | 0.23–0.46 | 0.9 → 0.16 | 33 |
| **100** | **−0.01 −0.01 −0.01** | **0.9–3.1** | **0.04–0.11** | **2.7 → 0.31** | **51** |
| 200 | 0.32 0.31 0.27 | 11–12 | 0.41–1.12 | 2.9 → **3.1** | 100 |

**Победитель `ki=100`, и он ЗАЖАТ С ОБЕИХ СТОРОН** — в отличие от крена, где сверху
была открытая полка. На 200 контур срывается в автоколебания периодом **4.5–4.7 с**.

⚠️ **Полная скорость и «размах за всё висение» тут ВРУТ — оба.** `v_уст` на `ki=200`
равна 0.90–0.95 при `vвпер` всего +0.3: это не снос, это качание (`v_уст` — модуль
скорости, `vвпер` — знаковое среднее, и расхождение втрое означает, что вектор ходит
туда-сюда). А размах `vвпер` за всё окно на `ki=100` и `ki=200` почти одинаков
(2.7 против 3.1) — потому что у обоих есть большой заброс на входе в висение, и он
забивает метрику. Различает их только **первая треть против последней**:

```
ki=100   размах 2.7 → 0.31   СКО 0.07..0.10   заброс ГАСНЕТ
ki=200   размах 2.9 → 3.1    СКО 1.00..1.05   заброс НЕ ГАСНЕТ
```

⚠️ **`PWMтг` растёт 33 → 51 → 100, и это НЕ противоречит «наклон задан ветром».**
Печатается СРЕДНИЙ МОДУЛЬ, поэтому качание его завышает само по себе (100 на `ki=200`
— это размах, а не удерживаемый наклон). Рост 33 → 51 объясняется иначе: на `ki=0`
борт УБЕГАЕТ по ветру со скоростью 0.3 м/с, встречный поток слабее, и наклона нужно
меньше. Настоящий установившийся наклон — это 51 на `ki=100`.

Период автоколебаний 4.5–4.7 с — **тот же, что у продольной оси при `kp=200`** в свипе
`F1` (4.5 с). Значит это собственная мода контура, и высокий `ki` её только возбуждает,
а не создаёт новую.

**Итог по двум осям.** Ветер сносит 3.3–3.5 м/с без стабилизации; П-член сбивает до
0.3–0.7; интегратор — до сотых. Рабочие точки: `roll_rate_ki=30` при `kp=30`,
`pitch_rate_ki=100` при `kp=100`. В нормированном виде **`ki ≈ kp` безопасно на обеих
осях**, `ki/kp = 1.67` (крен) ещё держится, `ki/kp = 2.0` (тангаж) уже рвёт. Это
единственное, что переносится между осями — сами числа нет.

⚠️ **Обе оси теперь упираются в ПЕРЕХОД.** У тангажа заброс на входе даже хуже
кренового: размах `vвпер` 2.7 м/с против 0.9 без интегратора. Установившийся режим
вычищен, весь остаток ухода набирается за первые секунды, пока накопитель пустой.

## ОКНО СКОРОСТИ: КОРОТКОЕ НЕ ПОКУПАЕТ ПЕРЕХОД (свип `S`, 2026-08-12)

Гипотеза из `ToDo5.md`: заброс на входе в висение — это запаздывание оценки скорости,
значит окно МНК 0.5 → 0.3 с должно его срезать. Свип `BS_IPM_WIN` на конфиге-победителе
тангажа (`ki=100`, ветер 3 м/с в нос, n=3). **Гипотеза не подтвердилась.**

| `ipm_win` | размах `vвпер` 1-я треть | последняя | `СКО` кон | уход, м | `v_макс` |
|---|---|---|---|---|---|
| **0.5** | 2.1–3.0 | **0.27–0.45** | **0.06–0.11** | 3.1–3.9 | 1.5–2.0 |
| 0.3 | 2.4–2.5 | 0.47–1.25 | 0.12–0.40 | 2.4–2.9 | 1.2–1.6 |

**Заброс не изменился** (2.5 против 2.5 в среднем), а установившийся режим стал вдвое-
вчетверо грязнее. Итоговая точка при этом чуть ближе (2.4–2.9 против 3.1–3.9) — и это
ровно тот размен, который правило «спокойствие, а не близость» велит отклонять.
**Оставляем 0.5.**

**Почему заброс не срезался — механизм оказался другой.** Он не в запаздывании оценки,
а в ПУСТОМ НАКОПИТЕЛЕ: на входе в висение интегратор ещё не набрал противоветровой
наклон, и борт успевает разогнаться, как бы быстро ни мерилась скорость. Ускорение
измерения не ускоряет ЗАРЯДКУ. Значит лечится это предзарядом накопителя (или
раздельным `ki` на вход и на удержание), а не окном — и `ToDo5` в этой части закрыт.

**Разброс внутри точки — не повод брать лучший прогон.** У `0.3` один из трёх (`S2s3`)
вышел чистым: `v_кон` 0.06, `СКО` 0.12, уход 2.4 м — почти как у `0.5`. Соседи по той же
точке дали 0.34 и 0.39. Это и есть причина правила «n≥3»: по лучшему прогону `0.3`
выглядит победителем, по тройке — проигравшим.

**Как проверить, что ветер вообще долетел до борта.** `WIND_*` в `.env` НИЧЕГО не
доказывает: мета пишется ДО старта стека (так потерялся `B3s`). Прямая улика — колонка
**`PWMтг`** в `wind_drift.py`: это заряд накопителя тангажа, то есть установившийся
наклон против ветра. По всей серии `S` он 52.8–61.0 — накопитель заряжен у всех шести,
ветер был. Без ветра он стоял бы у нуля. Для крена так же читается `PWMкр`.

⚠️ **Предсказание про запас устойчивости подтвердилось.** Короткое окно съедает фазу,
которой прикрыт интегратор: `СКО` в конце висения выросла 0.06–0.11 → 0.12–0.40, то
есть поехала в сторону `ki=200` (там было 1.00–1.05, полный автоколебательный режим).
Разрыва ещё нет, но запас сузился. Кто будет менять окно — **перемеряй верхнюю границу
`ki`**, она намерена под 0.5.

## КУРС ПОД ВЕТРОМ: НИЧЕГО НЕ ЗВЕНИТ (свип `YW`, 2026-08-13)

Жалоба: «при 5 м/с курс раскачивает». Пара точек по 3 прогона на конфиге-победителе,
`hover20`, разбор — `src/lab/yaw_ring.py`:

- **`YW1s`** — 5 м/с в нос (`WIND_DIR_DEG=8`): меняется ТОЛЬКО сила ветра;
- **`YW2s`** — 3 м/с по диагонали (`53`): ветер СЛАБЕЕ, но косой, грузит обе оси.

Пара выбрана как разделительный тест: собственный звон контура растёт с СИЛОЙ, фантом
от сноса — с КОСИЗНОЙ. Ответ оказался «ни то, ни другое».

| прогон | ветер | СКО курса° | размах° | `|PWM|` | сат | corr(`flow_yaw`, ωz) |
|---|---|---|---|---|---|---|
| `YW1s1-3` | 5 в нос | 1.17–2.31 | 5.1–13.0 | 4.4–5.6 | **0.00** | 0.20–0.22 |
| `YW2s1-2` | 3 диаг | 0.99–1.11 | 5.6–6.0 | 4.4–5.6 | **0.00** | 0.20–0.21 |
| **`G2s`/`G3s`** | **ШТИЛЬ** | **0.82–4.21** | **5.0–14.5** | 3.6–8.3 | 0.00 | 0.12–0.47 |

**Ветряные прогоны лежат ВНУТРИ штилевого разброса, местами спокойнее его.** Значит
раскачки от ветра нет вовсе: ±3–7° по курсу — это фон оси в любом прогоне. Контроль
штилем тут решил всё: без него мы бы «нашли» у ветра эффект и пошли крутить гейны.

**Контур при этом почти молчит.** Курсовой PWM 4–8 из 150, насыщения НОЛЬ ни в одном
прогоне. Звенеть от избытка гейна нечему — ось работает практически разомкнуто.

⚠️ **Низкая `corr` на висении — это не сломанный канал, а нечего мерить.** На тех же
прогонах с КОМАНДНЫМ разворотом (`Y4_kp20`, `Y5s1-2`) связь `flow_yaw` с истинной ωz
**0.73–0.81**. На висении истинная ωz ≈3 °/с, то есть ~1 px/кадр сигнала — отношение
сигнал/шум и даёт 0.2. Вывод «канал курса врёт» из висения делать НЕЛЬЗЯ; проверять
канал надо там, где ось реально крутится.

⚠️ `hover20` слишком короток для периода: «период» 12.3 с в 20-секундном окне — это
один взмах, а не колебание. Числа `T` в таблице читать только как порядок.

**`YW2s3` потерян не по вине курса.** `conf = 0.00` ВЕСЬ полёт — канал вида сверху не
запустился, стабилизации не было вообще, борт разогнало ветром до 6.7 м/с и выбросило
за забор на 25-й секунде. Это отдельная болезнь старта (1 прогон из 6), и она опаснее
всего, что нашлось про курс: молча выключает ВСЕ три оси.

### `YW1s1`: разворот на 90° сразу после отрыва — контур сработал ПРАВИЛЬНО

Тот самый прогон, что дал выброс `СКО 2.31` в точке 5 м/с. Разобран покадрово, и это
не ветер: **дрон развернул сам себя, честно отработав один мусорный кадр.**

Сначала пришлось установить знак цепочки, без него разбор бессмыслен: наклон истинной
ωz на единицу курсового PWM = **−0.35…−0.39 °/с** (corr −0.64…−0.74, прогоны с
командным разворотом `Y5s1-2`, `Y4_kp20`). То есть **отрицательный PWM = влево**.

| t, с | z | `flow_yaw` | накопитель | PWM | ωz | курс |
|---|---|---|---|---|---|---|
| 9.25 | 0.5 | — | 0 | 0 | 6 | −4.6° |
| **9.50** | **0.7** | **−43.81** | **−32.0** | 0 | 9 | −3.0° |
| 10.25 | 1.6 | +14.6 | −25.6 | **−150** | 61 | +16.5° |
| 10.75 | 2.1 | +24.8 | −13.3 | −122 | **81** | +54.7° |
| 11.25 | 2.6 | +18.3 | **−0.1** | +23 | 32 | +84.1° |
| 16.0 | 3.4 | +1.5 | 0.0 | +9 | 4 | +95.8° |

В момент отрыва (z 0.2 → 0.7) первый кадр потока дал `flow_yaw = −43.8 px/кадр` при
висенческих ±1. Накопитель мгновенно набрал −32 ед., а это по калиброванному масштабу
S=0.324 ровно **−99° фантомного разворота**. Контур упёрся в −150 PWM и **физически
довернул борт влево на 95°** — то есть на тот самый угол. Как только накопитель
отработался в ноль (t=11.25), команда пропала, и борт замер на новом курсе.

**Почему новый курс остался навсегда.** У оси нет абсолютной опоры: `_head` обнуляется
на входе в сегмент, утечка тянет к УСТАВКЕ, а не к исходному курсу. Фантом, попавший
в накопитель на взлёте, запекается смещением курса на весь полёт. В доке это записано
как осознанная цена (абсолютный курс — работа NN1/VINS), но подразумевались 3–4°
от смещения `flow_yaw`, а не 99° от одного кадра.

**Гейт `conf_min=0.05` выброс пропустил** — на том кадре достоверность была 0.18.
Отсева невозможных значений `flow_yaw` не было вообще: сырое число из оценщика шло
в накопитель как есть.

**ЛЕЧЕНИЕ (сделано, `DpYawHold`):**
1. **`yaw_max_rate` = 100 °/с** — потолок правдоподобия кадра. 43.8 px/кадр это 135 °/с
   у висящего борта, физически невозможно; полный стик даёт 28.65 °/с, так что запас
   троекратный. Кадр выше потолка **выбрасывается, а не подрезается**: подрезка влила бы
   максимально допустимый фантом, то есть заменила бы разворот на 99° разворотом на 75°.
2. **`yaw_arm_frames` = 5** — накопитель не копит, пока не пришло 5 подряд кадров с
   `conf ≥ conf_full`. Накопителю нужен СВОЙ порог, строже выходного: выходной гейт
   умеет ослабить команду, но не умеет не запомнить фантом.

Каждый из двух ловит это событие сам по себе (`0.18 < 0.20` и `43.8 > 32.4`) — оставлены
оба, потому что закрывают разные отказы: первый — правдоподобие, второй — доверие.
Цена: ось молчит первые ~0.25 с каждого сегмента. Тесты — `test_flow_strategies.py`.

### Проверка гейтов в полёте (`YW3s`, 3 прогона, 5 м/с в нос)

Тот же конфиг, что `YW1s`, плюс `BS_YAW_MAX_RATE=100 BS_YAW_ARM_FRAMES=5` (это уже
дефолты, в мете записаны явно). Окно взлёта — 10 с от `z>0.3 м`:

| прогон | max `|flow_yaw|` | max `|PWM|` | max `|ωz|`, °/с | Δкурс за взлёт |
|---|---|---|---|---|
| `YW1s1` | **64.2** | **150** (сат) | **82.8** | **+96.4°** |
| `YW1s2` | 23.1 | 29 | 13.4 | +0.8° |
| `YW1s3` | 25.0 | **129** | 61.1 | **+27.5°** |
| `YW3s1` | 7.5 | 48 | 18.1 | +5.2° |
| `YW3s2` | 9.2 | 27 | 12.5 | −0.4° |
| `YW3s3` | 20.1 | 37 | 9.0 | −0.1° |

Заодно выяснилось, что **болезнь была в ДВУХ прогонах из трёх**, а не в одном: `YW1s3`
довернуло на 27° тем же механизмом (PWM 129 из 150), просто это не бросалось в глаза на
фоне 96° у `YW1s1`. С гейтами максимальный доворот 5.2°, насыщения нет ни разу.

Висение не пострадало: `СКО курса` 0.76–3.48°, размах 3.7–12.8, `|PWM|` 3.7–5.3, сат 0.00 —
та же полоса, что у `YW1s` (1.17–2.31 / 5.1–13.0 / 4.4–5.6). Молчание оси первые 0.25 с
не сломало ничего.

⚠️ **Три чистых взлёта доказывают «не сломали», а не «починили»** — событие редкое, и
`YW1s2` взлетел чисто вообще без лечения. Доказательство починки — юнит-тест, где
сценарий `YW1s1` (43.8 px при `conf=0.18`) прогоняется на дефолтах и накопитель остаётся
в нуле. Показательно другое: `YW3s3` поймал в окне взлёта 20.1 px/кадр (гейт
правдоподобия 32.4 такое пропускает) и всё равно дал −0.1° — сработало ВЗВЕДЕНИЕ.
То есть из двух лечений в реальном взлёте несёт нагрузку в основном второе.

⚠️ **Осталось незакрытым:** та же болезнь на КРЕНЕ и ТАНГАЖЕ. У них сигнал — скорость,
и мусорный кадр даёт разовый пинок, а не запечённое смещение, так что цена ниже. Но
`YW2s3` (`conf=0.00` весь полёт) показывает, что доверие к каналу вида сверху не
ограничено нигде.

## ГОТОВЫЙ КОНФИГ: лучший трёхосевой на сегодня (`DpHoldM`, серия `P2s`)

Дальше по документу — методика, как настраивать с нуля. Здесь то, что настройкой уже
получено: строка запуска, которую надо брать, если нужен просто рабочий демпфер.

⚠️ Конфиг ниже — БЕЗ ветра и с `roll_rate_ki=0`. Под ветром к нему обязателен
`BS_ROLL_RATE_KI` (см. секцию выше): без интегратора борт уносит за забор за 20 с.

```bash
cd /root/13.17/docker/sim && setsid nohup env \
  NAME=A1s1 CPU=1 RES=960x540 FRESH=1 GDRIVE_UP=1 MP4=1 \
  BS_STAB=DpHoldM \
  BS_MISSION="climb3,hover40,land" \
  BS_ROLL_RATE_KP=30  BS_ROLL_OSIGN=1  BS_ROLL_IMAX=150 \
  BS_PITCH_RATE_KP=100 BS_PITCH_OSIGN=1 \
  BS_YAW_KP=20 BS_YAW_KI=0 BS_YAW_KD=6 BS_YAW_SMOOTH=5 \
  BS_IPM_MODEL=exact BS_IPM_DEROT=1.0 BS_IPM_WZ_TAU=2.0 BS_ATT_EXTRAP=0 \
  BS_FENCE=25 BS_FLOW_OBS=1 BS_SLEW=300 BS_THROTTLE_CLIMB=1800 \
  BS_ARM_BUDGET=80 BS_CLIMB_BUDGET=120 BS_MODE_BUDGET=80 BS_LAND_BUDGET=180 \
  bash /root/13.17/src/lab/calib_run.sh > output/A1s1.log 2>&1 < /dev/null &
```

Серией на N повторов — то же самое, только `N=5 PREFIX=A1s` и `hover_series.sh` вместо
`calib_run.sh` (имя тогда не задаётся, его лепит серия).

**Состав.** `DpHoldM` = `DpRollRate` (боковая скорость, м/с) + `DpPitchRate` (продольная,
м/с) + `DpYawHold` (курс, px). Обе горизонтальные оси — демпферы СКОРОСТИ на канале вида
сверху, курс — холд по положению.

**Почему выписано явно, хотя все эти числа уже стоят дефолтами в `config.py`.** Дефолт
может уехать, а прогон должен воспроизводиться из строки. Минимальная эквивалентная
запись — `BS_STAB=DpHoldM` плюс бюджеты фаз.

Три значения, которые НЕ надо «улучшать по интуиции»:

- `ROLL_RATE_KP=30` — это не «мало». 120 давало уход 8.0 ± 7.5 м (предельный цикл), 15
  сдавалось совсем. 30 — дно чаши.
- `PITCH_OSIGN=+1` при `ROLL_OSIGN=+1` — в env знаки одинаковы, хотя провод у осей
  противоположный (см. `config.py`, комментарий у `pitch_osign`). Не опечатка.
- `IPM_DEROT=1.0` **работает только в паре с** `IPM_WZ_TAU=2.0`. Порознь каждое хуже, чем
  оба выключенных: вычитание без снятого нуля гироскопа интегрирует сам ноль.

**Что этот конфиг даёт** (штиль, канонический стенд CPU=1, 960×540):

| замер | уход | выходов за забор |
|---|---|---|
| `P2s`, 5 прогонов, висение 20 с | 3.38 ± 1.65 м | 0 / 5 |
| `T2s1`, висение 40 с | 1.47 м | — |
| контроль `Q2s` (то же, но `IPM_DEROT=0`), 5 прогонов | 8.77 ± 8.05 м | 2 / 5 (12.5 и 21.1 м) |

**Чего он не умеет — держать против ветра.** При 3 м/с бортом сносит на забор, причём
PWM крена всего 39–46 из 150: контур не упирается в потолок, он упирается в ОТСУТСТВИЕ
ошибки. Это структурное свойство демпфера скорости, а не недостаток настройки —
против постоянной силы равновесие достигается на ненулевой скорости. Гейном не лечится
(выше — предельный цикл). Лечится только членом, который даёт команду при нулевой
скорости, то есть накоплением: утечным интегралом либо позиционным слоем поверх.

⚠️ **Сравнивать прогоны можно только внутри одного дня и одного стека.** Стенд
воспроизводим внутри серии и НЕ воспроизводим между кампаниями: тот же конфиг на разных
днях давал разное. Любое «стало лучше» — это контроль, снятый сегодня, тем же стеком.

## Одиннадцать правил, из которых растёт весь план

(Первые семь — итог кампаний A–R, ещё четыре добавила D2s–F1, они в конце документа.)

1. **Датчик раньше контура.** Настраивать петлю, не зная corr/крутизну/задержку её
   сигнала, — это подгонка под шум. Цена нарушения в этой кампании: серия `K2s` (kd 5000
   → 9000) сделала хуже, потому что D-член был наполовину шумом; `L1_mvfwd` показал, что
   гейн канала положения гуляет 0.35 / 1.81 / 1.46 внутри ОДНОГО полёта.
2. **Одна ось за раз, остальные держит оракул.** `GzRollHold`/`GzPitchHold`/`GzYawHold`
   держат по истинной позе Gazebo. Ось под настройкой — единственная, где работает `Dp*`;
   иначе увод одной оси уезжает в сигнал другой (крен создаёт боковой поток, который
   тангаж читает как масштаб).
3. **Ничего не значит одиночный прогон.** Разброс стенда снят шестью одинаковыми
   прогонами: **11.6 ± 0.9 м** (8%). При одном и том же конфиге встречались 7.7 и 13.1 м.
   Любое «стало лучше» — это серия из трёх (`hover_series.sh`) и t-критерий.
4. **Одна ручка на серию.** Две ручки сразу — и результат не атрибутируется. Исключение:
   ручки, связанные физикой (kp и kd меняются парой при заданном ζ), но тогда пара
   считается одним изменением и обосновывается формулой ДО прогона.
5. **Прогон атомарен.** Перезапуск всего стека → `make wait` → полёт → бэг. Ручной
   рестарт отдельного процесса даёт ложные диагнозы. Скрипты `src/lab/*.sh` НЕ править,
   пока прогон идёт (bash дочитывает файл на ходу — миссия ушла на второй круг).
6. **Порог ищут по НАСЫЩЕНИЮ, а не по дрожи.** Интуиция «слишком большой гейн → мелкая
   дрожь» на этих осях НЕ работает. Замер R6 (`roll_kp` 96 и 160): частота смен знака
   выхода 0.3–0.4 /с — НИЖЕ, чем на рабочих 32–48, то есть по «дрожи» порога нет вовсе.
   А выход при этом стоял в клампе 150 на 40–50% времени, и борт болтало 0.72 м/с против
   0.21 у базы. Большой размах PWM при РЕДКОЙ смене знака = медленные качели
   недодемпфированной петли в насыщении. То же у тангажа при `kd=5000` (Д-пик ровно 150
   половину висения). **Смотреть долю потолка; PWM СКО в одиночку метрикой не является** —
   она растёт линейно с гейном по построению (27→36→57→107 на kp 16→32→48→96).
7. **Команду проверять по СЕМАНТИКЕ, а не по факту «доехала».** Приёмка «стик даёт
   заданную скорость» проходит и у оси, которая не умеет останавливаться. Для `rate`-оси
   семантика — «отпустил → скорость в ноль», и её надо мерить отдельным числом (выбег
   после снятия команды). В кампании R выбег 1.5–4.3 м нашёлся ТОЛЬКО потому, что
   проверялась семантика; по приёмке команды ось проходила.

## Как запускать: прогоны и разбор

**Серия/свип — heredoc'ом целиком, НЕ `sed`'ом по предыдущему скрипту.** Замена `N=1` на
`N=3` однажды попала в `BS_ROLL_OSIGN=1` и в хвост `BS_ROLL_CMD_GAIN=10` — серия ушла в
воздух с перевёрнутым знаком оси и тройным гейном. Не падает, а даёт правдоподобно
странный полёт.

```bash
cat > /tmp/rN_run.sh <<'EOF'
#!/bin/bash
for pair in "R8a:0" "R8b:1.5"; do          # свип: одиночки, PREFIX:значение
    pre="${pair%%:*}"; val="${pair##*:}"
    env N=1 PREFIX="$pre" \
        BS_STAB=GzPitchHold+GzYawHold+DpRollHold \
        BS_MISSION=climb3,hover5,mv_right6,hover8,mv_left6,hover8,land \
        BS_SLEW=300 BS_FENCE=25 BS_FLOW_OBS=1 BS_THROTTLE_CLIMB=1800 \
        BS_MODE_BUDGET=80 BS_ARM_BUDGET=80 BS_CLIMB_BUDGET=120 BS_LAND_BUDGET=180 \
        BS_ROLL_KP=48 BS_ROLL_KI="$val" BS_ROLL_IMAX=150 BS_ROLL_OSIGN=1 \
        BS_ROLL_SMOOTH=25 BS_ROLL_CMD_GAIN=10 \
        GDRIVE_UP=1 MP4=1 \
        bash /root/13.17/src/lab/hover_series.sh
done
echo "######## СВИП ЗАВЕРШЁН ########"
EOF
grep -E "N=|KP=|KI=|OSIGN|CMD_GAIN" /tmp/rN_run.sh    # ГЛАЗАМИ сверить ДО запуска
setsid nohup bash /tmp/rN_run.sh > docker/sim/output/RN.log 2>&1 < /dev/null & disown
```
Серия n=3 — тот же скрипт без цикла, с `N=3 PREFIX=R9s`. `GDRIVE_UP=1 MP4=1` —
обязательно (видео на Drive). Отсоединённый запуск (`setsid nohup … & disown`) переживает
обрыв ssh; ждать — `until grep -q "ЗАВЕРШЁН" …; do sleep 60; done`.

**Разбор — из образа, без поднятого стека.** Контейнер `p1317_nav` между прогонами лежит,
а перезапускать его ради чтения бэга нельзя (дисциплина прогона). Одноразовый контейнер
с бэгами и скриптами только на чтение:

```bash
D="docker run --rm --network none \
   -v /root/13.17/docker/sim/output:/out:ro -v /root/13.17/src/lab:/lab:ro \
   sim-nav:latest bash -lc"
$D "source /opt/ros/humble/setup.bash; python3 /lab/<скрипт> <аргументы>"
```

| Что мерим | Скрипт | Аргументы |
|---|---|---|
| уход/размах/период/потолок/H СКО | `hover_stats.py` | `/out/N1s1_bag /out/N1s2_bag …` |
| разложение выхода на П и Д | `pd_split.py` | `KD=1500 … /out/N1s1_bag` (env `KD`) |
| здоровье опоры (kf_n, сегменты, пересевы) | `kf_health.py` | `/out/N1s1_bag …` |
| паспорт бокового датчика | `roll_sensor_check.py` | env `BAG=/out/…` ⚠️ бэг новее `be8688f` |
| калибровка боковой команды | `roll_cmd_check.py` | env `BAG=`, `GAIN=`, `S_LAT=` |
| выбег после снятия команды | `roll_brake_check.py` | `R7s1:48 R4s1:16 …` (бэг:значение) |
| потолок/период/размах на висении | `roll_osc_check.py` | то же |
| чистый снос (метрика для `ki`) | `roll_drift_check.py` | то же |
| сегменты курса, калибровка `S` | `yaw_seg_check.py` | env `BAG=`, `S=` |

⚠️ Пути монтирования — АБСОЛЮТНЫЕ. `-v "$PWD/output:/out:ro"` молча подставит не тот
каталог, если оболочка не в `docker/sim`, и бэг «не найдётся» при живом файле.

## Фаза 0 — паспорт стенда (2 прогона)

Прежде чем что-то улучшать, надо знать, что такое «не изменилось».

```bash
N=2 PREFIX=B0s BS_STAB=none BS_MISSION="climb3,hover40,land" bash src/lab/hover_series.sh
```

Без стабилизаторов, открытый контур. Снимаем: свободный уход за 40 с, его разброс, RTF,
темп кадров перцепта, темп IMU. Это нулевая линия и одновременно проверка, что стенд
здоров (не «Main loop slow», не просевший fps).

## Фаза 1 — паспорт ПРОВОДА (3 прогона, по одному на ось)

Что команда делает с бортом, БЕЗ обратной связи. `BS_STAB=none`, движение задаётся
профилем миссии:

| ось | миссия | что достаём |
|---|---|---|
| крен | `climb3,mv_right6,hover4,land` | °/PWM, τ, знак провода |
| тангаж | `climb3,mv_fwd6,hover4,land` | то же, продольно |
| рыскание | `climb3,mv_cw6,hover4,land` | °/с на PWM |

Эталонные значения этой кампании: **k = 0.0735 °/PWM**, **τ = 0.27 ± 0.03 с**,
**a₁ = 0.0115 м/с²/PWM**, полный стик 400 PWM. Знак провода у осей РАЗНЫЙ и это не
опечатка: у крена PWM>1500 = разгон вправо, у тангажа PWM>1500 = нос вверх = разгон
НАЗАД. Отсюда `roll_osign=+1`, `pitch_osign=+1` при однознаковых сигналах.

Здесь n=1 достаточно: эффект на порядок больше разброса. На знаке — не экономить, это
единственная ошибка, которая переводит демпфер в РАЗГОН (замер `H4_mask`: борт
разогнало до 12.8 м/с и положило раму на −12.9°).

## Фаза 2 — паспорт ДАТЧИКА (0 новых прогонов, офлайн по бэгам фазы 1)

Самая дешёвая и самая доходная фаза: считается по уже снятым бэгам, полётов не нужно.
Инструмент — `src/lab/kf_vel_check.py` (переигрывает боевой `FlowEstimator` по кадрам
бэга) и `src/lab/mvfwd_check.py`.

По каждой оси считаем против истины (одометрия Gazebo):

| величина | зачем | порог годности |
|---|---|---|
| corr сигнала с истиной | видит ли ось вообще | > 0.8 |
| крутизна (ед./м или ед./(м/с)) | масштаб для пересчёта гейнов | стабильна ±20% по сегментам |
| остаток производной | можно ли вообще ставить kd | < 30% рабочей скорости |
| задержка контура | потолок полосы | измерить, а не предполагать |

**Это ворота.** Ось, не прошедшая пороги, в фазу настройки НЕ идёт — сначала чинится
канал. Пример из кампании: продольная скорость в межкадровом сдвиге имеет corr −0.22
(канал слеп) — поэтому тангаж переведён на `kf_logs` (масштаб), а не докручен гейнами.
Второй пример: ω из `gz_imu` с low-pass 5 Гц роняла corr положения 0.89 → 0.61; переход
на MAVROS ATTITUDE поднял её обратно — это была правка ПЕРЦЕПТА, а не контура.

## Фаза 3 — РЫСКАНИЕ первым (5–7 прогонов)

Почему первым: петля первого порядка (самая простая), а её ошибка загрязняет обе
поступательные оси — перцепт вычитает вращательный поток по ω, и остаточное рыскание
уезжает прямо в оценку смещения.

```bash
BS_STAB="GzRollHold+GzPitchHold+DpYawHold" BS_MISSION="climb3,hover40,land" \
  BS_YAW_KP=<свип> BS_YAW_KI=0
```

Свип `kp` одиночными прогонами (4, 6, 10, 16) — ищем порог дрожи; берём вдвое ниже
порога. Затем **проверка победителя серией n=3** против базы. `ki` пробуем ТОЛЬКО если
виден устойчивый снос курса; в этой кампании интеграл оказался вреден (смещение
`yaw_flow` копится) — победил `kp=6, ki=0, сглаживание 5`.

Отдельно: сглаживание — это ВРЕМЕННОЕ окно, а не число кадров. При смене fps его надо
пересчитывать, иначе настройка «переезжает» вместе с нагрузкой стенда.

```bash
$D "source /opt/ros/humble/setup.bash; BAG=/out/Y5s1_bag S=0.324 python3 /lab/yaw_seg_check.py"
```

## Фаза 4 — КРЕН (6–8 прогонов; фактически ушло 9 — кампания R)

Петля по СКОРОСТИ (сигнал `flow_lateral` — межкадровый сдвиг), первого порядка. Звенеть
не может, поэтому настраивается быстро. ⚠️ «Не может звенеть» ОТНОСИТСЯ К ПЕТЛЕ, а не к
контуру целиком: при большом `kp` выход упирается в кламп, и связка «насыщенный выход +
инерция борта» качается как угодно (замер R6 ниже).

Миссия для крена — НЕ `hover40`, а с командными проездами: половина дефектов оси видна
только под командой.

```bash
BS_STAB="GzPitchHold+GzYawHold+DpRollHold" \
BS_MISSION="climb3,hover5,mv_right6,hover8,mv_left6,hover8,land" \
  BS_ROLL_KP=<свип> BS_ROLL_KI=3 BS_ROLL_IMAX=150 BS_ROLL_CMD_GAIN=10
```

1. **Сначала КОМАНДА, потом гейны.** Проверить знак и калибровать `cmd_gain` по
   `roll_cmd_check.py` (заказ `c_right·gain/S_lat` против истинной скорости). Замер R2/R3:
   знак был ЗЕРКАЛЕН (`mv_right` уводил влево на 4.38 м), а сам гейн 10 оказался верен
   (10.7 ± 0.9). Настраивать гейны на зеркальной команде бессмысленно: промежуточный
   разбор R2 дал «77% отдачи и смещение −0.23 м/с», и это целиком артефакт зеркала.
2. `kp` свипом одиночных (16, 32, 48, 96, 160) → порог → вдвое ниже. **Порог тут — доля
   ПОТОЛКА, а не дрожь** (см. правило 6).
3. `ki` — здесь он ОСМЫСЛЕН (петля по скорости позицию не держит, интеграл её
   подтягивает), свип (0, 1.5, 3) при `imax` 150. ⚠️ В кампании R так и НЕ проверен:
   `ki=3` стоит по наследству от монолита.
4. Победитель — серией n=3 против базы, и мерить ОБЕ стороны размена (см. приёмку).

**Приёмка — две метрики, а не одна.** Одной мало: удержание и торможение тянут `kp` в
разные стороны, и по любой одной ось «принимается» с невыявленным дефектом.

- *удержание*: доля потолка, средняя `|v|` и РАЗМАХ `v` на свободном висении
  (`roll_osc_check.py`). Эталон на `kp=48`: 5% потолка, |v| 0.29 м/с, размах 3.1–3.9 м/с;
- *торможение*: выбег вбок за 6 с ПОСЛЕ снятия команды (`roll_brake_check.py`). Эталон:
  **1.13 ± 0.43 м** вправо, **0.81 ± 0.35 м** влево;
- *снос* (только для свипа `ki`): `roll_drift_check.py` — чистое смещение по свободным
  участкам и отношение `|снос|/|путь|`. База на `ki=3`: −2.29 ± 1.65 м (n=3),
  направленность 0.09–0.44. ⚠️ Разброс почти равен эффекту — одиночка значима, только
  если вылетит далеко за полосу. Именно на этом обжёгся свип R8: `ki=0` дал −0.67 м и
  выглядел победителем, а подтверждающая тройка R9s — −0.72 / −2.01 / −2.75.

```bash
$D "source /opt/ros/humble/setup.bash;
    python3 /lab/roll_cmd_check.py                        # знак и gain, env BAG=
    python3 /lab/roll_brake_check.py  R7s1:48 R4s1:16     # выбег
    python3 /lab/roll_osc_check.py    R7s1:48 R4s1:16     # потолок/период
    python3 /lab/roll_drift_check.py  R8a1:0  R7s1:3"     # снос (для ki)
```

Итог кампании R: `kp` 16 → **48** (выбег 3.05 → 1.13 м ценой |v| 0.23 → 0.29 при
неизменном размахе), `cmd_gain=10` подтверждён, `ki` 3 → **0** — свип R8/R9 показал,
что ручка не работает (разброс внутри значения больше разницы между значениями), а
риск накопления смещения датчика известен по рысканию.

⚠️ **Отрицательный результат свипа — это результат, а не повод свипить дальше.** Если
две-три точки легли внутри полосы разброса, ручка НЕ «требует более тонкого шага»: она
не влияет на выбранную метрику, и правильный ход — поставить безрисковое значение и
идти дальше. Дробить шаг имеет смысл, только когда полосы уже разошлись.

## Фаза 5 — ТАНГАЖ последним (8–12 прогонов, и только после ворот фазы 2)

> ⚠️ **УСТАРЕЛО ЦЕЛИКОМ.** Описывает настройку петли по ПОЛОЖЕНИЮ, которой больше нет.
> Оставлено как история: именно эта фаза съела пять серий (E2/E3/E5/E6/E7) и не сдвинула
> уход с двадцати метров. Замена — «Фаза 5′» в конце документа.

Петля по ПОЛОЖЕНИЮ (`kf_logs` — логарифм масштаба), второго порядка: обязана звенеть без
честного демпфера. Самая дорогая ось, поэтому идёт последней.

```bash
BS_STAB="GzRollHold+GzYawHold+DpPitchHold" BS_MISSION="climb3,hover40,land" \
  BS_PITCH_KP=<из ω_n> BS_PITCH_KD=<из ζ> BS_PITCH_KI=0
```

Гейны берутся НЕ свипом, а расчётом от паспорта, и свипом только проверяются:

```
kp → ω_n = √(kp · крутизна · a₁)      целимся в ω_n втрое ниже 1/задержка_контура
kd → 2ζω_n                            ζ ≈ 0.9, но не выше, чем позволяет остаток D-канала
ki = 0                                интеграл по положению копит то, что и так копит kp
```

Обязательная проверка ДО прогона: `kd · (рабочая скорость)` должно быть заметно меньше
потолка `max_pwm=150`, иначе демпфер уйдёт в насыщение на собственном шуме. Именно это
убило `K2s` (kd=9000): 15.4 ± 4.0 м против базовых 11.6 ± 0.9 (t=2.33 при пороге 2.31).

**Итог кампании N (`pitch_kd`): расчёт указал не туда, решила доля потолка.** Расчётное
почти критическое ζ≈0.98 давало `kd=5000` — и на нём Д-пик у ВСЕХ прогонов ровно 150,
то есть половину висения выход стоял в клампе и П-член не значил ничего. `kd=1500`:
доля потолка 42–54% → 24–35%, вклад `kf_vel` в выход 111 → 57 СКО при НЕизменном
`|kf_vel|` (0.030) — то есть упал не сигнал, а гейн шума.

Ниже 1500 нельзя, и причина не в средней ошибке, а в РЕЖИМЕ. Прогон либо держится с
размахом 11–22 м, либо срывается в 28–38 м; промежуточных нет, и доля срывов растёт по
мере снижения `kd`:

| `kd` | 5000 | 1500 | 700 | ≤300 |
|---|---|---|---|---|
| срывов | 1 из 6 | **0 из 3** | 3 из 7 | 2 из 2 |

Одиночка на `kd=700` дала лучший прогон кампании (уход 10.7 м) — и серия n=3 на том же
значении дала 9.8 / 20.2 / 34.9. Гейн не меняет амплитуду плавно, он двигает контур к
границе устойчивости, а по какую сторону ляжет конкретный полёт, решает начальное
возмущение. **Мерить долю СРЫВОВ, а не среднее по серии.**

⚠️ Ложный след, чтобы не идти повторно: СКО высоты почти монотонно совпадало с
горизонтальным уходом, и напрашивалась цепочка «ушла высота → опора пересевается →
сегмент выбрасывается → П слепнет → уход». Опыт N4s (`kf_alt_max` 0.06 → 0.25, всё
прочее то же) обнулил выбросы (11/12/36 → 0/0/1) — и срыв всё равно случился при НУЛЕ
выбросов. Высота оказалась симптомом (срыв → крен → просадка тяги), а не причиной.

Разбор кампании тангажа:

```bash
$D "source /opt/ros/humble/setup.bash;
    python3 /lab/hover_stats.py /out/N1s{1,2,3}_bag       # уход/размах/потолок/H СКО
    KD=1500 python3 /lab/pd_split.py /out/N1s1_bag        # выход на П и Д
    python3 /lab/kf_health.py /out/N1s{1,2,3}_bag"        # здоровье опоры
```

Диагностика звона по бэгу: период автоколебания. Если при СНИЖЕНИИ `kp` период
УКОРАЧИВАЕТСЯ (замер `K4s1`: 6.3 с против базовых 7.4–8.6) — это не второй порядок, а
лимит-цикл по задержке/насыщению, и лечится он не гейнами, а снятием насыщения
(`slew`, потолок) или укорочением задержки.

## Фаза 6 — общие ручки (4–6 прогонов)

Это НЕ per-axis PID, их настраивают после осей, серией каждую:

- **`slew`** (PWM/с, ограничение скорости изменения выхода). Мало — борт отрабатывает
  огибающую вместо значений; много — доезжает чаттер. Признак «мало»: доля кадров в
  потолке ±150 (в `L1_mvfwd` было 46%). Текущая проверка: серия `M1s` на 800 против 300.
- **окна сглаживания** (`roll_smooth`, `pitch_smooth`, `yaw_smooth`) — пересчитывать при
  смене fps, см. фазу 3.
- **`cmd_gain`** — впрыск команд. У осей-скоростей это цель скорости, у оси-положения —
  скорость уставки (`control.md`). Проверяется миссией
  `climb3,hover10,mv_fwd5,hover20,land` по трём каналам разом: `/flow_dbg5` (уставка),
  `/flow_dbg3` (зрение), одометрия (истина).

## Фаза 7 — возмущение (6 прогонов)

Настройка, проверенная только на тихом стенде, ничего не стоит.

```bash
WIND_SPD=3 WIND_DIR_DEG=<боком к носу> …   # серия n=3
WIND_SPD=5 …                               # серия n=3
```

Ветер в Gazebo откалиброван замером (не по формуле плагина — она ошиблась впятеро):
**F ≈ 1.46 · factor² · v** Н, то есть отклик КВАДРАТИЧЕН по `WIND_FACTOR` и ЛИНЕЕН по
скорости ветра. Воспроизводимость силы 2–4%. Эталон: ветер 3 и 5 м/с стоят статистически
незначимых +1.8 / +1.4 м к уходу.

## Бюджет

| фаза | прогонов | зачем |
|---|---|---|
| 0 стенд | 2 | что такое «не изменилось» |
| 1 провод | 3 | °/PWM, τ, ЗНАКИ |
| 2 датчик | 0 (офлайн) | ворота: годна ли ось к настройке |
| 3 рыскание | 5–7 | чистит ω для обеих поступательных |
| 4 крен | 6–8 | петля по скорости |
| 5 тангаж | 8–12 | петля по положению |
| 6 общие ручки | 4–6 | slew, окна, cmd_gain |
| 7 возмущение | 6 | подтверждение под ветром |
| **итого** | **34–44** | ≈ 12–15 часов стенного времени |

Каждый прогон — 15–20 минут на CPU-боксе; серия из трёх — около часа. Бэги потрошатся
после каждого прогона (`STRIP=1`), иначе три по 2.6 ГБ не влезут; видео уходит на Drive
под именем прогона.

## Чего НЕ делать (всё это уже стоило времени)

- **Не выводить из одного прогона.** «J4 дал 7.7 м, значит проводка ω помогла» — оказалось
  выбросом −4.2σ от базы, которую тогда ещё не измерили.
- **Не поднимать kd против шумной производной.** Рост kd усиливает шум ровно настолько
  же, насколько демпфирует.
- **Не верить документации плагина/симулятора вместо замера.** Формула ветра ошиблась
  впятеро; предсказание «квадратично по скорости» тоже оказалось неверным (линейно).
- **Не менять источник сигнала «на всякий случай».** RAW_IMU (20.8 Гц) выглядел лучше
  ATTITUDE (12.5 Гц), а нёс тряску рамы 7.5 Гц — стало хуже.
- **Не править `src/lab/*.sh` во время прогона.** Bash дочитывает скрипт по офсету: сдвиг
  байтов увёл миссию на второй круг в тот же бэг и убил заливку видео.
- **Не менять две ручки сразу** и не сравнивать серии, снятые на разных мирах/разрешениях
  камеры: интринсики и текстуры входят в крутизну канала напрямую.
- **Не мерить паспорт датчика по бэгу, снятому до правки перцепта.** `L1_scale2ax` старше
  правки маски (`be8688f`), и паспорт по нему дал corr +0.09 при крутизне 0.414 — это
  задокументированный «до маски» S_lat=0.4 против 2.42 после. Слепой канал двухнедельной
  давности чуть не стал приговором рабочей оси. Сверять `# commit` в `.env` бэга.
- **Не настраивать ось, чья команда не пишется в бэг.** У `rate`-осей команды не было
  вовсе (`/flow_dbg5` шлёт только `pos`-оси) — сегменты пришлось резать по истинной
  скорости, и откат борта после торможения засчитался как ещё одна команда: три «проезда»
  на две команды миссии, разброс гейна 26%. Чинится записью цели (`rate_dbg` →
  `/flow_dbg7`), а не аккуратностью нарезки.
- **Не доверять юнит-тесту знака, если он писался под реализацию.** Тест крена БЫЛ и
  кодировал то же зеркало: снос ВЛЕВО гасился стиком ВПРАВО. Знак в тесте выводить из
  физики (контур — трекер скорости: ноль на выходе ⇔ измеренная скорость = заказанной),
  а не копировать из кода.
- **Не оставлять дефолты конфига расходящимися с тем, что летает.** У крена в конфиге
  годами стояли `kp=8, ki=2, imax=120, smooth=1`, а во всех прогонах через env жило
  `16 / 3 / 150 / 25`. Все замеры оси сделаны на лётных значениях; запуск без env давал
  НЕизмеренный контур. Победителя серии класть в конфиг сразу.

## Фаза 9 — ТРИ ОСИ ВМЕСТЕ (серия D1s: что нашлось и что делать дальше)

До этой фазы каждая ось меряется в одиночку, остальные держит оракул. Это НЕ произвол,
а условие измеримости (см. правило «одна ось за раз»), но у него есть цена: связка осей
— отдельный факт, и получить его можно только полётом всей связки.

```bash
BS_STAB=DpHold BS_MISSION=climb3,hover40,land   # все три по потоку, гейны-победители
```

### Что показала D1s (n=3, `roll_kp=48/ki=0`, `pitch_kd=1500`, `yaw_kp=20`)

Два прогона из трёх отвисели полные 40 с. Третий развалился через 10 с после набора:
**все три выхода одновременно упёрлись в предел** (крен +150, тангаж −150, рыскание +150)
и простояли там 3 с, после чего борт оказался на грунте — при том что барометр показывал
3.08 м, и остаток «висения» борт пролежал на земле. Одометрия покрывает весь прогон,
это не дыра в данных.

| | K1s (roll+pitch, без yaw) | D1s (все три) | Y5s (yaw + оракул) |
|---|---|---|---|
| СКО курса | 2.21 / 2.56 / 6.01° | 3.79 / 4.42 / **17.20°** | 2.8–3.6° |
| corr(PWM, ω_ист) | — | −0.81 / −0.77 / **−0.17** | −0.82…−0.83 |
| боковой путь за висение | — | **89–99 м** | 1.6–2.8 м |
| макс. удаление (забор 25) | ~12 м | 19.7 / 12.9 / **29.5 м** | — |

**Знак и гейн рыскания НИ ПРИ ЧЁМ.** Корреляция выхода с истинной угловой скоростью на
нормальных прогонах −0.81/−0.77, ровно как на эталонных Y5s. Контур гасит, а не разгоняет.
На сорвавшемся прогоне та же корреляция обвалилась до −0.17: датчик перестал видеть
истинное вращение, контур ушёл в упор за фантомом, борт открутило на +55°.

**Механизм связи осей — перенос против вращения.** На Y5s (оракул на крене и тангаже)
борт за висение наездил 1.6–2.8 м, на D1s — 89–99 м. Канал курса впервые работает, когда
борт много ЕЗДИТ, а в картинке перенос и вращение различимы плохо. Ни один свип этого
поймать не мог: там две другие оси держал оракул. **Обобщение: ось, измеренная с оракулом
на соседях, проверена только на неподвижном борте.**

**Опора тангажа НЕ пострадала** — `kf_n` 98–121, ни одного проседания ниже 30, скачков
скорости нет. Гипотеза «жёсткий крен трясёт `kf_logs`» не подтвердилась.

### Что делать со связкой (по убыванию отдачи на прогон)

1. **Снять рыскание со связки.** В `ALT_HOLD` центр стика по рысканию УЖЕ означает «держи
   курс» — это делает полётник по гироскопу, и делает лучше: K1s без визуального курс-холда
   держал ровнее, чем D1s с ним. Мы не добавляем удержание, а конкурируем с более хорошим
   контуром и доплачиваем за это отказом. Цена проверки — 3 прогона.
2. **Сторож насыщения.** Любая ось в упоре дольше ~1 с → авторитет ВСЕЙ связки в ноль на
   несколько секунд (плавный fade, механизм уже есть — используется для протухшего кадра).
   Демпфер, упёршийся во все стороны сразу, уже не демпфирует; центр стиков в `ALT_HOLD` —
   это дрейф, а дрейф строго лучше того, что случилось. Ловит ровно наблюдавшийся отказ.
3. **Понизить потолок выхода** 150 → 80–100: что не может насытиться, не может и разнести
   борт. Плата — тормозная способность, меряется готовыми `roll_brake_check`/`roll_osc_check`.
4. **Гейт рыскания по переносу** — если ось оставлять: морозить накопитель курса при большом
   боковом/продольном потоке, то есть ровно там, где перенос неотличим от вращения.
5. **Бесплатно, без полётов:** проверить derotation (`rotflow_sign` в оценщике, вычитание
   вращательного потока по гироскопу) офлайн-скриптом `flow_derotation_check.py` по уже
   записанным бэгам. Неверный знак поправки или её отсутствие УСИЛИВАЮТ смешение переноса
   с вращением — то есть прямо кормят наш отказ.

Порядок: **5 → 1 → 2 → 3**. Пятое бесплатно; первое снимает ось и заодно проверяет, не она
ли источник; второе закрывает класс отказа целиком; третье торгуется по метрикам.

### Мерить в единицах задачи, а не «красоты висения»

Демпферу не нужно держать пятачок — ему нужно **продержаться до инициализации VINS**.
Правильная метрика поэтому не разброс позиции, а **бюджет до забора**: сколько секунд до
25 м по фактической скорости ухода. У K1s это ~85 с (11.9 м за 40 с ≈ 0.3 м/с), у D1s уже
меньше — один прогон вышел на 29.5 м. Отсюда и длительность серий: гонять `hover40`, когда
инициализации нужны секунды, значит мерить режим, который не требуется. Серия D2s
(10 × `hover20`) снимает частоту отказа на дистанции, которая реально нужна.

---

## Фаза 5′ — ТАНГАЖ ПО СКОРОСТИ (замена фазе 5)

Продольная ось теперь `rate`, как крен: сигнал `ipm_vfwd` — метры в секунду из
выпрямленной полосы земли. Порядок настройки от этого меняется принципиально.

**Датчик проверяется ОФЛАЙН и ДО полётов.** Нужен бэг с кадрами (`STRIP=0` на первом
прогоне серии), дальше `ipm_flow_test.py` даёт крутизну «намерено/истина» и корреляцию —
по пути и по скорости, на нескольких окнах сглаживания сразу. Ворота: крутизна около 1.0
и ОДИНАКОВАЯ от прогона к прогону. Пока это не так, гейны трогать бессмысленно — мы уже
потратили на это пять серий.

**Гейн считается, а не угадывается.** При `a₁ = 0.0115 м/с²/PWM` демпфер `kp` даёт
постоянную торможения `1/(kp·a₁)`: 200 → 0.43 с, 100 → 0.87 с. Отсюда и стартовое
значение, а свип уточняет.

**Что мерить у rate-оси** (не то же, что у позиционной):
- **СКО скорости** — есть ли предельный цикл. У тангажа в `E8` было 0.54 при kp=200;
- **период** колебаний — определился, значит цикл; не определился, значит снос;
- **снос** за висение — плата за низкий гейн;
- контроль — **соседняя ось** (крен на своём канале): если сдвинулась и она, дело не в
  ручке, а в стенде.

**Гейном выбирается, какую болезнь терпеть.** Свип `F1` (по прогону на точку):

| kp | размах | v СКО | период | уход |
|---|---|---|---|---|
| 200 | 1.90 м | 0.54 | 4.5 с | 1.7 м |
| 100 | 9.20 м | 0.18 | нет | 9.5 м |
| 50 | 9.21 м | 0.31 | нет | 7.9 м |

Высокий гейн держит близко, но качается; низкий не качается, но уползает. Вылечить обе
разом может только позиционный член поверх скоростного — и это ОТДЕЛЬНЫЙ слой, который
добавляется последним, а не вперемешку с настройкой демпфера.

## Ещё четыре правила, которые кампания D2s–F1 добавила к семи

**8. Одиночный прогон на точку свипа — не замер, если разброс стенда сопоставим с
эффектом.** Одна и та же настройка (`pitch_rate_kp=200`) дала СКО скорости 0.66 в `E8` и
0.47 в `F1a1`; разница между 100 и 50 в свипе — 0.18 против 0.31, того же порядка. Правило:
на точку свипа либо n≥3, либо вывод формулируется как «направление», а не как «победитель».
Дёшево спасает: ставить ПОВТОР БАЗЫ первым прогоном свипа — тогда сравнение идёт с
сегодняшним стендом, а не с позавчерашним.

**9. Ручка, которая меняет две вещи сразу, не свипится.** `kf_alt_max` служил и порогом
затвора («кадру не верим»), и поводом пересева («забыли точку»). Свип по нему двигал оба
разом, и результат был неинтерпретируем. Сначала развести механизмы в коде, потом свипить.

**10. Геометрию проверять численно ДО полёта.** Проекция земли была написана с
перевёрнутым знаком поворота: земля в 3 м падала в строку 1098 при кадре высотой 540.
Десять строк проверки («куда проецируются 3, 5, 12 м») поймали это до первого прогона.
То же и с полярностью сигналов: прежде чем менять знак в контуре, свериться с
конвенцией соседней оси — `flow_lateral` и `ipm_vlat` оба ЛЕВОположительны, и «очевидная»
инверсия оказалась ложной тревогой.

**11. В офлайн-разборе не смешивать WALL и SIM часы.** `rel_alt` в бэге штампован временем
записи (wall), кадры — sim-временем. При RTF≈0.07 шкалы расходятся в 14 раз, и
`np.interp` молча упирается в край массива. Это дало занижение метрики в 3–5 раз и два
ложных диагноза подряд («сцена не плоская», «знак тангажа»), прежде чем нашлась причина.
Признак: величина систематически занижена, а сужение/расширение окон ничего не меняет.
В ПОЛЁТЕ такого нет — нода читает живой топик; это дефект именно разбора.
