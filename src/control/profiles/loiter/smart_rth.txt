# КАНДИДАТ — ВОЗВРАТ ПО СЛЕДУ (cmd/smart_rth, 2026-09-07). Всё остальное =
# loiter/guard.txt (гейты яруса 2) → loiter/baseline.txt.
# Отличие от loiter/rth.txt (прямая на home) — SMART_RTL разматывает ПРОЙДЕННЫЙ путь
# по хлебным крошкам, которые FCU копит с арма:
#   SRTL_ACCURACY  2 → 1     прореживание крошек, м: путь не упрощается мельче этого.
#                            Дефолт 2 м на нашей сортии 20–30 м = грубый след (срезает
#                            всё, что уложилось в 2 м); 1 м — вдвое честнее и всё ещё
#                            далеко от исчерпания буфера
#   SRTL_POINTS    300       размер буфера крошек (стоковый; при 1 м хватает с запасом —
#                            кончится → FCU просто не даст режим, шаг вернёт RTH_REFUSED)
# Хвост возврата (высота, пауза, мягкие последние метры) — как в rth.txt: домой борт
# приходит по следу, а садится теми же RTL_*/LAND_* ручками.
include guard.txt
BS_FCU_PARAMS=RTL_ALT_M=7 RTL_LOIT_TIME=1000 LAND_ALT_LOW_M=3 SRTL_ACCURACY=1 SRTL_POINTS=300
