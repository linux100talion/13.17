# КАНДИДАТ — ВОЗВРАТ ДОМОЙ НИЗКО И БЫСТРО (cmd/rth, 2026-09-07). Всё остальное =
# loiter/guard.txt (гейты яруса 2) → loiter/baseline.txt.
# Зачем: стоковый RTL под наши задачи слишком «дальнобойный» — набор до 15 м ради
# сортии в 20–30 м, 5 с висения над точкой и последние 10 м на LAND_SPD_MS 0.15 м/с
# (≈70 с одной посадки, весь возврат 100–120 с). Ужимаем до масштаба полётов у земли:
#   RTL_ALT_M      7      — высота возврата, м (было 15): выше крон сцены, но борт
#                           не уходит из поля зрения камеры и не тратит время на набор
#   RTL_LOIT_TIME  1000   — пауза над домом, мс (было 5000)
#   LAND_ALT_LOW_M 3      — с этой высоты садимся на LAND_SPD_MS 0.15 м/с (было 10):
#                           мягкие последние 3 м ≈ 20 с, выше — штатный WP_SPD_DN
# LAND_SPD_MS не трогаем (0.15 — доказанная мягкость касания, sa-soft-land).
# ⚠️ LAND_ALT_LOW_M правит и ветку pos мягкой посадки SoftLand (SA) — в ЭТОМ прогоне
# SA выше 3 м снижается быстрее обычного. Соседние прогоны восстанавливает
# loiter/baseline.txt (стоковые значения там стоят явно).
include guard.txt
BS_FCU_PARAMS=RTL_ALT_M=7 RTL_LOIT_TIME=1000 LAND_ALT_LOW_M=3
