# КАНДИДАТ — ВОЗВРАТ ПО СВОЕМУ ТРЕКУ (cmd/rth_track, 2026-09-09). Всё остальное =
# loiter/guard.txt (гейты яруса 2) → loiter/baseline.txt.
# Возврат ведём САМИ (BS_RTH_MODE=guided, шаг RthTrack): GUIDED + поток уставок по
# треку, записанному от латча. Ручки FCU здесь нужны для двух вещей:
#   WP_SPD 3         — скорость, с которой полётник идёт к нашей уставке, М/С:
#                      стоковые 10 м/с на сортии 20–30 м = борт всё время в разгоне/
#                      торможении, а нам нужен ровный проход по точкам трека
#   WP_ACC 1         — ускорение, М/С² (мягче стоковых 2.5: меньше клевков на точках)
# ⚠️ ИМЕНА 4.8: группа WPNAV_* ПЕРЕИМЕНОВАНА в WP_* и переведена в СИ (ReleaseNotes:
# «Waypoint navigation params converted to deg and meters and renamed from WPNAV to
# WP»). Старые WPNAV_SPEED/WPNAV_ACCEL прошивка не знает — сеттер eeprom честно ругался
# «нет эха PARAM_VALUE» (прогон 2026-09-09). Та же ловушка, что LOIT_SPEED→LOIT_SPEED_MS.
#   RTL_ALT_M 7 / RTL_LOIT_TIME 1000 / LAND_ALT_LOW_M 3 — как в loiter/rth: они нужны
#                      ФОЛБЭКУ (BS_RTH_MODE=fcu / кнопка при отказе GUIDED)
# ⚠️ WP_* правят и штатный LOITER (ярус 2) этого прогона — соседние прогоны
# восстанавливает loiter/baseline.txt.
include guard.txt
BS_FCU_PARAMS=WP_SPD=3 WP_ACC=1 RTL_ALT_M=7 RTL_LOIT_TIME=1000 LAND_ALT_LOW_M=3
