# КАНДИДАТ — КУРС EKF ОТ VINS (cmd/yaw_vins, 2026-09-09). Всё остальное = mission/baseline.
# После латча возврата нода ставит EK3_SRC1_YAW=6 (ExternalNav): курс полётнику ведёт наша
# поза, компас из контура уходит. Приращения у обоих от ОДНОГО гироскопа — меняется
# источник КОРРЕКЦИИ дрейфа: компас (абсолютен, но шумит и врёт над железом) →
# визуальные фичи (гладко и без помех, но без абсолюта). Откат на компас — по пропаже
# опоры (перерождение / не sane / закрытый мост / протухший поток), BS_EKF_YAW_FALLBACK=1
# из baseline. Механика, риски и порядок проверки — src/nav/frames.md.
include baseline.txt
BS_EKF_YAW_SRC=vins
