# КАНДИДАТ A/B — чек ЗАНИЖЕНИЯ VINS против IPM до 25 м (cmd/10, 2026-09-06):
#   BS_VINS_SCALE_ALT_MAX 4 → 25 (потолок высоты перцепции для третьего канала vins_sane);
#   BS_VINS_SCALE_IPM_MIN 2 → 1.2 (порог |ipm_v|, м/с — 3σ шума канала на 23 м).
# Всё остальное = vins/twist.txt (twist, гейт здоровья, зрелость, мост).
# Цепочка: чек занижения (114248: VINS «0.4–0.9» при истине 3–5.5, IPM видел 5.0) стоял с
# потолком 4 м — «выше IPM не опорник» было допущением. Замер bag 171454 (висение DpHold 10/14/
# 17/23 м, src/lab/ipm_alt_quality.py): годных кадров 100 % до 23 м, гейн канала к истине
# боковой 0.49–0.77 / продольный 0.53–1.14, шум (СКО остатка) 0.16 (10 м) → 0.19 (14) → 0.22
# (17) → 0.41 м/с (23 м), VINS/истина 1.00. Реплей настоящего гейта (src/lab/vins_sane_replay.py
# --scale-alt-max 25 --scale-ipm-min 1.2): 0 ложных срабатываний на 6 здоровых bag 2026-09-06
# (включая 23 м под порывами: условие набиралось 0.4 с из 3), эмуляция коллапса --vins-scale 0.2
# ловится на плечах (150448, 160730) при истине 1.3–2.0 м/с и palt 4.3–6.5; ×0.35 — тоже.
# ОЖИДАНИЕ: висение и плечи на 10–20 м на обоих ярусах — без единого scl= в статусе (регрессия);
# при коллапсе масштаба на высоте (реборн VINS ×0.14–0.3 на ходу) — scl растёт, ярус 1 падает
# на демпфер на 30 с, LOITER с loiter_guard — тоже. Ограничения по построению: чек молчит на
# живом стике и на чистом висении без хода (нужно ≥ ~2 м/с истины — реальный коллапс даёт унос);
# мягкую лотерею масштаба 0.5–0.85 не ловит (это детектор зрелости, ratio по высоте).
# Реальная камера 2.3 MP global shutter ≈ ×1.6 px/м против сима → шум на 20 м как в симе на 12 м.
# ЧЕМ ДОКАЗЫВАТЬ: (1) регрессия — полёт как 171454 (висение 10/15/20 м на DpHold и DpVins, плечи)
# → в статусе scl=0 весь полёт, brake_phase/gust_hold_compare как раньше; (2) реплей на новых
# bag: vins_sane_replay.py с ручками профиля — 0 ложных, с --vins-scale 0.2 — срабатывания на
# плечах; (3) ipm_alt_quality.py на боевых bag Orin — гейн/шум по высотам перед включением там.
BS_HANDOVER_VINS=1
BS_VINS_MIN=300
BS_VINS_RESTART_ARM=1
BS_RIPE_SEC=30
BS_RIPE_DET=1
BS_VINS_SANE_N=10
BS_VINS_VEL_SRC=twist
BS_VINS_RESTART_DIVERGE=0
BS_VINS_SCALE_RATIO=0.5
BS_VINS_SCALE_ALT_MAX=25
BS_VINS_V_MAX=12
BS_VINS_HOVER_V=3
BS_VINS_HOVER_SEC=2
BS_VINS_IPM_TOL=0
BS_VINS_RESTART_CD=6
BS_VINS_SCALE_IPM_MIN=1.2
BS_VINS_SCALE_SEC=3
BS_VINS_SCALE_HOLD=30
BS_VISION_POSE_SRC=extern
BS_VISION_VEL=1
