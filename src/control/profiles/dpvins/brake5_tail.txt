# КАНДИДАТ A/B — DpVins BRAKE 5 (кап 2.0) с ХВОСТОМ БРЕЙКА КАК У ДЕМПФЕРА: трим в брейке
# НЕ заморожен (BS_DPVINS_POS_BRAKE_T=-1, только анти-виндап в упоре), ki 15 → 8. Всё
# остальное = dpvins/brake5.txt. Дата: 2026-09-06, эталон коммит adfaf59 (cmd/3 + повтор).
# Цепочка: cmd/3 (brake 5, заморозка первые 8 с брейка) — DpHold всё равно лучше в 3 из 4
# (пик 2.0–2.7 против 4.6–5.5). Разбор cmd/3/README: с заморозкой порыв 11 с держит один
# П-канал 192 PWM/(м/с) — равновесный снос 0.4 м/с; после 8 с трим учится на хвосте
# порыва и на возврате → уползание против ветра ~3 м в затишье. Демпфер трим на
# возврате и в брейке УЧИТ (flow_damper.py update): заморожено у него только стик→гвоздь
# и анти-виндап в упоре, таймера нет — его «заморозка» = насыщение при |v| > 0.42 м/с.
# Перенос правила как есть: трим берёт порыв за секунды и работает как feed-forward
# ветра. Цена — в брейке ошибка ×(1+brake) = 6·v, эффективный ki ×6: ki 15 раскачивает,
# нужен ki 8–10. Запирание BRAKE (cmd_3/right/1) при этом невозможно по построению: оно
# живёт в ненасыщенном упоре, где трим учится — таймер brake_t лишний.
#
# ОЖИДАНИЕ (стенд src/lab/dpvins_gust_stand.py, τ_meas 0.35, порыв 75 PWM-экв., kp 40/32,
# brake 5 кап 2.0; пик/остаток/vmax; калибровка: brake5+ki15+brake_t 8 → 4.34/0.55, полёт
# cmd/3 дал 4.6–5.5):
#   brake_t 8,  ki 15 (cmd/3)        : 4.34 / 0.55 / 0.95   боковая ось kp 32
#   brake_t −1, ki 15                : 5.57 / 1.90 / 2.48   РАСКАЧКА (ki×6 = 90)
#   brake_t −1, ki 12                : 3.83 / 0.54 / 1.90
#   brake_t −1, ki 10                : 2.28 / 0.03 / 1.06   (при τ 0.5: 3.77/0.59 — просел)
#   brake_t −1, ki 8                 : 2.44 / 0.04 / 1.29   ← ЭТОТ кандидат (τ 0.5: 2.40/0.09)
#   brake_t −1, ki 8, продольная kp 40: 2.05 / 0.04 / 1.19
#   демпфер как есть (kp 90 ki 30 brake 3): 2.24 / 0.06
#   ki 8: запирание (трим −56) 2.36/0.19; порыв 10 м/с 3.51/0.14 (brake_t 8 ki 15: 7.93/4.05);
#         порыв 5 м/с 1.50/0.19 (2.90/0.74)
# ki 8 против ki 10: −0.16 м пика, зато переживает лаг 0.5 с — запас по фазе важнее.
# РИСК: стенд не знает шума VINS и предельного цикла σθ с контуром ориентации FCU;
# трим теперь ходит на десятки PWM за порыв — смотреть wnr/трим в статусе на раскачку
# T 5–10 с в затишье. Twist VINS (лаг 0.35 → 0.11) расширил бы окно ki на ~20 %.
#
# ЧЕМ ДОКАЗЫВАТЬ: сценарий cmd/3 (висение 2 и 5 м, CH6 в центре, порывы 8/20 с, слева и
# в лоб), разбор gust_hold_compare.py, сравнение с cmd_3 повтором (DpHold 2.0–2.7,
# DpVins brake5 4.6–5.5 слева, 2.8 в лоб на 2 м):
#   успех — pk_mean DpVins ≤ 3 м при rs_mean ≤ 0.5, vmax ≤ 1.5, без flips/reb/scl, в
#           затишье нет уползания против ветра > 1 м и качания T 5–10 с;
#   провал — качание (размах в затишье > 2 м) → ki 6 (стенд 3.41/0.07); уползание
#           осталось → учить трим только у гвоздя на малой скорости (пункт б cmd/3).
BS_VINS_STAB=dpvins

# ГЕЙНЫ — как в brake5, кроме ki и brake_t
BS_DPVINS_KP_FWD=40
BS_DPVINS_KP_LAT=32
BS_DPVINS_KI=8
BS_DPVINS_KI_TRIM=60
BS_DPVINS_IMAX=120
BS_DPVINS_CMD_GAIN=4
BS_DPVINS_VSMOOTH=0.3
BS_DPVINS_POS_KP=0.3
BS_DPVINS_POS_VMAX=0.3
BS_DPVINS_POS_ACC=0.15
# ФАЗА BRAKE станции: −brake·v при уходе от гвоздя быстрее brake_v, кап brake_vmax;
# brake_t −1 = трим в брейке не морозить (правило демпфера: только анти-виндап в упоре)
BS_DPVINS_POS_BRAKE=5
BS_DPVINS_POS_BRAKE_V=0.25
BS_DPVINS_POS_BRAKE_VMAX=2.0
BS_DPVINS_POS_BRAKE_T=-1
BS_DPVINS_TRIM_KEEP=1
BS_DPVINS_TRIM_SEED=1
BS_VINS_I_LATCH=1
