# Alfa AWUS036ACH на борту (Realtek RTL8812AU)

Внешний Wi-Fi для ssh/телеметрии с дрона — дальше и мощнее штатного M.2-модуля Jetson
(`wlP1p1s0`, RTL8822CE, антенна на раме). Всё ниже — разбор 2026-09-21, ветка
`laptop_drone`. Правило то же, что для всего борта: конфиги и скрипты — в `distro/`,
на борт — через `deploy.sh`, руками на Jetson ничего не менять.

**Железо.** Чип RTL8812AU, USB ID `0bda:8812`, 2T2R, 2.4 + 5 ГГц. Разъём на адаптере —
**USB 3.0 micro-B** (широкий, двухсекционный). Ток: в USB 3.0-режиме запрашивает
**800 мА**, в USB 2.0 — 500 мА; на передаче тянет до ~0.9 А.

## 1. Подключение: что мы прошли

**Симптом.** Адаптер воткнут коротким угловым кабелем, в `dmesg` бесконечный цикл
(31 попытка с загрузки), на любом порту:
```
usb 1-2.1: new high-speed USB device number 4 using tegra-xusb
usb 1-2.1: Device not responding to setup address.
usb 1-2.1: device not accepting address 7, error -71
usb 1-2-port1: Cannot enable. Maybe the USB cable is bad?
usb 1-2-port1: attempt power cycle
```
`mavlink-router` при этом падает («Start request repeated too quickly»), MAVROS молчит —
если полётник тоже на USB (`/dev/ttyACM0`), симптомы легко перепутать.

**Что это НЕ было.** Не «зарядный кабель»: строка `new high-speed USB device` означает,
что хост договорился о 480 Мбит по D+/D− — линии данных есть. У чисто зарядного
кабеля в `dmesg` тишина. Не драйвер: устройство не доходит до стадии, где он нужен.
Не порт: два разных разъёма — та же ошибка.

**Что это было: micro-USB 2.0 кабель в USB 3.0 micro-B гнезде.** Узкий штекер
физически входит в левую половину широкого гнезда, щёлкает, держится — но
SuperSpeed-пар в нём нет, а тока он даёт 0.5 А. Ноутбук такое вытягивает (адаптер
«работает» на USB 2.0), порт Jetson — нет: `error -71`.

```
Гнездо адаптера (USB 3.0 micro-B):        ПРАВИЛЬНЫЙ штекер (~12 мм):   НЕПРАВИЛЬНЫЙ (~7 мм):
   ┌──────────┬────────────┐               ┌──────────┬────────────┐     ┌──────────┬────────────┐
   │ ▄▄▄▄▄▄▄▄ │ ▄▄▄▄▄▄▄▄▄▄ │               │██████████│████████████│     │██████████│            │
   └──────────┴────────────┘               └──────────┴────────────┘     └──────────┴────────────┘
     основная    SuperSpeed                  закрывает всё гнездо          только левая половина
```

**Как отличить кабель, не гадая:**
1. Штекер в адаптер закрывает **всё** гнездо (см. выше).
2. В Type-A конце **9 контактов** (4 на язычке + 5 глубже у задней стенки) — USB 3.0;
   4 контакта — USB 2.0. Синий язычок и «SS» на дешёвых кабелях рисуют не по делу.
3. Контрольный — воткнуть и посмотреть шину. Адаптер на **`Bus 2` / 5000 Мбит** =
   кабель USB 3.0; на `Bus 1` / 480 — USB 2.0. На ноуте:
   ```
   for d in /sys/bus/usb/devices/*/; do [ "$(cat $d/idProduct 2>/dev/null)" = 8812 ] && \
     echo "$d speed=$(cat $d/speed) bcdUSB=$(cat $d/version) power=$(cat $d/bMaxPower)"; done
   ```
   A/B на одном адаптере: плохой кабель → `speed=480 bcdUSB=2.10 500mA`, родной →
   `speed=5000 bcdUSB=3.00 800mA`. (USB 3.0 устройство на USB 2.0 кабеле честно
   рапортует `bcdUSB 2.10` — это не значит, что оно «само USB 2.0».)

**Про порты девкита.** Все четыре Type-A Orin Nano Developer Kit — USB 3.2, но каждый
разъём внутри раздвоен: контакты USB 2.0 идут на Realtek-хаб `1-2.x` (Bus 001, 480M),
SuperSpeed-пары — на Realtek-хаб `2-1.x` (Bus 002, 10000M). Это один комплект разъёмов,
«переткнуть в USB 3.0 порт» ничего не меняет.

**С родным кабелем на Jetson** энумерация проходит с первой попытки — но адаптер всё
равно садится на **USB 2.0** (`1-2.3`, 480 Мбит, `bcdUSB 2.00`, 500 мА). Это норма:
RTL8812AU стартует в USB 2.0-режиме, в USB 3.0 его переключает драйвер
(`rtw_switch_usb_mode=1`), по умолчанию — нет. 480 Мбит ≫ радиоканал, менять незачем.

**Кабель под габариты дрона.** Родной — длинный, штекер прямой. Нужен **короткий
угловой USB 3.0 micro-B** (такие делают для внешних HDD, 0.2–0.3 м, четыре варианта
загиба; искать «USB 3.0 Micro B 90 degree 0.3m») либо угловой переходник USB 3.0
micro-B + короткий USB 3.0 кабель. Компактный USB 2.0 кабель годится только с
**отдельной подачей 5 В** на адаптер (Y-кабель / BEC), по USB — только данные.
Активный хаб на дроне — лишний вес и точка отказа.

## 2. Драйвер

В ядре борта `5.15.185-tegra` драйвера нет: in-tree `rtw88_8812au` появился в 6.13
(на ноуте с 6.x он подхватывается сам), каталога `realtek/rtw88` в модулях Jetson нет
вовсе. Устройство энумерируется, интерфейс `1-2.3:1.0` без драйвера, `wlx…` не
появляется.

Ставится **`distro/usr/local/sbin/setup-rtl8812au.sh`** — DKMS-сборка
[morrownr/8812au-20210820](https://github.com/morrownr/8812au-20210820) @ `4722250`
(2026-08-20), пакет `rtl8812au/5.13.6-23`, модуль **`8812au`**, исходники на борту
`/opt/src/8812au-20210820`, опции — `/etc/modprobe.d/8812au.conf` (копия installer'а
лежит в `distro/etc/modprobe.d/`, источник правды — она).
```
cd distro && ./deploy.sh -X /usr/local/sbin/setup-rtl8812au.sh usr etc
```
Ставит `dkms build-essential bc git`, клонирует, `install-driver.sh NoPrompt`,
`modprobe 8812au`. Сборка на Orin Nano ~3 мин. Идемпотентен (installer снимает старую
dkms-версию). **После обновления ядра — запустить снова.** Заголовки ядра на борту
есть (`nvidia-l4t-kernel-headers`), интернет нужен (Wi-Fi/Ethernet).

Результат 2026-09-21: `rtl8812au/5.13.6-23, 5.15.185-tegra, aarch64: installed`,
интерфейс `wlx00c0cabacab9`, через NetworkManager видит сети в обоих диапазонах
(`TP-Link_6611` 100 % @2457 МГц, `Sheny1` @5180 МГц).

## 3. Причуды драйвера (не баги)

| Наблюдение | Что на самом деле |
|---|---|
| `iw dev wlx… scan` пуст | Out-of-tree драйвер сканирует только по триггеру. Смотреть `nmcli dev wifi rescan ifname wlx… && nmcli dev wifi list ifname wlx…` |
| `txpower -100.00 dBm` | Фикция, драйвер мощность не рапортует |
| **Диод не горит** на Jetson, на ноуте горит | `rtw_led_ctrl=1` («normal blink»): тёмный, пока адаптер **не ассоциирован**; мигает при скане и трафике. На ноуте `rtw88` зажигает диод сразу при включении радио. Хочется всегда — `rtw_led_ctrl=2` в `8812au.conf` (через distro). На дроне полезнее «по трафику» — индикатор живого линка |
| `rtw_power_mgnt=2` | Дефолт модуля = **maxPS**, агрессивное энергосбережение. Для линка с дроном — задержки и провалы на простое. С 2026-09-21 в `8812au.conf` **`rtw_power_mgnt=0`** (см. §4) |

Все параметры: `ls /sys/module/8812au/parameters/`, значения —
`grep . /sys/module/8812au/parameters/*`.

## 4. Профиль NM и опции модуля (сделано 2026-09-21)

Бортовой Alfa: MAC **`00:c0:ca:b9:55:0c`**, интерфейс **`wlx00c0cab9550c`** (имя от MAC,
стабильное). Наземный — `00:c0:ca:ba:ca:b9`; дублей нет.

- **`etc/NetworkManager/system-connections/TP-Link_6611_alfa.nmconnection`** —
  `interface-name=wlx00c0cab9550c`, `autoconnect-priority=200`, **`route-metric=700`**
  (ipv4/ipv6). Встроенный `wlP1p1s0` (`TP-Link_6611`, метрика 600) — **основной**,
  Alfa — резерв. Сначала было наоборот (Alfa 100), и это укусило в тот же час, см. ниже.
- **`etc/modprobe.d/8812au.conf`**: `options 8812au rtw_switch_usb_mode=0 rtw_led_ctrl=1
  rtw_power_mgnt=0` — энергосбережение выключено (было `2` = maxPS по дефолту модуля).
  Применение — перезагрузка модуля: `./deploy.sh -r -X 'modprobe -r 8812au && modprobe
  8812au && nmcli connection up TP-Link_6611_alfa' etc`.

Результат: Alfa `192.168.0.105`, встроенный `192.168.0.104`, ssh на `.105` работал,
ping роутера через Alfa 3.7 мс. Линк рядом с точкой: Alfa **−44 dBm / 300 Мбит**,
встроенный −70 dBm / 162 Мбит — 26 дБ разницы, ради этого всё и затевалось.

**Молчаливая смерть линка (18:15–18:20 того же дня).** Через несколько минут после
подключения Alfa перестал ходить: `iw dev wlx… link` → `Not connected`, station dump
пуст, за 8 минут 52 rx / 79 tx пакетов — при этом `wpa_supplicant` `COMPLETED`,
`carrier=1`, NM «connected», в журнале **ни deauth, ни disconnect**. Драйвер не поднял
событие; NM лишь по connectivity-check задрал default-метрику до 20100, а connected-
маршрут `192.168.0.0/24 … metric 100` остался → пакеты на `.104` приходили по
встроенному, **ответы уходили в мёртвый Alfa** — с ноута не отвечали оба адреса,
хотя борт сам через `wlP1p1s0` всё пинговал. Причина обрыва не установлена: кандидаты
— драйвер (известная болячка out-of-tree Realtek) и ток на TX-пике (0.9 А через
USB 2.0 порт даже с родным кабелем). `nmcli connection up TP-Link_6611_alfa` линк
поднимает. Вывод: **пока Alfa не доказал стабильность, он не должен быть основным
маршрутом**; два адаптера в одной подсети с «мёртвым, но up» — ловушка. TODO: сторож
(`iw link` пуст при `carrier=1` → `nmcli con up`) или policy-routing по source-IP.

**Тест простоя 25 мин (2026-09-21, 21:33–21:58, все «сны» выключены).** Гипотеза
(по опыту с TP-Link-свистком): линк умирает после нескольких минут без трафика. Проверено
и выключено: NM `wifi.powersave=2` в профиле (было `Power save: on`), драйвер
`rtw_ips_mode=0` (IPS — отдельно от `rtw_power_mgnt`), USB autosuspend не при чём
(`power/control=on`, `runtime_suspended_time=0`). Модуль перезагружен, Alfa
переподключён, трафика на нём нет (маршруты у встроенного), замер `iw link` раз в минуту.
Результат: **18 минут непрерывно UP** (−38…−43 dBm; раньше обрывы за 5–15), на 19-й
минуте (21:52:21) обрыв — но **впервые с событием**: `disconnected → scanning`, NM
**сам переподключился за 8 с** (раньше — «зомби» без события, до ручного `con up`).
Минуты 20–25 UP. Оговорка: за 2–3 мин до обрыва я опускал `txpower` 17 → 12 → 10 dBm
(бустер, §5) — совпадение по времени есть, но ping через Alfa при 10 dBm идёт без потерь.
Вывод: сны выключать **обязательно** (обрывы стали видимыми и самолечащимися), но
корень обрывов, скорее всего, **внеполосный бустер** (§5) — проверять антеннами напрямую.

Новая сеть для Alfa = второй файл по образцу `TP-Link_6611_alfa` (свой uuid,
тот же `interface-name`, `route-metric=700`, пока Alfa резерв).

## 5. РЧ-бустер EDUP EP-AB025 (на борту, антенны через него)

Спека с [bezpeka-veritas](https://bezpeka-veritas.com.ua/ru/pidsyliuvach-syhnalu-5.8-hhts-edup-ep-ab025-4w-dlia-fpv-droniv/)
(2026-09-21):

| | |
|---|---|
| **Диапазон** | **5725–5850 МГц (5.8 ГГц)** — только |
| Выход | 36 dBm (4 Вт) |
| Усиление TX / RX (LNA) | 18 dB / 16 dB, шум 3.0 dB |
| **Макс. входная мощность** | **20 dBm** |
| **Порог включения передачи** | **3 dBm** |
| Питание | 5–16 В (12 В), штекер 2.5×0.7 мм |
| Разъёмы | вход SMA-F (к Alfa), выход RP-SMA-F (антенны) |

**Мощность Alfa.** Полные 4 Вт бустер отдаёт при 36 − 18 = 18 dBm на входе, предел 20,
порог 3. Заводские 17 dBm драйвера были в норме (ничего не горело), уставка —
**15 dBm**: 5 dB запаса от предела, −3 dB от полного усиления, порог перекрыт. Драйвер
`iw dev … set txpower fixed <mBm>` **слушается** (17 → 12 → 10 → 15 проверено), но
значение **не переживает переассоциацию** — после каждого `con up` снова 17. Поэтому
**`etc/NetworkManager/dispatcher.d/50-alfa-txpower`** (root 755): на `up` для
`wlx00c0cab9550c` ставит `DBM=15`, пишет в журнал `journalctl -t alfa-txpower`.
Проверено: срабатывает на каждом переподключении. NM-свойство `wifi.tx-power`
драйверами не поддерживается, `rtw_target_tx_pwr_*` — калибровочная таблица, не трогать.

**Диапазон — главная засада.** Бустер только 5.8 ГГц, а `TP-Link_6611` — **2.4 ГГц**
(канал 10, 2457 МГц). Для 2457 МГц усилитель вне полосы: PA/фильтры не на ту частоту,
усиления нет, детектор TX/RX может дёргаться. Следствия: (1) −38…−44 dBm у точки —
близость, не заслуга бустера; (2) **обрывы линка каждые 10–20 мин (§4) — вероятно, от
него**: TX через внеполосный тракт теряет пакеты, точка снимает клиента; (3) домашняя
5 ГГц (`Sheny1` @5180, канал 36) тоже **не в полосе** (5.15–5.25). Рабочая конфигурация
в поле: **обе стороны на 5 ГГц, каналы 149–165 (5745–5825 МГц)** — наземный Alfa на
ноуте в режиме AP на ch 149+, бортовой Alfa клиентом. Дома с 2.4 бустер — просто
потери в тракте.

## 6. Что дальше (TODO)

1. Короткий угловой **USB 3.0** micro-B кабель под раму (п. 1).
2. **Стабильность линка** (§4): сны выключены, обрывы стали видимыми и самолечатся, но
   не исчезли. Следующий тест — **антенны напрямую на Alfa, мимо бустера**, на 2.4: если
   обрывы уйдут — виноват внеполосный бустер (§5). До доказательства Alfa = резерв (700).
3. **5.8 ГГц под бустер**: наземный AP на канале 149–165, профиль NM бортового Alfa под
   этот SSID; проверить, что драйвер берёт ch 149+ (regdomain `00` даёт 5 ГГц UNII-3?).
4. У Alfa бывают партии с одинаковыми MAC — если появится третий адаптер, сверить;
   лечится `wifi.cloned-mac-address` в профиле NM.
