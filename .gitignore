# Raw camera frames
*.raw

# Compiled binaries (placeholders with __bin suffix are tracked)
orin_shutdown
!*__bin
camera_node
tuner_cuda
tuner_cpu
*.so
*.o
*.a

# Editor backup files
*.save

# Python
__pycache__/
*.pyc

# Симуляция — временные выходные данные
docker/sim/output/
docker/sim/worlds/preview/*.bag
# Персистентные калибровочные бэги (крупные, вне git; см. HowToFlow_PID_synth.md)
docker/sim/bags/
# Машинно-зависимые пути compose (VINS_SRC и т.п.) — у каждого бокса свои
docker/sim/.env

# Секреты деплоя на Jetson — НЕ коммитить (репозиторий публичный)
distro/doc/ssh-keys/jetson
distro/doc/wifi.txt

# Настройки редактора — у каждого свои, в репу не едут
.vscode/

# Ключи радиолинка WFB-ng (пара drone.key/gs.key из wfb_keygen) — секрет, репо публичный
distro/etc/drone.key
tools/wfb/gs.key
