---
name: control-refactor-split-2026-09
description: Разбивка монолитов control_pkg (stabilization/flow_estimator) — доказана A/B бит-в-бит и полётом; техника A/B-доказательства рефакторингов
metadata: 
  node_type: memory
  type: project
  originSessionId: 8423e528-c203-4954-82d5-dd4d83930db7
  modified: 2026-09-01T09:59:01.498Z
---

Серия рефакторингов 2026-09-01 (ветка nn2_c2_laptop_eagle, cff07b1 → 1f03a64),
ПОДТВЕРЖДЕНА ПОЛЁТОМ 2026-09-01 («всё работает»):

- `stabilization.py` (1430) → 8 модулей + фасад-реэкспорт (все старые импорты живут);
- `StationKeeper` (station_keeper.py) — State-автомат станции (RELEASED/SETTLING/
  HOLD/BRAKE), демпфер держит `self.station`, исторические `_pos_*` — делегирующие
  свойства для стендов; `StationFrame.dbg()` — пулл вместо записи st_* в снапшот
  (bootstrap_node заполняет сам); порты ожили (test_ports.py — AST-сверка без rclpy);
- `flow_estimator.py` (1198) → миксины `ipm.py` + `keyframe.py`, класс один,
  состояние на self. ⚠️ cv2 для перехвата LK патчить в `perception/ipm.py`;
  standalone `import flow_estimator` мёртв — только пакетом.

**Техника A/B-доказательства рефакторинга (переиспользовать):** снапшот пакета в
scratchpad под другим именем (`cp -r control_pkg control_pkg_old`, снести
__pycache__; relative imports работают под любым именем пакета) → гнать старый и
новый код ОДНИМИ сценариями (случайные тики для стратегий / синтетические кадры
из шумовой текстуры для перцепции, с рампами высоты через все гейты, сбросами,
разворотами) → сравнивать на ТОЧНОЕ равенство выходы И внутренности каждый тик.
Плюс diff перенесённых блоков против `git show HEAD:файл | sed -n 'A,Bp'`.
Сами харнессы были в scratchpad сессии (ab_station.py, ab_flow.py) — одноразовые,
при повторе писать заново по этому рецепту.

**Why:** монолиты по 1200-1400 строк с лётными обоснованиями в комментариях нельзя
рефакторить «на глаз» — только с бит-эквивалентностью, комментарии переносить
байт-в-байт.

**How to apply:** при следующих разбивках (кандидат — ros_perception.py 400 строк
или process() дальше) — тот же рецепт: фасад/миксины ради стендов с приватным API,
A/B-харнесс, полный свип тестов (19 control + 7 mission), потом полёт.
Связано: [[lv2-gps-denied]], [[damper-low-alt]].
