---
name: cmd-scripts-convention
description: Договорённость 2026-09-05 — каждый скрипт запуска живёт в cmd/<имя>/<имя>.sh + README.txt; с 2026-09-06 cmd/bl/bl.sh = текущий baseline (бывш. cmd/11), отлетавшие — cmd/history/<кампания>/<n>/ (wind/1…11)
metadata:
  type: feedback
---

Скрипты запуска (профили → env → `src/lab/freefly_lv.sh`) кладутся в `cmd/<имя>/<имя>.sh`,
рядом обязательный `cmd/<имя>/README.txt`: зачем создан, что меняет против эталона и для
чего, как летать (сценарий пилота), чем судить (инструмент + критерии успеха/провала),
результат — дописывается после полёта. Скрипт не зависит от cwd (корень репы от своего
расположения, `/../..`), ручки берёт из `src/control/profiles/*/*.txt` (baseline + кандидат),
внешний env сильнее. Общая шпаргалка — `cmd/README.txt`. Первый: `cmd/1` — A/B DpVins ki 30
под порывами (кандидат `profiles/dpvins/ki30.txt`, стенд `src/lab/dpvins_gust_stand.py`).

**Раскладка с 2026-09-06 (перестроил пользователь):** `cmd/bl/bl.sh` — ТЕКУЩИЙ BASELINE, чем
летаем по умолчанию (сейчас бывший cmd/11: cmd/10 + WindTrim, плечо `WT=0|1`); отлетавшие
скрипты — архив `cmd/history/<кампания>/<n>/` как есть (кампания ветра/станции 2026-09-05…06 =
`cmd/history/wind/1…11`). Ссылки «cmd/<n>» в доках/памяти/профилях ведут в history. Новый
кандидат = копия bl → `cmd/<имя>/`; доказанный кандидат становится новым bl. Коммит 0399d4d.

**Why:** пользователь хочет, чтобы каждая лётная команда была самодокументированной
единицей: через месяц по README понятно, зачем летали и что проверяли, без раскопок чата.

**How to apply:** новый скрипт запуска = новая папка `cmd/<имя>/` + `<имя>.sh` + `README.txt`
по разделам выше, стартуя копией `cmd/bl/bl.sh`; не класть скрипты в корень `cmd/`; после полёта
дописать раздел «Результат»; когда кандидат становится дефолтом — он же становится `cmd/bl`, старый
уходит в `cmd/history/`. Связано: [[dphold-vs-dpvins-gusts]].
