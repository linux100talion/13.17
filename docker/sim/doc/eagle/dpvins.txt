Я сделал 4-ре вылета (см. ниже). Ветер 10 м.с.
В идея каждого вылета одна - летаю на dphold до уверенной
инициализации vins. Затем опускась на 3 метра в каждом из
4-х направлений по отношению к ветру:

/docker/sim/output/joystick/wind_right
/docker/sim/output/joystick/wind_left
/docker/sim/output/joystick/wind_front
/docker/sim/output/joystick/wind_back

И дрон некоторое время несет. Это основная проблема.

При этом, после того как дрон уже стабилизировался в dpvins
то он летает и тержится на ветру хорошо
/docker/sim/output/joystick/freeflight

Такое ощущение, что он медленно учится ветру.

=====================================================================
ОТВЕТ (Claude, 2026-09-03)

Ощущение точное — он БУКВАЛЬНО медленно учится ветру, и это не слабость
контура, а арифметика обучения трима. Разобрал все 5 bag'ов.

1. ЧТО ПРОИСХОДИТ. На входе в ярус 1 DpVins.enter() обнуляет ветровой
   трим (_itx/_ity = 0, лесенка зовёт enter на каждом переходе). Ветру
   10 м/с нужен трим ~100 PWM (замер lv2_joy_082437). Пока трима нет,
   держит один kp: равновесие kp·v = 100 PWM при kp 40/32 — это дрейф
   2.5–3 м/с. Трим наматывается как ki·∫v dt, а ∫v dt — это буквально
   ПУТЬ, пройденный по ветру. Обучение закончено, когда
   ki·путь = 100 PWM → путь = 100/6 ≈ 17 м. Это СТРУКТУРНАЯ константа:
   унос не зависит от kp (kp меняет лишь пиковую скорость дрейфа),
   только от ki и силы ветра.

2. ЗАМЕР (истина Gazebo, от фронта ярус 0→1, стики центр):
   wind_right: пик 2.0 м/с, унос 16.8 м, ~15 с до |v|<0.3
   wind_left:  пик 2.1 м/с, унос 17.0 м
   wind_front: пик 2.0 м/с, унос 16.0 м
   wind_back:  унос 23.7 м — см. п.3
   freeflight: пик 2.1 м/с, унос 17.5 м
   Предсказание 100/6 = 16.7 м — совпадение в пределах полуметра на
   четырёх прогонах. После обучения трим мировой, следует за курсом —
   поэтому дальше freeflight держит и летает хорошо.

3. WIND_BACK: двойное обучение. На t=85.66 один тик vins_sane=false
   (глитч VINS: res 0.02→0.99, дыра одометрии 1.3 с сразу после) →
   ярус 1→0→1 за 60 мс → enter() обнулил трим ПОВТОРНО на скорости
   2.1 м/с → второй цикл обучения → 24 м вместо 17. Бонус-находка: во
   время дыры гвоздь завязался по ПРОТУХШЕЙ VINS-скорости (spx/spy
   латч при истинной скорости ~2 м/с).

4. ПОЧЕМУ У ДЕМПФЕРА ЭТОГО НЕТ. У станции демпфера есть механизм
   БЫСТРОГО захвата трима: _POS_BRAKE_TRIM (трим ветра в упоре первого
   брейка + перевзвод ×2) и ki_trim=60 против рабочего ki=30. DpVins
   сознательно строился без автомата станции («у VINS позиция чистая»)
   — но вместе с автоматом выпал и быстрый захват ветра: остался
   только медленный интегратор ki=6.

5. ЧТО ДЕЛАТЬ (по цене, рекомендую 1+2):
   1) ki_trim-фаза обучения (зеркало демпфера): до первого гвоздя
      (_trim_armed) интегрировать с ki_trim 30–60, после — рабочий
      ki=6. Унос ∝ 1/ki_обучения: 17 м → 2–3 м. Анти-виндап уже
      стоит, перелёт первого стопа им прикрыт.
   2) Не обнулять трим на ПОВТОРНЫХ входах в ярус 1: _itx/_ity — в
      мировой раме VINS, ветер на переключении яруса не исчезает.
      Сбрасывать только на рестарте VINS (мир перерождается — вектор
      в старой раме недействителен). Лечит wind_back-дребезг.
   3) (полный фикс свапа) Посев трима от демпфера при передаче: его
      установившийся выход на висении ≈ тот же ветровой трим в осях
      тела → повернуть в мир по vins_yaw. Унос ≈ 0, но нужна
      проводка между стабилизаторами — можно позже.

6. ПРЕДУПРЕЖДЕНИЕ. Дрейф обучения (пик 2.0–2.1 м/с) подходит к гейту
   «физики висения» vins_hover_v=3.0: при более сильном ветре или
   ниже kp честный дрейф обучения превысит порог — гейт объявит
   здоровый VINS разносом и уронит ярус (+/restart). Быстрое обучение
   из п.5.1 сужает и это окно.
=====================================================================
РЕАЛИЗОВАНО (2026-09-03): пункты 5.1 + 5.2, дефолты ВКЛ.

- ki_trim (BS_DPVINS_KI_TRIM, дефолт 60; 0 = выкл): до первого гвоздя
  трим учится с ki_trim, после — рабочий ki. Трим теперь хранится в
  PWM (смена скорости не дёргает выход).
- trim_keep (BS_DPVINS_TRIM_KEEP, дефолт 1; 0 = старое поведение):
  enter() трим не сбрасывает; сброс только на ФАКТИЧЕСКОМ /restart
  VINS (нода зовёт handover.note_vins_restart на обеих точках отправки
  — арм-фронт и демоут-по-разносу; сам фронт гейта трим не трогает —
  рестарт может не состояться из-за кулдауна).
- Стенд test_dpvins.py: +5 тестов, в т.ч. замкнутый контур «ветер
  100 PWM»: унос при ki=6 → 16.7 м (в точности полётные 16-17.5),
  с ki_trim=60 → 2.2 м. Вся регрессия (34 стенда) зелёная.
- Полётом НЕ доказано: повторить wind_right/left/front/back тем же
  сценарием, ждать унос ≤3 м на фронте яруса 1.
=====================================================================
РАСКАЧКА (2026-09-03, полёт lv2_joy_20260903_220204, ветер 5)

Пилот: «на 00:49 dpvins подхватил хорошо, но начал раскачивать; на
01:28 переключился на DpHold — встал как вкопанный». Bag подтвердил
и уточнил: это не kp, это ФАЗА ki_trim, которая не закончилась.

1. ЗАМЕР. Вход в ярус 1 на 50.3 с, унос всего ~1.4 м (ki_trim
   отработал). Дальше колебания вдоль оси ветра, период 7.1 с,
   НАРАСТАЮЩИЕ: ±0.8 м → ±2 м, пик |v| 2.2 м/с к 88-й секунде.
   Все 41 с яруса стики не тронуты НИ РАЗУ (815 статусов rcr=rcp=0).

2. МЕХАНИЗМ. Выход из фазы быстрого обучения (_trim_armed) был
   привязан к гвоздю, а гвоздь — к стику (_pin_pending). Стика нет →
   гвоздя нет → ki_trim=60 молотил весь ярус. Контур трима = скрытый
   позиционный член (∫v = путь): ωn=√(ki/100), ζ=(kp/100)/(2·ωn).
   При ki 6 → ζ≈0.8 (спокойно), при ki 60 → ζ≈0.26, T≈8.1 с (факт
   7.1). Остаток запаса съели лаги канала (vsmooth 0.3 + наклон FCU +
   латентность VINS) → рост. Стенд не ловил: в модели не было лага.

3. ПОЧЕМУ ДЕМПФЕР НЕ КАЧАЕТ (замер того же полёта: подхват на
   2.14 м/с, стоп за 2.5 с, дальше |v|ср 0.16, мод 7-10 с нет):
   а) той же болезнью болел — «звон 10.7 с (ki по скорости = скрытый
   позиционный контур)» из damper-low-alt — и вылечен кампанией
   (kp 30→90 + станция); б) его фаза быстрого трима КОНЧАЕТСЯ
   автоматом: гвоздь на каждом стопе + захват трима одним махом в
   упоре первого брейка (_POS_BRAKE_TRIM); в) гейны тюнились против
   реальных лагов канала (τ_s 0.4-1.05 с).

4. ФИКС (та же дорога, что у демпфера): гвоздь вяжется и БЕЗ стика —
   первым стопом после движения (_moved: |v| побывала > _PIN_V, потом
   < _PIN_V → гвоздь в точке стопа + _trim_armed). _moved отсекает
   ложный стоп на самом входе (борт ещё не понесло — гвоздь на входе
   съел бы фазу обучения). Заморозка трима — только на живом стике и
   торможении после стика (_pin_pending), не «нет гвоздя»: иначе
   после enter() трим замерзал бы навечно и смена ветра между
   входами в ярус не доучивалась.

5. СТЕНД: test_dpvins.py +2 теста. Гвоздь по первому стопу без
   стика; замкнутый контур С ЛАГОМ наклона (τ=0.5 с, ветер 0.5 м/с²):
   до фикса хвост висения |v| до 1.21 м/с (раскачка — репро полёта),
   после — 0.05. Вся регрессия 35/35 зелёная.

   В этом полёте фикс закончил бы фазу на +3.3 с (первый |v|<0.3).
   ДОКАЗАНО ПОЛЁТОМ (2026-09-03, после abfa324): пилот — «всё отлично,
   на разных высотах и скоростях, на разных стабилизаторах;
   стабилизаторы переключаются, ветер держит».
=====================================================================
РЕАЛИЗОВАН п.5.3 (2026-09-03): посев трима от демпфера при передаче.

Демпфер секунду назад держал этот ветер — его установившийся И-член и
есть ветровой трим. На входе в ярус 1 (handover.vins_stabs, лесенка
всегда передаёт демпферную базу) он сеется в DpVins: унос ~2 м
(обучение ki_trim) → ~0.

- Валюта — PWM КАНАЛОВ (после osign демпфера, до psign DpVins):
  DpHold.trim_pwm() отдаёт (pitch_off, roll_off) = osign·И-член осей;
  DpVins.seed_trim() обращает СОБСТВЕННОЕ уравнение выхода
  (po = psign·(kp·err + i_fwd) → i_fwd = psign·pitch_off) и кладёт
  вектор в мир по vins_yaw. Никаких рассуждений о внутренних знаковых
  конвенциях — у проекта три зеркальных знака в истории.
- Сеется только ДЕВСТВЕННЫЙ трим (<1 PWM, не armed): начатое обучение
  (дребезг гейта, trim_keep) и выученный ветер не перетираются — своё
  свежее. armed НЕ ставится: посев — оценка (blend, протухание),
  ki_trim остаётся страховкой и быстро доучивает остаток.
- Сброс по /restart и посев дружат: в одном входе в ярус — сначала
  reset_trim (старая рама), затем посев в свежую.
- Ручка BS_DPVINS_TRIM_SEED (дефолт 1; 0 = выкл, учить с нуля).
- Стенды: test_dpvins.py +5 (круговой знак-тест: посев → выход = ровно
  каналы демпфера; мировой трим под курсом; замкнутый контур: точный
  посев → унос 0.00 м, посев 70 из 100 → 0.52 м — ki_trim доучил);
  test_handover.py +7 (trim_pwm, посев из vins_stabs, девственность,
  ручка, /restart+посев, VinsHold не падает). Регрессия 35/35.

ДОКАЗАНО ПОЛЁТОМ (2026-09-03): пилот — «унос на входе в ярус исчез».
=====================================================================
