
❯ Полетал. docker/sim/output/joystick/base/LV2/1/lv2_joy_20260827_034514 Улучшения заметны. Но при превоначальном подлете дрон все равно уносит сильно. Предлагаю - затянуть пиды (или может сделать пид адаптивным каким то образом). Какие сейчас значения пидов ?

  Searched for 6 patterns, ran 12 shell commands

Модель контура из самого полёта — чтобы предсказать свип, а не жечь прогоны:

  Searched for 1 pattern, ran 2 shell commands

Разобрал прогон. Сначала прямой ответ на вопрос.

Текущие ПИДы (профиль freefly_lv.sh, LV=2)

┌────────────────────┬─────┬─────┬─────┬──────┬─────────┬──────────┬───────────┐
│                    │ kp  │ ki  │ kd  │ imax │ потолок │ cmd_gain │   slew    │
├────────────────────┼─────┼─────┼─────┼──────┼─────────┼──────────┼───────────┤
│ крен DpRollRate    │ 30  │ 30  │ 0   │ 150  │ 150 PWM │ 5 м/с    │ 300 PWM/с │
├────────────────────┼─────┼─────┼─────┼──────┼─────────┼──────────┼───────────┤
│ тангаж DpPitchRate │ 30  │ 30  │ 0   │ 120  │ 150 PWM │ 5        │ 300       │
└────────────────────┴─────┴─────┴─────┴──────┴─────────┴──────────┴───────────┘

Заданы жёстко в src/lab/freefly_lv.sh:193-200 (дефолты config.py другие: крен 30/0, тангаж 100/0 — ki=30 добавлен только в профиле). Станция-кипинг выключена: roll_pos_kp = pitch_pos_kp = 0.

Фикс сработал, и это меряется

┌─────────────────────┬─────────────────────────┬────────────────────┐
│                     │   194912 (acc_tau=0)    │ 034514 (acc_tau=5) │
├─────────────────────┼─────────────────────────┼────────────────────┤
│ затухание ζ         │ −0.07 (колебание РОСЛО) │ +0.14 (гаснет)     │
├─────────────────────┼─────────────────────────┼────────────────────┤
│ снос установившийся │ 9.5 ± 0.7 м             │ 2.9 ± 0.0 м        │
├─────────────────────┼─────────────────────────┼────────────────────┤
│ снос за 45 с        │ 10.2 м                  │ 3.0 м              │
├─────────────────────┼─────────────────────────┼────────────────────┤
│ |v| средняя         │ 0.35 м/с                │ 0.23 м/с           │
└─────────────────────┴─────────────────────────┴────────────────────┘

Но «уносит на подлёте» — это не слабость, это раскачка

Канал жив с первого же кадра после отрыва та в нуле, SF/CH6 на демпфере. При этом:

t+2.5с  v=1.06 м/с ←пик скорости      PWM
t+5.0с  v=0.16                        PWM крена +79 ←пик команды

Команда отстаёт от скорости на 2.2 с (corr +0.96) — это ровно четверть периода, подпись чистого интегратора. Пики
боковой скорости: +1.07 → −0.64 → +0.49 →  ζ ≈ 0.14 (комфорт 0.7). Борт не уносит —он звенит: 3.8 м → 1.8 → 3.5 → 2.6 → 3.1.

Причина архитектурная: ki по ошибке СКОРОСТИ — это скрытый позиционный контур (интеграл скорости = смещение). То есть контур уже второго порядка, а единствp. При kp=30/ki=30 демпфирования почти нет. «Затянуть ПИДы» в смысле «поднять оба» сделает хуже: ki задаёт частоту (ω_n = √(K·ki)), kp — затухание.

Модель контура, снятая с этого же полёта

датчик: τ_s ≈ 0.55 с          (канал против истины)
плант:  v̇ = −0.0141·PWM + 0.023·v + 0.675
        150 PWM = 2.11 м/с² (≈12° крена);  аэродемпфер ОТСУТСТВУЕТ (β>0)
        постоянная составляющая 0.675 м/с²M,
        которые демпфер держит в установившемся режиме — сходится с записью)

Контрфактный свип по этой модели (тем же порывом, с той же начальной скоростью). Модель занижает пик на ~30% против записи — сравнивайте строки между с

┌─────┬─────┬────────┬─────────┬──────────
│ kp  │ ki  │ pos_kp │ пик |v| │ путь макс │ на 45 с │
├─────┼─────┼────────┼─────────┼──────────
│ 30  │ 30  │ 0      │ 0.77    │ 2.3       │ 1.6     │
├─────┼─────┼────────┼─────────┼──────────
│ 60  │ 30  │ 0      │ 0.54    │ 1.6       │ 1.5     │
├─────┼─────┼────────┼─────────┼──────────
│ 90  │ 30  │ 0      │ 0.41    │ 1.6       │ 1.5     │
├─────┼─────┼────────┼─────────┼──────────
│ 120 │ 15  │ 0      │ 0.35    │ 3.2       │ 3.2     │
├─────┼─────┼────────┼─────────┼──────────
│ 90  │ 0   │ 0      │ 0.59    │ 23.0      │ 23.0    │
├─────┼─────┼────────┼─────────┼──────────
│ 90  │ 0   │ 0.5    │ 0.38    │ 1.0       │ 1.0     │
├─────┼─────┼────────┼─────────┼──────────
│ 120 │ 0   │ 0.5    │ 0.28    │ 0.7       │ 0.7     │
└─────┴─────┴────────┴─────────┴──────────

Два неочевидных вывода: ki убирать нельзя ратора борт уезжает на 23 м), и уменьшатьki тоже вредно (15 хуже 30: медленнее набирается те самые +48 PWM противоветра).

Что предлагаю

Шаг 1, один прогон: BS_ROLL_RATE_KP=90 BS_PITCH_RATE_KP=90 (ki оставить 30). Это «затянуть», но именно по P —
добавляем затухание, не частоту. Ожидание: звон гаснет за один период вместо четырёх.

⚠️ Честная оговорка: старый свип крена (в  хуже 60 хуже 30. Но он мерился с ki=0 и на канале до латча высоты и acc_tau, то есть на сигнале со смещением. Поэтому идти лесенкой 60 → 90 → 120, по прогону на ступень, и смотреть период: если PWM начинает лупить — упёрлись взапаздывание, ступень назад.

Шаг 2 (то, что вы назвали «адаптивным»): заменить скрытый позиционный контур явным — BS_ROLL_RATE_KI=0
BS_ROLL_POS_KP=0.5 BS_ROLL_POS_VMAX=1 (то скад: внешний P по пути даёт цель скоростис ограничением, внутренний демпфер её отрабатывает — нет накопления, нет windup, скорость возврата ограничена. По
модели это лучший вариант (1.0 м против 2.ть не нужно.

Шаг 3, если упрёмся в запаздывание: τ_s = ля kp. Уменьшать BS_IPM_WIN 0.5 → 0.3 (окно МНК) и/или ipm_vel_tau 0.4 → 0.25. Плата — шум скорости, поэтому только после шагов 1-2.



--------------------------------------------------



Поставил и запушил (230ca08) — чтобы мета следующего прогона записала правильный коммит, а не старый, как вышло с acc_tau.

Что изменилось

src/lab/freefly_lv.sh: BS_ROLL_RATE_KP и BS_PITCH_RATE_KP 30 → 90, ki=30 не тронут. Рядом — комментарий с разбором (почему только P, идентификация контура, почему ki убирать нельзя), чтобы через месяц не пришлось выводить заново.

Просто запускайте как обычно:

bash src/lab/freefly_lv.sh

Откатиться на любой прогон — снаружи: BS_ROLL_RATE_KP=30 BS_PITCH_RATE_KP=30 bash src/lab/freefly_lv.sh.

Что смотреть в полёте

Хорошо: подлёт тормозится заметно раньше, пик скорости вместо ~1.05 м/с около 0.6, звон гаснет за один-два размаха вместо четырёх.

Плохо (ступень назад на 60): период колебания падает с 10.7 с до ~5 с, борт начинает подрагивать на месте, PWM крена в HUD/flow_dbg мечется вместо плавного хода. Это значит, что упёрлись в запаздывание канала τ_s = 0.55 с, и дальше поднимать P бессмысленно — надо резать окно (BS_IPM_WIN).

В FPV самый быстрый индикатор — строка IPM OK NN%: она должна остаться зелёной 100%. Если при новом gain пойдут коды FEW LK, значит борт задёргался настолько, что канал теряет трекинг.

После прогона киньте каталог — прогоню тот же разбор (ζ, период, отставание команды) и сравню три полёта одной таблицей. Заодно скажите, оставить ли стенд идентификации контура (gain_sim.py, сейчас во временной папке) в src/lab/ — после каждого прогона лесенки он пересчитывает модель и предсказывает следующую ступень.


 recap: Мы гасим снос дрона у земли по каналу вида сверху; ipm_acc_tau уже доказан полётом, и я поставил в профиль kp 30 → 90 (шаг 1 лесенки), закоммитил и запушил. Дальше — ваш прогон freefly_lv.sh, потом киньте каталог на разбор. (disable recaps in /config)

------------------------------------------------------



Полетал. docker/sim/output/joystick/base/LV2/1/lv2_joy_20260827_061854 
Как по мне - это уже прямо сильно лучше. 
Однако первые 4-5 суекнд после отрыва от земли (20-25 секунды видео) 
    - дрон все равно уносит. Потом уже отлично.

  Ran 4 shell commands

Прогон разобран. Улучшение подтверждается, и остаток теперь имеет точное имя.

  Pushed to nn2_c3_laptop_vins, ran 3 shell commands

Шаг 1 зашёл — и остаток, который вы увидели, теперь имеет точное имя и цену в метрах.

Что дал kp=90 (окно взлёта, три прогона подряд)

┌────────────────────────┬─────────────────┬────────────────┬──────────────────────────┐
│                        │     194912      │ 034514 acc_tau │       061854 kp=90       │
├────────────────────────┼─────────────────┼────────────────┼──────────────────────────┤
│ пик боковой            │ +1.05 м/с @2.4с │ +1.07 @2.4с    │ +0.78 @1.8с              │
├────────────────────────┼─────────────────┼────────────────┼──────────────────────────┤
│ откат после торможения │ −0.91           │ −0.64          │ −0.22                    │
├────────────────────────┼─────────────────┼────────────────┼──────────────────────────┤
│ снос за 5 с            │ 4.12 м          │ 3.82 м         │ 2.83 м                   │
├────────────────────────┼─────────────────┼────────────────┼──────────────────────────┤
│ звон                   │ 1.7↔4.9↔7.2 м   │ 1.8↔3.5↔2.6 м  │ тормозится к t+5 и стоит │
└────────────────────────┴─────────────────┴────────────────┴──────────────────────────┘

Пик не только ниже — он приходит на 0.6 с раньше, и болтанки после него больше нет. Именно это вы и увидели как «потом уже отлично».

Почему первые 4-5 секунд не изменились: долг интегратора

Ветер в этом мире стоит 43-44 PWM — замер сошёлся в обоих прогонах независимо (γ/α = 0.647/0.0147 и 0.534/0.0124). Ключевое: выдать эти 44 PWM может только интегратор. Чтобы столько дал P, нужна постоянная ошибка 44/90 = 0.5 м/с — то есть борт обязан ехать, иначе P молчит. P — это тормоз, ветер он держать не умеет по устройству.


А интегратор набирает их за

I/ki = 44/30 = 1.45 м ИЗМЕРЕННОГО пути ≈ 2.6 м истинного
                                (канал на разгоне видит 0.55 истины)

— ровно те 2.8 м, что борт и проезжает. От kp это не зависит вообще, поэтому шаг 1 транзиент и не тронул: он срезал пик скорости и убрал звон, но платить долг всё равно пришлось. И платится он заново каждый взлёт — enter() обнуляет накопитель при входе в стратегию.

В записи это видно прямо: на t+4.0 PWM крена +79 (44 ветер + 35 торможение), к t+6 спадает к +48 — это и есть «выученный» ветер.

Поставил шаг 1b: ki 30 → 60 (запушено, aa1d7fc)

Долг вдвое дешевле — 0.73 м измеренного пути вместо 1.45. Затухание платит только √2 и остаётся с большим запасом:

┌──────────────────────┬─────────────────────────────┬──────────────────────┬─────────────┐
│                      │ kp=30 ki=30 (было до серии) │ kp=90 ki=30 (сейчас) │ kp=90 ki=60 │
├──────────────────────┼─────────────────────────────┼──────────────────────┼─────────────┤
│ ζ                    │ 0.33                        │ 1.20                 │ 0.70        │
├──────────────────────┼─────────────────────────────┼──────────────────────┼─────────────┤
│ ω_n                  │ 0.66 рад/с                  │ 0.66                 │ 0.94        │
├──────────────────────┼─────────────────────────────┼──────────────────────┼─────────────┤
│ путь за 5 с (модель) │ —                           │ 1.2–1.3 м            │ 0.2–0.6 м   │
└──────────────────────┴─────────────────────────────┴──────────────────────┴─────────────┘

То есть после удвоения ki запас устойчивости всё равно вдвое больше, чем был в начале серии — мы его просто тратим на скорость выучивания ветра.

Выше 60 не лезу, и вот почему. Запаздывание канала τ_s зависит от режима: на чистом висении у земли 0.60 с, а в этом прогоне (вы поднялись до 4.5 м и маневрировали) — 1.05 с. По пессимистичной идентификации ki=90 уже звенит (пик 1.09 м/с), ki=120 разносит. ki=60 устойчив по обеим.

Летите как обычно. Откат — BS_ROLL_RATE_KI=30 BS_PITCH_RATE_KI=30 bash src/lab/freefly_lv.sh.

Что ждать: пик подлёта примерно тот же (0.7-0.8 м/с — его держит kp), но борт остановится за ~2.5-3 с вместо 5 и уедет ~1.5 м вместо 2.8. Признак перебора: появится новый быстрый звон с периодом ~6 с — тогда шаг назад на 45.

Если и после этого первые секунды будут мешать — следующий рычаг не ki, а станция-кипинг (BS_ROLL_POS_KP=0.3-0.5 поверх интегратора: позиционный контур даёт власть сразу, без накопления) либо резать сам τ_s через BS_IPM_WIN 0.5 → 0.3.



Стенд идентификации (gain_sim.py) пока живёт во временной папке сессии 
— скажите, класть ли его в src/lab/, 
чтобы после каждого прогона лесенки пересчитывать модель штатно.



--------------------------------------------------



❯ Клади gain_sim.py в src/lab/

  Pushed to nn2_c3_laptop_vins, read 1 file, ran 2 shell commands

Положил, задокументировал, запушил (14a9a3c).

src/lab/gain_sim.py

Самодостаточный (зависимостей от временных скриптов нет — читает bag сам, cv2/control_pkg не нужны):

docker exec p1317_nav bash -lc 'source /opt/ros/humble/setup.bash;
  python3 /lab/gain_sim.py /root/sim_ws/output/joystick/<RUN>/bag'

Что печатает: по записи — пики, период, ζ, снос за 5 с; по идентификации — τ_s, коэффициенты планта, долг интегратора; дальше контрфактный свип, где строка с лётными гейнами помечена ← лётные.

Три вещи, которые в нём стоит знать:

- Лётные гейны берёт из меты прогона (<RUN>/<NAME>.env) — той же лесенкой, что ipm_video.py. Это лечит ошибку, на которую я сам налетел: сверять модель гейнами, которыми не летели.
- Ось выбирается (GS_AXIS=roll|pitch). Заодно вышла независимая проверка всей трактовки: на тангаже того же прогона ветер выходит 3 PWM против 43 на крене — то есть γ/α действительно читается как ветер, а не как артефакт подгонки. И это же объясняет, почему продольная ось всё это время вела себя прилично.
- Окно по умолчанию 12 с после отрыва. Не косметика: на длинном окне с набором до 4.5 м и манёврами τ_s подгоняется вдвое больше (1.05 против 0.60 с) — подгонка лага съедает ошибку масштаба канала, и свип начинает пугать раньше времени.

Ограничения выписаны и в докстринге, и в самом выводе: контура FCU по углу в модели нет (на высоких гейнах стенд оптимистичен), абсолютный пик занижается в 1.4-2× — сравнивать строки между собой. Стенд сам ругается, если пилот рулил этой осью в окне.

Следующий прогон уже полетит с ki=60 — киньте каталог, прогоню обе оси и сравню предсказание свипа с тем, что вышло на самом деле.



--------------------------------------------------



❯ Полетал. docker/sim/output/joystick/base/LV2/1/lv2_joy_20260827_064122/ Помоему стало хуже. А можешь подготовить две команды с явным заданием коэфициентов для этого и для предидущего полетов? Что бы я еще полетал и проробовал.

  Pushed to nn2_c3_laptop_vins, ran 3 shell commands

Ваше ощущение верное, и цифры показывают ровно размен, а не регресс.

Что дал ki=60 (прогон 064122)

┌──────────────────┬──────────────┬──────────────┬──────────────┐
│                  │ 034514 30/30 │ 061854 90/30 │ 064122 90/60 │
├──────────────────┼──────────────┼──────────────┼──────────────┤
│ пик подлёта      │ 1.06 м/с     │ 0.78         │ 0.68         │
├──────────────────┼──────────────┼──────────────┼──────────────┤
│ снос за 5 с      │ 3.82 м       │ 2.83         │ 1.60 ✅      │
├──────────────────┼──────────────┼──────────────┼──────────────┤
│ долг интегратора │ 2.7 м        │ 2.6          │ 1.4 м        │
├──────────────────┼──────────────┼──────────────┼──────────────┤
│ ζ (успокоение)   │ 0.14         │ 0.11         │ 0.02 ❌      │
├──────────────────┼──────────────┼──────────────┼──────────────┤
│ период           │ 10.7 с       │ 10.2         │ 7.8 с        │
└──────────────────┴──────────────┴──────────────┴──────────────┘

Подлёт починился ровно как предсказано — вдвое короче. Но затухание село в ноль: после любого толчка борт качается ±0.2-0.5 м/с и больше не гаснет. Пять секунд подлёта заметить трудно, а незатухающее покачивание весь полёт — легко. Отсюда «стало хуже».

Вернул дефолт на ki=30 (d514832), kp=90 оставил.

Заодно вскрылось, почему лесенка дальше не поедет

По трём прогонам измеренное ζ = 0.14 / 0.11 / 0.02 против «идеальных» по формуле 0.33 / 0.98 / 0.63 — формула завышает, и с ростом kp просто не сбывается. Зато ω_n = √(α·ki) сходится точно (0.59 / 0.62 / 0.80 против 0.65 / 0.65 / 0.84).

Читается однозначно: kp затухания почти не даёт — его съедает запаздывание канала (фаза ∝ ω·τ_s при τ_s = 0.6–1.05 с), а ki поднимает частоту и тем ζ ухудшает. То есть ki торгует подлёт против успокоения напрямую, и узкое место теперь одно — τ_s. Моя прошлая оценка «ζ заплатит только √2» была неверна: она опиралась на идеальную формулу, а не на замер.

Две команды

# A — успокаивается, но первые 5 с уносит ~2.8 м
BS_ROLL_RATE_KP=90 BS_ROLL_RATE_KI=30 BS_P_KI=30 \
  NAME=ab_kp90_ki30 bash src/lab/freefly_lv.sh

# B — подлёт вдвое короче (~1.6 м), но качается и не гаснет
BS_ROLL_RATE_KP=90 BS_ROLL_RATE_KI=60 BS_P_KI=60 \
  NAME=ab_kp90_ki60 bash src/lab/freefly_lv.sh

Обе лежат и в комментарии freefly_lv.sh — не искать в переписке. NAME делает архив самоназывающимся, гейны и так уезжают в мету прогона.

Чтобы сравнение было честным, летите оба оот крена → висеть ~30 с на одной высоте →посадка. Прошлые два прогона этим и различались (161 с с набором до 4.5 м против 99 с до 1.35 м, оба с манёврами)
— участок удержания в них несравним, сравн

Если захотите третью — вот та, что бьёт в

# C — ki=60 на укороченном запаздывании ка
BS_ROLL_RATE_KP=90 BS_ROLL_RATE_KI=60 BS_PITCH_RATE_KP=90 BS_PITCH_RATE_KI=60 \
  BS_IPM_WIN=0.3 NAME=ab_ki60_win03 bash s
                                                                                                              Замысел: вернуть затухание при коротком по станет шумнее (меньше кадров в МНК), этобудет видно по кодам FEW LK в строке IPM на HUD. Киньте каталоги — прогоню gain_sim.py по всем и сведу в одну таблицу.














































