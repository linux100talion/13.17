
Готово. По твоему вопросу: почти да — источников на самом деле три, но два независимых. alt= в статусе — это высота миссии rel_alt (при BS_ALT_SRC=baro в LV-профилях это сырой барометр через BaroAlt), а EKF3 отдаёт свою высоту в /mavros/local_position/pose.z — но нода до сих пор хранила от него только свежесть (пульс для баннера EKF READY), само значение выбрасывалось. Третья ручка — высота перцепции (BS_PERC_ALT_SRC), но в LV2 она указывает на тот же EKF z, отдельного датчика за ней нет.

Что добавлено:

- ros_telemetry.py + state.py — _on_lpos теперь сохраняет ekf_z (z из local_position) рядом с пульсом свежести.
- hud.py — в строку /mission/status добавлено поле zekf=. Если local_position протух (штатно после GPS-kill в LV1) — честное zekf=--, а не залипшее последнее значение; критерий свежести тот же EKF_FRESH_SEC=2.0, что у баннера и WaitEkfPos.
- hud_renderer.py — строка ALT baro X.Xm  ekf X.Xm под баннерами (и в живом FPV, и в scene_hud.mp4 — код общий). Расхождение двух высот больше 0.5 м подсвечивается жёлтым: это ровно тот механизм из вчерашнего разбора демпфера — в прогоне 174603 EKF занижал высоту на 0.27 м и гейт IPM alt<0.5 глушил канал на истинных 0.7 м. Теперь эта ошибка видна пилоту прямо в полёте.
- Совместимость честная: старый bag без zekf= → «ekf --», совсем старый без alt= → строки нет.

Проверено офлайн: test_hud_status.py — все 16 проверок зелёные (3 новые на zekf: округление, протухание → --, отсутствие z), плюс numpy-smoke рендера по всем веткам — на кадре строка легла под res/rat, жёлтая при расхождении. CLAUDE.md и память обновлены.

Не закоммичено — скажи, и закоммичу/запушу. Живым прогоном строка (как и EKF-баннер) пока не доказана — подтвердится первым же полётом LV=2 с воткнутым пультом.
 


-------------------------------------------------------------------


Включается — и без единой новой ручки. Все три фикса активны той же командой:

- Авторитет (п.1) — это не опция, а изменение поведения самих классов DpPitchRate/DpRollRate: их _authority теперь всегда возвращает 1.0. Никакого env/флага нет и не нужно — blend по flow_conf остался только у полнокадровых осей (DpRollHold, yaw), где он судит свой собственный сигнал.
- Гейт земли 0.08 (п.2) — константа класса, работает всегда при включённом поле (ipm_alt_floor, лётный дефолт 0.5 в config — уже стоял с прошлого прогона).
- LOITER-гейт (п.3) — дефолт cfg.loiter_alt = 0.5, так что и он активен сам. Ручка BS_LOITER_ALT нужна только если захочешь другой порог (например, вернуть легаси: BS_LOITER_ALT=1.5).

Единственное условие — обычный атомарный цикл прогона: freefly_lv.sh сам делает перезапуск стека, и nav_up.sh пересоберёт пакеты из смонтированных исходников, так что код подхватится штатно. Летишь ровно как раньше: LV=2 BS_SF_MASTER=1 bash src/lab/freefly_lv.sh.



-------------------------------------------------------------------










Что происходит на малых высотах (данные)

1. Ниже 0.5 м демпфер выключен по построению. В _ipm_update стоит гейт alt < 0.5 (flow_estimator.py:573) — канал вида сверху глушится, PWM-команды нулевые, а борт в этом бине дрейфует с RMS 1-2 м/с. Хуже того: гейт срабатывает по перцепционной высоте (EKF local z), а она врёт вниз — в прогоне 174603 смещение −0.27 м, и 57% провалов «выше гейта» — это на самом деле гейт, сработавший на истинных 0.6-0.8 м. Мёртвая зона по факту не 0.5 м, а ~0.8 м.

2. На ~1 м канал слепнет на треть времени. Прогон 173339 (почти весь на 1.1 м): ipm_ok=0 в 31% воздушного времени, провалы кучкуются ровно на висении (v≈0) при повышенном тангаже (медиана |pitch| 4.5° против 2.2° у хороших кадров). Геометрия: полоса IPM захардкожена 3-6 м впереди — на высоте 1 м она видна под скользящими углами 9-20°, варп растягивает несколько строк кадра в метрическую сетку, и LK не находит 20 фич / 15 выживших. Какой именно ранний return стреляет — с бэга не восстановить: в /flow_dbg8.z пишется только бинарный ipm_ok.

3. Там, где канал жив, он занижает скорость. Corr/gain «IPM против истины» по бинам: на 2-6 м — 0.93-0.99 / 0.8-1.0 (канал честный), на 0.5-1.2 м — 0.69-0.91 / 0.40-0.81. Демпфер видит 40-80% реальной скорости, то есть эффективное демпфирование падает в 1.5-2.5 раза ровно в его зоне ответственности. В 174603 занижение gain численно совпадает с относительной ошибкой высоты — масштаб IPM ∝ h, и −0.27 м на высоте 0.7 м это −40% масштаба. Плюс лаг сигнала 0.3-0.6 с (окно МНК 0.5 с).

Как улучшить (по приоритету)

1. Код причины вместо бинарного ipm_ok в /flow_dbg8.z (0=ok, 1=гейт высоты, 2=окно не видно, 3=варп за кадром, 4=мало фич, 5=мало выживших LK). Дёшево, один файл, обратная совместимость (z>0.5 остаётся «ок»), и следующий прогон точно скажет, какой return бьёт на 1 м.
2. Масштабно-инвариантная полоса — главный фикс. Сейчас x0..x1 = 3-6 м оптимальны только у проектной высоты 3 м. Сделать окно функцией высоты с постоянными углами визирования: x0 = h, x1 = 2·h (на 3 м это ровно нынешние 3-6 м — поведение отлётанных серий не меняется), и ipm_res ∝ h. Тогда полоса всегда лежит в одних и тех же строках кадра, число фич и пиксельный шум постоянны, а шум скорости в метрах падает ∝ h — у земли демпфер становится точнее, а не слепнет. Бонусом плечо деротации (сейчас 3-6 м на любой высоте) тоже ∝ h — вклад смещения ω_z у земли сжимается. Механика переноса окна между кадрами уже есть (ipm_adapt вычитает сдвиг x0 из накопителя) — добавится только обработка смены res (квантовать ступенями, кадр заморозки на переходе). После этого гейт 0.5 м опускается до ~0.2-0.3 м — геометрия на 0.3 м будет та же, что сейчас на 3 м.



-----------------------------------------------------------

3. Включить уже реализованный ipm_vel_tau 
    (комплементарный фильтр «наклон тяги + коррекция МНК» — 
    выключен лётным дефолтом до валидации знаков). 
    Он мостит провалы до 1 с прогнозом — ровно те 0.3-0.5-секундные дыры, 
    на которых демпфер сейчас глохнет. 
    Валидацию знаков наклона можно сделать оффлайн по этим же четырём бэгам, 
    без полётов.



- Фильтр давно лётный дефолт: config.py → ipm_vel_tau: float = 0.4, включён коммитом a1eae31 от 2026-08-18 («комплементарный фильтр скорости IPM + pitch kp=30 по свипу»). Выключен (0) только класс-дефолт FlowEstimator — это намеренно, чтобы офлайн-инструменты воспроизводили старые прогоны, к полёту отношения не имеет.
- Знаки наклона провалидированы полётом, не офлайн: прогон VELTAU_bag 2026-08-18 (миссия с mv по обеим осям) — corr с истинной body-скоростью +0.88/+0.88 против 0.76–0.84 у голого МНК, ipm_ok в воздухе 100% бинов. Это зафиксировано прямо в комменте config.py.
- Мостики работают и видны в данных: весь датасет LV2/1 и вчерашние прогоны летали с фильтром. В bag 210917 мы прямо видели коды z=1.4/1.5 — «фильтр мостит брак кадра» (одиночные провалы 0.3–0.5 с закрываются прогнозом, семантика ipm_ok = «скорости можно верить», свежесть до 1 с). Мой вчерашний реплей тоже шёл с ipm_vel_tau=0.4 — фиделити к записи полёта 0.92.

Почему при живом фильтре демпфер всё равно глох у земли: дыры там были не 0.3–0.5 с, а длиннее секунды (_VEL_HOLD) и подряд — канал голодал из-за бесфактурного мира, и фильтр честно признавался слепым, а не мостил вслепую (что правильно: прогноз наклоном тяги на манёврах у земли за секунды уехал бы далеко). Это мы и вылечили масштабной полосой + fine-миром, а не фильтром.

Единственный открытый хвостик у фильтра, задокументированный там же: на манёврах он наследует занижение МНК-измерения (~×0.6). Если после полёта на fine-мире gain у земли останется заниженным — вот тогда будет предмет, но это уже калибровка, не знаки.


------------------------------------------------------------








4. Высота перцепции у земли. 
    Относительная ошибка EKF-z ∝ 1/h — на 0.7 м это уже десятки процентов масштаба. 
    На боевом борту это классически закрывается дальномером вниз 
    (ToF 0-4 м, как в optical-flow-loiter у ArduPilot/PX4) — 
    как раз диапазон «зоны ответственности».
     Минимум без железа: 
        гистерезис на гейте высоты, 
        чтобы дрожь EKF около порога не хлопала каналом.


5. Опционально: ipm_win ∝ h — короче окно МНК у земли (шум в метрах после п.2 это позволит), меньше лаг там, где динамика относительно земли быстрее.



-------------------------------------------------------------------










❯ Давай начнем с п.1 - диагностика причин


$ claude

Resume this session with:
claude --resume 47154f72-2494-4536-9b19-2c3ac4908ee1
andriy@andriy:/usr/local/DATA/Calude/13.17






Коды работают и уже дали ответ. Разобрал bag lv2_joy_20260825_210917 (полёт на 0.8-1.6 м — ровно зона вопроса):

Что сказали коды. В bag ровно новая кодировка: {−7, −1, 1.0, 1.4, 1.5}. Лента полёта:

- 0–16.5 с — код 7: перцепция ещё не жила (до t=7.6 нет даже local_position, основной поток conf=0). Честный «канал не родился».
- 16.5–21.7 с — код 1 (гейт высоты), и это главная находка: на взлёте канал слеп до истинных 0.92 м, потому что EKF занижает высоту на 0.3–0.4 м (z_ekf 0.27 при истинных 0.58; открылся на z_ekf 0.54 = z_gt 0.95). Механизм «вранья EKF-z» подтверждён третьим прогоном подряд — мёртвая зона демпфера на взлёте почти вдвое шире номинальных 0.5 м.
- Дальше — канал жив 99% на 0.8–1.2 м: кадровые морги — единичные код 4 (мало фич, совпадают с провалами conf до 0.1–0.2 на манёврах) и пара код 5, и их мостит комплементарный фильтр (значения 1.4/1.5 = «скорость держится прогнозом»). Моя ставка «на 1 м бьёт код 5» не подтвердилась — слепота этого полёта была не в варпе, а в гейте.

Попутное открытие №1: ipm_vel_tau=0.4 — оказывается, уже лётный дефолт с 2026-08-18 (a1eae31); выключен только класс-дефолт FlowEstimator для офлайн-репро. Так что п.3 моего плана («включить фильтр») давно сделан — весь датасет LV2/1 летал с ним. Память исправил.

Попутное открытие №2 — дефект: после посадки (t=141.4) перцепция умирает, а publish_axes ещё 12 с публикует замороженный снапшот z=1.0 «годен» (все поля — константы). Staleness существовал всегда (RosPerception обновляет ipm-поля только при живом res), но раньше врал незаметно, а теперь врёт кодом. Дешёвая правка — честный код «перцепция молчит» по возрасту flow_seq.

Что это значит для плана. Приоритет сдвигается: главный убийца на малых высотах в этих полётах — не геометрия полосы, а высота у земли (п.4: гейт по заниженному EKF-z). Полоса ∝ h (п.2) всё равно нужна, чтобы гейт вообще можно было опустить, но без честной высоты у земли опускать его не к чему.

Напомню: изменения п.1 (ipm_fail) ещё не закоммичены — прогон летал на рабочем дереве. Скажи — закоммичу, заодно могу сразу поправить замороженный хвост паблишера.








❯ на взлёте канал слеп до истинных 0.92 м, потому что EKF занижает высоту на 0.3–0.4 м ... А если мы предположим, что любая высота меньше 1 м. - это 0.5 метра?

Идея рабочая — но в форме «пола», а не «ступеньки». Разложу по данным этого же прогона.

Что это чинит. Найденная слепота — это гейт alt < 0.5, съедаемый занижением EKF-z: канал молчал до истинных 0.92 м. Если высоту перцепции снизу подпереть константой, гейт перестаёт стрелять вовсе — те 5.2 с взлётной слепоты исчезают, демпфер работает с отрыва. Это главная ценность, и она реальна.

Что это НЕ чинит. Масштаб. Скорость IPM пропорциональна подставленной высоте: v_изм ≈ v_ист · (h_подставленная / h_истинная). EKF-z в этом прогоне занижал весь полёт (−0.25…−0.30 м даже на 1.5 м: z_ekf 1.30 при z_gt 1.52), поэтому канал и так видит 0.6-0.8 скорости. Подстановка 0.5 на истинных 0.92 даёт 0.54× — то же занижение, что и сырой EKF. То есть заплатка убирает слепоту, но не лечит недодемпфирование — его лечит только честная высота (п.4).

Почему не ступенька «<1 м → 0.5». У ступеньки разрыв ×2 на границе: EKF шумит около 1 м → высота прыгает 0.99→0.5→1.0, масштаб канала скачет вдвое кадр-к-кадру, а окно МНК 0.5 с смешивает кадры двух масштабов — на каждом пересечении границы родится фантомный рывок скорости, который демпфер отработает рулями. Непрерывный вариант той же идеи — пол: alt_eff = max(alt_ekf, 0.5). Выше 0.5 ничего не меняется, ниже — плавно упирается в 0.5, разрывов нет.

Нижний край. С полом на истинных 0.2 м канал будет завышать скорость ×2.5 (подставляем 0.5 вместо 0.2) — усиление контура ×2.5, возможен звон у самой земли. Но честно: сейчас там канал просто ВЫКЛЮЧЕН, дрейф 1-2 м/с — звон демпфера вряд ли хуже. Плюс на земле поток статичен (v≈0), команды и так близки к нулю. Для посадки/земли оставить крошечный гейт по сырому EKF (например, <0.10-0.15 — «точно на земле», это же место сброса фильтра скорости).

Итого моя рекомендация: alt_eff = max(z_ekf, 0.5) внутри _ipm_update + гейт вниз до ~0.15 — ручкой (BS_IPM_ALT_FLOOR), чтобы старые реплеи не менялись. Мёртвая зона сжимается с ~0.9 м до ~0.4 (порог 0.15 при смещении −0.3), дальше её добьёт только честная высота у земли.

Проверить можно без полёта: у нас есть стенд, который прогоняет FlowEstimator по кадрам из bag (att_extrap_test.py-подход) — прокрутить 210917 с полом и без, сравнить скорость канала с истиной Gazebo на взлётном участке 16.5–21.7 с, где сейчас дыра.

Делать? Могу: ручка + офлайн-прогон по этому бэгу, и отдельно — коммит уже готового п.1.


-------------------


Запушено, два коммита: 4be802d (п.1 — коды причин) и 7e73c73 (пол высоты). Что сделано во втором:

Реализация. Твоя идея в форме пола: высота для геометрии IPM = max(alt, 0.5), гейт «на земле» опущен 0.5 → 0.15 по сырой высоте (константа _ALT_GROUND, тут же сброс фильтра скорости). Ступеньку «<1 м → 0.5» отверг осознанно — разрыв масштаба ×2 на границе плюс дребезг EKF около 1 м дали бы фантомные рывки скорости. Ручка BS_IPM_ALT_FLOOR / --ipm-alt-floor: лётный дефолт 0.5 (в config), класс-дефолт 0 — старые прогоны офлайн-инструментами воспроизводятся как были.

Валидация без полёта — реплей _ipm_update по кадрам /image_color твоего же бэга 210917 (высота = EKF z как в полёте, углы/ω — истина Gazebo), пол 0 против 0.5:

- Крейсер 26–140 с: точный no-op — corr/gain совпали до последнего знака (пол выше 0.5 не делает ничего, как задумано).
- Взлётное окно 14–26 с: живость канала 36% → 83% (гейт остался только на земле до отрыва). Боковая ось — главный выигрыш: corr 0.64→0.96, gain 0.45→0.79 — а именно она демпфирует на взлёте.
- Продольная на кадрах набора ловит известный фантом «климб = ход вперёд» (corr −0.35), но продольная ось демпфера на наборе и так молчит по своему гейту _AltSettled; на ровных кадрах окна corr +0.67.

Попутно тест-стенд стал честнее: синтетическая земля теперь многооктавная — на чистом мелком шуме варп под скользящим углом алиасился так, что LK менял знак потока (поймал при написании теста масштаба). Все 33 проверки test_ipm_adapt зелёные, память обновлена.

Осталось доказать полётом: тот же профиль, повиси на 0.3–0.8 м — теперь демпфер должен держать с отрыва, а в bag код −1 должен появляться только у самой земли (<0.15 по EKF). В дереве остался только твой doc/tmp/9.txt — скажешь, закоммичу с очередной пачкой заметок.



Resume this session with:
claude --resume 47154f72-2494-4536-9b19-2c3ac4908ee1
andriy@andriy:/usr/local/DATA/Calude/13.17
$ 














Кладу генератор в репо рядом с моделью (скретчпад эфемерен) и правлю ссылку в шапке SDF:

  Ran 1 shell command

Готово — п.2 сделан, но реплей вскрыл вещь поважнее: симовский мир слеп у земли по построению, и я сделал для этого опт-ин фикс мира.

1. Масштабно-инвариантная полоса (ipm_scale_ref, лётный дефолт 3.0). Вся геометрия полосы теперь ∝ h/3: на 0.6 м канал смотрит на 0.6–1.2 м впереди под теми же углами 27–45°, что сегодня на 3 м. Пиксельный размер варпа не меняется вовсе (заодно ушёл старый дребезг формы от float-пыли, изредка рвавший канал кодом 7). На h=3 — бит-в-бит со старым поведением. Ручка BS_IPM_SCALE_REF, 0 = легаси. Все 19 тестов зелёные (новая секция 8: тождество на h_ref, метрика на 0.6 м, пол+масштаб).

2. Офлайн-реплей по твоему бэгу 041255 (фиделити реплея к записи полёта 0.92): крейсер 1.8–3.5 м — регресс-нейтрально (gain 0.96 у обоих). А вот у земли новая полоса выдала ok всего 17% — и разбор этого привёл к находке: LK мёр не от величины смещения и не от гейтов, а потому что варп пустой. Я вывел кадр с полосой на нём: земля ближе ~3 м в этом мире — гладкая серая каша без единой детали (лаплас 1.5–3 против 22–30 у горизонта). Причина записана прямо в модели мира: gz-sim не тайлит текстуру по грани бокса, один grass_dry.png 512 px растянут на 150 м — texel 29 см. Это же убивало и полнокадровый LK ниже 1.2 м (conf 0.04) — слеп мир, а не алгоритмы. При этом на кадрах, где хоть что-то нашлось, масштабная полоса меряет отлично: gain +0.86, corr +1.00 (фиксированная там же гнала 0.5).

3. Фикс мира — mili_fortress_fine.sdf (опт-ин, база не тронута). Модель grass_plane_fine: текстура тайлится геометрией (сетка визуалов 15×15 м → texel 1.5 см, многооктавная фактура, повороты тайлов рвут периодичность); коллизия, высота, спавны — 1:1 с базой. Проверил живым кадром на поднятом стеке: земля плотно фактурная до среднего плана. VINS от этого только выиграет (фич больше). Базовый мир остался дефолтом — сравнимость старых серий не тронута.

Полетать у земли:
WORLD=/root/worlds/mili_fortress_fine.sdf LV=2 BS_SF_MASTER=1 bash src/lab/freefly_lv.sh
(env доедет сам — freefly_lv делает fresh-start). Ожидание: на 0.5–1.2 м канал впервые получит и фактуру, и правильные углы — демпфер должен начать честно мерить скорость, а не давить в упор вслепую. Стек сейчас стоит поднятый на fine-мире после моей проверки; твой запуск его всё равно пересоздаст. Скажешь — закоммичу всё (плюс твой 9.txt).





Всё сделано, дерево чистое, ветка nn2_c3_laptop_vins запушена.

.env: строка WORLD=/root/worlds/mili_fortress_fine.sdf лежит в docker/sim/.env — теперь любой прогон по умолчанию идёт на fine-мире, префикс не нужен:
LV=2 BS_SF_MASTER=1 bash src/lab/freefly_lv.sh
Вернуться на базовый мир — закомментировать строку в .env. Сам .env в gitignore (локальный конфиг машины), поэтому в коммиты не попал — это штатно, как с VINS_SRC.

Четыре коммита ушли:
- 7ac8b5f feat(control) — авторитет IPM-осей = 1, гейт земли 0.08, масштабно-инвариантная полоса (ipm_scale_ref) + тесты и реплей-валидация в сообщении;
- fc338eb feat(mission) — LOITER ниже 1.5 м (loiter_alt=0.5, одна правда на Freefly/LoiterHold/HUD) + прокидка лётных ручек (BS_IPM_SCALE_REF, BS_LOITER_ALT);
- 722bbe0 feat(sim) — мир mili_fortress_fine (тайлинг геометрией, 4096px → texel 3.7 мм, генератор в каталоге модели, ручка WORLD в compose);
- 2069a0b docs(sim) — твой doc/tmp/9.txt.

Можно лететь.




















