
    
    src/control/control_pkg/domain/control/stabilization.py
    


    ПРУЖИНА (разбор yaw_ab_ki60_win03/spring, 2026-08-27). До `_dot` D-член брался
    разностью ОШИБКИ, куда утечка подмешивает −err/leak — при kp=0 это скрытый П-член
    kd/leak = 0.75 PWM/ед. Юнит-тест оценил его в «0.6% за кадр», но мерил при err≈0;
    в полёте контур не успевает за уставкой (заказ 50°/с, борт давал 11–14) и ошибка
    НАМАТЫВАЕТСЯ до 350–430° — тогда член утечки это 70–90 PWM, доминанта выхода. Итог
    двусторонний: во время команды намотанная ошибка ДУШИТ собственную команду (PWM
    пресса падал −47 → −29 от нажатия к нажатию), а после отпускания стика утечка
    сливает ошибку через D — контур сам РАЗМАТЫВАЕТ борт назад на 92–96% разворота
    (длинный пресс: +349° вправо → −330° обратно, τ≈10 с). Пик обратного PWM сошёлся
    с теорией kd/leak·S·err в пределах 3%. Лечение — `_dot`: утечка осталась жить в
    накопителе (для П-члена курс-холда), но производную больше не кормит.

    ПРЯМАЯ ПЕРЕДАЧА СТИКА (`pilot_gain` > 0, PWM при полном стике). Пока yaw-стик жив,
    ось отдаёт пилоту провод: PWM = pilot_gain·c_yaw, зрительный контур ОБНУЛЯЕТСЯ
    каждый тик (`enter`) — уставке не из чего наматываться, отпускание = демпфер с
    чистого листа (после взведения arm_frames). Это тот же ход, что открытый контур
    ControlStack на незанятой оси (c_yaw → PWM выше центра при стике вправо), поэтому
    знак БЕЗ osign. Смысл двойной: (1) пружина невозможна по построению — стирается
    сам её накопитель; (2) полный стик получает честный темп FCU, а не хвост погони
    контура за уставкой (замер spring: заказ 0.86·60≈50°/с, борт давал 11–14°/с).
    Цена: пока стик жив, визуального демпфера нет — рыскание держит rate-контур FCU
    (RC yaw = команда скорости, центр = стоп), этого достаточно. 0 = выкл (токены
    yaw_l/yaw_r калиброваны через уставку — им передача не нужна и вредна).
    В /flow_dbg6 передача видна как (0, 0, PWM≠0)."""



src/control/test/test_flow_strategies.py
# --- ПРУЖИНА (регрессия разбора yaw_ab_ki60_win03/spring, 2026-08-27) ---
# Боевой сценарий: kp=0, борт не успевает за уставкой → ошибка НАМАТЫВАЕТСЯ (в полёте
# до 350–430°). Старый D-член (разность ошибки) после отпускания стика сливал её через
# утечку в PWM и разматывал борт обратно на 92–96% разворота. Требование: при kp=0
# накопленная ошибка ИНЕРТНА — нет вращения (flow_yaw=0) и нет команды (стик отпущен)
# → нет и выхода, сколько бы ни намотало.



src/control/control.md

### ПРУЖИНА курса и прямая передача стика (серия spring, 2026-08-27)

Живой пилот (freefly, `yaw_kp=0`) жаловался: yaw ведёт себя как пружина, наматывающаяся
вокруг борта. Разбор `docker/sim/output/joystick/yaw_ab_ki60_win03/spring/` (8 прогонов
long/short × left/right, `src/lab/yaw_spring_check.py`) показал механизм из двух
половин:

1. **D-член видел утечку.** До фикса производная бралась разностью ошибки, куда утечка
   подмешивает `−err/leak` — при kp=0 это скрытый П-член `kd/leak = 0.75 PWM/ед.` по
   накопленной ошибке. Юнит-тест оценивал его в «0.6% за кадр», но мерил при err≈0.
2. **Ошибка наматывается без предела.** Контур не успевает за уставкой (заказ
   0.86·60≈50 °/с, борт давал 11–14): за длинный пресс уставка убегала на 3–4 оборота
   (`sp_move` до −2049°), намотанная ошибка выходила на квазистационар 350–430°.

Итог в полёте: намотанная ошибка ДУШИТ собственную команду (PWM пресса падал −47 → −29
от нажатия к нажатию — «пружина сопротивляется»), а после отпускания стика утечка
сливает ошибку через D-член и борт РАЗМАТЫВАЕТ обратно 92–96% разворота (длинный пресс:
+349° вправо → −330° назад, τ≈10 с; пик обратного PWM сошёлся с теорией `kd/leak·S·err`
в пределах 3%). Конфиденс 0.97–1.00 — гейты ни при чём.

Лечение двухслойное (оба слоя — `DpYawHold`):
- **`_dot`**: D-член читает замеренную скорость кадра, утечка правит только накопитель.
  При kp=0 накопленная ошибка ИНЕРТНА — пружина невозможна (регрессия в
  `test_flow_strategies.py`).
- **`yaw_pilot_gain` (`BS_YAW_PILOT_GAIN`, PWM при полном стике; 0 = выкл)**: пока
  yaw-стик жив, PWM идёт прямо со стика (знак — как у открытого контура, без osign),
  контур обнуляется каждый тик; отпустил — демпфер с чистого листа. Пилот получает
  честный темп FCU вместо хвоста погони контура. `freefly_lv.sh` включает 130
  (~60 °/с по замеру spring: ~0.45 °/с на PWM). Токены `yaw_l/yaw_r` калиброваны через
  уставку — им передача не нужна (дефолт конфига 0). В `/flow_dbg6` передача видна как
  `(0, 0, PWM≠0)`.






Готово. Пружина найдена, доказана цифрами и вылечена в коде; 
    лётной проверки ещё не было.

Диагноз (по вашим 8 бэгам, src/lab/yaw_spring_check.py)

Пружина — наш собственный курс-холд (DpYawHold), из двух половин:

1. D-член видел утечку. Производная бралась разностью ошибки, куда утечка подмешивает −err/leak. При боевом kp=0 это скрытый П-член kd/leak = 0.75 PWM/ед. по накопленной ошибке уставки. Юнит-тест оценивал его в «0.6% за кадр» — но мерил при err≈0.
2. Ошибка наматывается без предела. Контур не успевает за уставкой: заказ 0.86·60≈50°/с, борт реально крутился 11–14°/с. За длинный пресс уставка убегала на 3–4 оборота (sp_move до −2049°), намотка выходила на 350–430°.

В полёте это выглядит ровно как ваша пружина: во время удержания стика намотка душит собственную команду (PWM пресса падал −47 → −29 от нажатия к нажатию — сопротивление растёт), а после отпускания утечка сливает намотку через D-член и борт разматывается обратно на 92–96% разворота (long/left: +349° вправо → −330° назад, τ≈10 с; на коротких сериях долг копится через нажатия и в конце разматывает 190–260°). Теория сошлась с записью: пик обратного PWM = kd/leak·S·err с точностью 3% (предсказано 84, замерено 86). Конфиденс 0.97–1.00 — гейты ни при чём.

Фикс (внесён, все офлайн-тесты зелёные)

- _dot (stabilization.py): D-член кормится замеренной скоростью кадра, утечка правит только накопитель. При kp=0 намотанная ошибка теперь инертна — пружина невозможна; заявление «при kp=0 совпадает со старым законом побитово» стало наконец правдой.
- BS_YAW_PILOT_GAIN — прямая передача стика (это про «больше доверия»): пока yaw-стик жив, PWM идёт прямо со стика (знак как у открытого контура), зрительный контур обнуляется каждый тик; отпустил — демпфер с чистого листа. Пилот получает честный темп FCU вместо хвоста погони контура. В freefly_lv.sh дефолт 130 PWM ≈ 60°/с (по замеру серии: ~0.45°/с на PWM); конфиг-дефолт 0 — токены yaw_l/yaw_r не задеты. Проводка: config → recipes → argparse → bootstrap_arch2 → freefly_lv.
- Регрессия в test_flow_strategies.py (намотка при kp=0 больше не командует; передача: PWM со стика, кламп, обнуление контура). Все 14 сьютов src/control/test и 6 src/mission/test — зелёные.
- Дока: control.md «ПРУЖИНА курса и прямая передача стика», анализатор положен в src/lab/yaw_spring_check.py + строка в src/lab/CLAUDE.md, память yaw-spring.md.

Проверка

Повторить тот же протокол spring (restart-all пересоберёт colcon сам) и прогнать тем же скриптом — ждём err@rel≈0, return%≈0, PWM после отпускания ≈0. Один нюанс: реплеи старых spring-бэгов теперь поведут себя иначе (передача перехватывает стик) — со старыми сериями бит-в-бит не сравнивать. Коммитить не стал — скажете.






------------------------------------
BS_YAW_PILOT_GAIN — прямая передача стика (это про «больше доверия»): пока yaw-стик жив, PWM идёт прямо со стика (знак как у открытого контура), зрительный контур обнуляется каждый тик; отпустил — демпфер с чистого листа.
------------------------------------








Запушено в nn2_c3_laptop_vins, дерево чистое. Два коммита:

┌─────────┬────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Коммит  │                                                Что                                                 │
├─────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ af40901 │ fix(control): пружина курса — _dot (D-член без утечки) + прямая передача yaw-стика                 │
│         │ BS_YAW_PILOT_GAIN=130; регрессия в тестах, yaw_spring_check.py, разбор в control.md                │
├─────────┼────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ c099356 │ docs(tmp): ваши заметки yaw.txt + хвост стенограммы pid.txt                                        │
└─────────┴────────────────────────────────────────────────────────────────────────────────────────────────────┘

Следующий шаг за вами: повторить протокол spring (обычный make restart-all пересоберёт colcon сам) — и прогнать ту же раскладку через yaw_spring_check.py. Критерий: err@rel≈0, return%≈0, PWM после отпускания ≈0.




● Running spring analyzer on the two post-fix bags
  ⎿  $ for r in long_right short_right; do docker run --rm -v /usr/local/DATA/Calude/13.17/docker/sim/output/joystick/yaw:/yaw:ro -v /usr/local/DATA/Calude/13.17/src/lab:/lab:ro -e BAG=/yaw/$r/bag sim-nav:latest bash -lc 'source /opt/ros/humble/setup.bash 2>/dev/null; python3 /lab/yaw_spring_check.py' 2…


┌──────────────────────────┬───────────────────────────────────┬─────────────────────────────────────────────┐
│         Метрика          │      До (spring, 2026-08-27)      │           После (yaw/*, сегодня)            │
├──────────────────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
│ Возврат после длинного   │ 92–96% разворота (−330° назад)    │ 4% (+7.6°)                                  │
│ пресса                   │                                   │                                             │
├──────────────────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
│ Возврат после коротких   │ 30–70%, долг копился до размотки  │ 0 (докат −7..−14° в ту же сторону, 0.1–0.2  │
│                          │ 190–260°                          │ с — инерция)                                │
├──────────────────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
│ Намотка err@rel          │ 350–430°                          │ 0 на всех прессах                           │
├──────────────────────────┼───────────────────────────────────┼─────────────────────────────────────────────┤
│ Авторитет стика          │ 11–14°/с при заказе ~50 (пружина  │ 0.44°/с на PWM, полный стик ≈ 57–60°/с — по │
│                          │ душила)                           │  расчёту                                    │
└──────────────────────────┴───────────────────────────────────┴─────────────────────────────────────────────┘

Обе половины фикса отработали как задумано: 
передача рулит (sp_move=0 — контуру нечего наматывать), 
демпфер после отпускания молчит (PWM ≈ 0), 
конфиденс 0.98–1.00. 
Докат на коротких тычках — физика торможения FCU + slew 300 на сбросе команды, 
не контур; если когда-нибудь захочется резче стопить, рычаг — kd демпфера, 
но по ощущениям, судя по вашему «сильно лучше», трогать нечего.

Обновил: память (yaw-spring → доказан полётом, с цифрами), control.md (блок «ДОКАЗАНО полётом 2026-08-28») и строку анализатора в src/lab/CLAUDE.md. Правки в двух файлах репы не закоммичены — скажете «комить» — запушу.


Resume this session with:
claude --resume 9f2bb906-9706-4607-97a2-4f583cf9aa84





Два момента по ходу, чтобы ты знал:
- Форк на этом ноуте лежит в /home/andriy/VINS-MONO-ROS2 (VINS_SRC в .env), не в /root, как пишет CLAUDE.md по умолчанию. В .git было 27 root-овых файлов (наследие git-команд из контейнера, включая мои git status в этой сессии) — коммит с хоста падал с «Permission denied»; вернул владельца andriy через chown в одноразовом контейнере, теперь git работает с хоста напрямую. Пуш — по ssh (в контейнере кредов нет).
- Стек сейчас остановлен (ты его погасил после полёта) — make restart-all поднимет уже с патчами, пересборка не нужна.









