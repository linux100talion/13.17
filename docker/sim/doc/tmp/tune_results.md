# FLOW-DAMP — тюнинг гейнов (гибридный харнес)

Журнал прогонов тюнинга бокового флоу-демпфера. Харнес и теория —
`HowToFlow_PID_synth.md` (синтетика A+B+C) и `FAQ_pid.md` (почему ki=0).

Харнес: `BS_GZHOLD=1 BS_FLOWHOLD=1` — gz держит продольную (pitch) истиной Gazebo,
флоу демпфирует боковую (roll). Метрика — абсолютная боковая RMS_v по drift_check.py
(SAFE_SEC=30, R_SAFE=40). Продольная gz-held ≈ 0.1 (норма — нормировка не применима).

Зафиксировано: `ki=0` ✅ (ki>0 = пружина+windup → растущая осцилляция),
`osign=+1` ✅ (osign=-1 = положительная ОС → разгон-убегание).

| # | config | боковая RMS_v | e_y (мин→макс) | итог |
|---|---|---|---|---|
| H1 | kp8 ki2 osign+1 | 2.04 | +15 → −45 м (РАСТЁТ) | ki-пружина раскачивает — снято |
| H2 | kp8 ki0 osign+1 | **0.41** | +7.5 → −3.7 м (огранич.) | задемпфирован ✅ |
| H3 | kp16 ki0 osign+1 | хуже H2 | шумно | (переоценено синтетикой — см. ниже) |
| H4 | kp8 ki0 osign+1 | 2.40 | −64 м (медл. осц.) | ТОТ ЖЕ конфиг что H2 — убежал (недетерминизм) |
| O1 | kp8 ki0 **osign−1** | 3.03 | −479 м, v_y→−33 м/с | КАТАСТРОФА: полож. ОС, разгон-убегание. Знак опровергнут |
| O2 | **kp4** ki0 osign+1 | 2.76 | +42.8 м, v→6.35 м/с→−1.85 | СЛИШКОМ СЛАБО: монотонный снос на 42 м. «kp вниз» опровергнут |

## Вывод после O1
- `osign=+1` — правильный демпфирующий знак (O1 доказал: −1 = разгон до 33 м/с).
- Знак НЕ причина дисперсии H2(0.41)↔H4(2.40) при идентичном конфиге.
- (ГИПОТЕЗА, позже опровергнута синтетикой) лаг-индуцированная маргинальная устойчивость,
  направление «kp вниз». O2 её опроверг.

## Вывод после O2 (пересмотр)
- «kp вниз» ОПРОВЕРГНУТ: kp4 слишком слаб (снос 42 м, v до 6.35 м/с, roll_off ≤26).
- O2 — НЕ осцилляция, а один медленный сброс: e_right монотонно 0→42.8, потом v
  разворачивается (−1.85) → osign=+1 = чистая отриц. ОС, просто слабая/медленная.
- Центральная загадка H2(0.41) vs H4(2.40): один конфиг, два исхода. Боковой замкнутый
  контур недетерминирован (тайминг обработки кадров flow — wall-clock), sim-override
  детерминизировал только продольную → одиночный прогон ненадёжен.

## СИНТЕТИКА A+B+C (коммиты 442d07d, 3fbc2f3) — разрыв тупика
Полный прогон дорог (RTF 0.07, ~5 мин) и недетерминирован → тюнить им нельзя.
Три детерминированных оффлайн-теста (см. `HowToFlow_PID_synth.md`):
- **A** (`flow_synth_test.py`): rsign=+1 и матрица R подтверждены детерминированно
  (без Gazebo) — синтетические пары кадров с известным сдвигом/поворотом.
- **B** (`flow_loop_sim.py`): numpy-ОДУ контура + ТОТ ЖЕ PID. osign−1→runaway (=O1);
  P-only ограничивает v (`v_term=d/(|k|·kp·s)`), выше kp → ниже v_term. Осцилляции
  P+лаг нет (пружина слишком медленная для реалистичного τ) → уносы O2/H4 = слабый kp,
  не P-нестабильность.
- **C** (`flow_calib.py`): system-ID планта из O2-бэга, ВАЛИДНА (пред 4.2 vs набл
  2.76 м/с). Параметры: s=0.807 px/(м/с), bias=−0.5px, noise=1.65px, k=−0.0196,
  d=0.266, τ≈0.
  ⚠ **R²(flow↔v)=0.19** — ПОТОК СЛАБЫЙ ПРОКСИ СКОРОСТИ (81% шум). Это потолок тюнинга.

## Направление (пересмотр по калиброванному B) — kp ВВЕРХ
Калиброванный свип (ki0 osign+1): kp2=5.53(РАСХ) kp8=1.44 kp16=0.47 kp24=0.19 kp32=0.18.
→ Целевой режим: **kp16-24, ki0, osign+1** → RMS_v ~0.2-0.5 м/с, дрейф ограничен.
(H3 kp16 «хуже» был одиночным недетерминированным прогоном — калибровка его пересматривает.)

## C1 — подтверждающий kp16 (реальный гибрид): модель промахнулась
| C1 | kp16 ki0 osign+1 | **1.68** | −39.7 м (30с, НЕ обрезано) | устойч. (v→0 к концу), но снос 42 м на транзиенте |
- Реальный **1.68** vs предсказ. B **0.47** → модель оптимистичнее в 3.6×.
- ПОДТВЕРЖДАЕТ структурный шум: H3 (kp16 «хуже») не случаен — kp16 реально хуже
  белошумной B. B перехваливает высокий kp → числам при большом kp верить нельзя,
  пока не заложена реальная структура шума (автокорреляция).
- kp16 НЕ убежал (устойч., v_right→0), но velocity-демпфер не вернул 42 м позиции.

## Кампания калибровочных бэгов (коммит 58edcd2+) — итог
| бэг | окно | conf | s (R²) | k (R²) | τ | noise/автокорр |
|---|---|---|---|---|---|---|
| O2 (замкнутый kp4) | 15.5с | 0.97 | 0.81 (0.19) | −0.0196 физика | ~0 | 1.65px |
| roll-chirp (амп50/f0.15) | 12с | 0.82 | 0.37 (0.14) | −0.0067 (0.31) | ~0 | 1.83px/бел |
| **gz-круг R4/T10** | **35с** | **0.95** | 0.45 (0.07) | **−0.0119 (0.95)** | 1.14с | 1.89px/бел |

Выводы (чистый gz-круг — эталон):
- **τ=1.14 НЕ причина** промахов B: B при τ=0.15 и 1.14 почти идентичен (петля медленная).
- Промах B — **параметры скачут между бэгами** (s 0.37↔0.81, k 0.0067↔0.0196):
  kp16 предсказ. 0.47 (O2) ↔ 2.81 (круг), реальный C1=1.68 МЕЖДУ. Не пиннятся.
- **s скачет из-за `flow∝v/глубина`** (монокулярный масштаб) — детектором не лечится.
- **ЖЁСТКИЙ ПОТОЛОК: пол шума ≈1.9px БЕЛЫЙ + s≈0.5 → демпфер слеп к v≲2-4 м/с**
  (сигнал s·v тонет в шуме). Реальный kp16=1.68 сидит на этом полу; kp32→1.48, дальше
  kp не помогает. Это НЕ детектор (conf 0.95), а шумовой пол.

## Сглаживание потока (коммит b8d69d0) — работает
Оффлайн (gz-круг, flow_calib SMOOTH): пол шума 1.89→0.77px (N9), R² 0.07→0.24.
| C2 | kp16 ki0 osign+1 **smooth N=5** | **1.58** | −15.7 м (30с) | дрейф 39.8→15.7 (2.5×!), RMS_v 1.68→1.58 |
- Сглаживание СИЛЬНО режет дрейф (шумовые броски позиции), RMS_v лишь чуть (пол ~1.5
  задан петлёй/терминальной скоростью, не шумом).
- B брекетит реальность, НЕ пиннит: kp16 → 0.47 (O2-params) ↔ 3.02 (круг-params),
  реал 1.58. Как абсолютный предсказатель B пока не надёжен (d,s гуляют между бэгами).
- Тренд согласован: kp вверх ↓RMS_v. B(сглаж): kp32→1.55, kp48→1.01.

## Balanced roll-excite бэг (ветка b) — catch-22, но d запинен
Balanced +τ/−2τ/+τ НЕ удержал (снос до e_right −272м): roll свободен (gz держит только
pitch), а возмущение d монотонно уносит → профиль-возврат работает лишь БЕЗ возмущения.
Catch-22: свободный roll нужен чтобы измерить d, но d же и уносит. НО короткое окно дало:
- **d = −0.328** (реальный свободный снос; круг маскировал 0.04, совпадает с O2 0.27) ✓
- **k = −0.0097 (R²0.72)** ✓ — консистентно с кругом (−0.012).

## Мета-вывод: синтетика выдала максимум, финал — эмпирика
- Направление НАДЁЖНО: osign=+1, ki=0, **kp вверх**, сглаживание режет дрейф/шум.
- Запинено: k≈−0.010, d≈0.33.
- **s неустранимо неопределён** (масштаб flow∝1/глубина, монокуляр) → абсолютный RMS_v
  у B «плавает» ~2× → B НЕ называет точный kp. Плюс B стабильно пессимистичен
  (реал kp16smooth=1.58 < B 2.8+). Реальность добрее модели.
- Вывод: B = инструмент НАПРАВЛЕНИЯ и границ, не точного kp. Финал — эмпирика.

## Глубина сцены — жёсткий предел сигнала (челнок, изолированная трансляция)
Мягкий боковой челнок (крен ±4°, conf 1.00, чистая трансляция) → s=0.43 →
эффективная глубина сцены ≈50м (s=f·dt/D). При v=1.5 м/с сигнал s·v=0.64px < шум
1.6px → ПОКАДРОВО демпфер слеп к v≲3.75 м/с. Не крен/детектор/тюнинг — ДАЛЬНЕЕ ПОЛЕ
(FAQ_vins §10). Рычаг для скачка — близкая текстура (лететь ниже/у объекта, s∝1/D).

## Ручка kp вверх + сглаживание — РАБОТАЕТ (замкнутый контур бьёт покадровый пол)
| config | RMS_v | дрейф |
|---|---|---|
| kp4 | 2.76 | убежал |
| kp16 | 1.68 | 39.8м |
| kp16+smooth5 | 1.58 | 15.7м |
| **C3: kp32+smooth5** | **0.62** | 13.5м | ← ОПТИМУМ |
| C4: kp48+smooth5 | 1.07 | 28.1м | ХУЖЕ (прошли оптимум) |
Замкнутый контур + сглаживание усредняют белый шум по кадрам → извлекают устойчивую
скорость дрейфа НИЖЕ покадрового шумового пола. Демпфер реально гасит (0.62 м/с).
kp48 — РАЗВОРОТ В МИНУС: высокий kp усиливает остаточный шум (ниже сигнального пола на
D~50м) → джиттер roll → нетто хуже, дрейф вдвое, yaw-размах 17° vs 11°.

## ФИНАЛ ручки: kp32, ki0, osign+1, smooth5 → RMS_v 0.62 м/с
Ручка kp выжата (оптимум kp32; kp48 хуже). Следующий скачок — НЕ гейн, а близкая
текстура (глубина сцены), либо динамические гейны (gain scheduling kp∝1/s) — см.
FLOW_DAMP_spec.md «Динамические гейны».
