Переписал docker/sim/env.md начисто под новую схему. Теперь документ строится от пары файлов, а не от одного .env:

- Заголовок и вступление — таблица ролей: env.default (в git, эталон проекта) против .env (gitignore, рабочая копия бокса), сразу за ней механизм автопосева (make ensure-env на четырёх точках входа + freefly_lv.sh) и правило «существующий .env никогда не перетирается».
- Явное правило «куда писать правку» вынесено наверх отдельным блоком: дефолт проекта → env.default и свой .env (автопосев старый файл не обновит); машинная подгонка → только .env. Раньше это пряталось в граблях.
- «Кто читает» — compose и freefly_lv с лесенкой приоритетов и баннером в логе — сохранён, лесенка теперь внутри пункта про freefly_lv, где ей место.
- Таблица ключей переименована в «Ключи эталона», содержимое то же (с пометками машинно-зависимости у VINS_SRC/CUDA_ARCH_BIN).
- Грабли почищены от дубля с «куда писать правку»: остались четыре реальных — restart-all env не перечитывает, обновлённый эталон на старом боксе не самоприменяется, чек двух машинных ключей после посева, ключи-которые-никто-не-ест.

Разделы «что ещё можно класть» и правила разбора не менялись — они были свежие.