❯ Отлично. Давай сделаем высоту с 2м, скорость с 1 м.с, и в 2 раза медленее скорость опускания.

Меняю дефолты: гейт 2 м / 1 м/с, снижение 0.15 м/с (обе ветки — и наш газ, и LAND_SPEED). Сначала найду все места, где эти числа зашиты.

  Ran 5 shell commands

Сделано. Новые дефолты ноды (config.py): гейт rel_alt ≤ 2 м, |v| ≤ 1 м/с, снижение 0.15 м/с — в обеих ветках:

- ветка alt (ALT_HOLD под демпфером/VinsHold): газ по формуле AltHold → 1381 PWM (было 1362 при 0.3 м/с);
- ветка pos (штатный LAND из LOITER): LAND_SPEED 15 в sitl-extra.parm (было 30). В метаданных параметра диапазон 30–200, но это подсказка GCS — прошивка нижней границы не ставит (land_run_vertical_control берёт просто −|LAND_SPEED|), значение применится. .parm подхватывается на fresh-start.

Тесты: test_mission_plan.py проверяет новые дефолты (1381; 2.0/1.0/0.15), test_freefly_land.py оставлен на явных параметрах (механику он тестирует, не дефолты) — вся тройка зелёная. Доки/шапки (CLAUDE.md, hud.md, freefly_lv.sh, README джойстика, make sa-land, память) переписаны под новые числа.

Два замечания к прогону:
- Время снижения выросло ×4: с 2 м на 0.15 м/с ≈ 13–17 с. Бэкстоп land_budget 45 с — запас есть, но уже не 13-кратный; если баро «застрянет» и касание не поймается, LAND_TIMEOUT придёт через 45 с.
- 0.15 м/с — это 19 PWM ниже мёртвой зоны; реальную скорость по ALT в HUD/ленте стоит сверить с формулой (она калибрована на большие шаги газа), при расхождении крутить BS_LAND_RATE.











































