# FLOW-DAMP — спецификация (черновик v0)

Scale-free velocity-демпфер от ветра по ОДНОЙ форвард-камере. Переключаемый режим:
включается у трудного места, игнорирует VINS/одометрию, гасит снос, пропускает стики.
Концептуальное обоснование целиком — `FAQ_vins.md` (разделы 6–15). Связано: `todo4.txt`
(срыв VINS при малом движении), `src/lab/CLAUDE.md` (gz-hold как референс-костыль).

Легенда: ✅ — твёрдое (выводится из разобранного), ⚠️ — открыто (нужно решение).

---

## 1. Назначение ✅

Переключаемый режим: включается у трудного места. Scale-free velocity-демпфер по одной
форвард-камере, ИГНОРИРУЕТ одометрию/VINS, РЕАГИРУЕТ на стики. Цель — гасить снос
(особенно от ветра) там, где VINS хрупок/срывается (малое движение). Комплементарен VINS,
не замена: выживает ровно в том режиме, где VINS дивергирует (FAQ_vins п.12 Q1).

## 2. Границы — чего НЕ делает ✅

- **не** position hold — только velocity damping → медленный дрейф позиции остаётся;
- **не** управляет наклоном — pitch/roll держит FCU (rate=гироскоп, level=гироскоп+
  акселерометр через AHRS); камера для углов не нужна (FAQ_vins, наблюдаемость);
- **не** метрический, **не** через EKF3 — это путь (B) из FAQ_vins п.15: прямой регулятор
  поверх ALT_HOLD (scale-free flow в EKF3 не лезет — он хочет метры).

## 3. Тракт ✅

```
базовый режим: ALT_HOLD (баро-высота, self-level от FCU)
нода = расширение alt_hold_bootstrap.py (флаг --flow-hold), реюз RC override 20 Гц
       (каналы 1-2 = roll/pitch)
вход:  /image_mono (или /image_color) + gz-IMU гироскоп (FLU) + extrinsicRotation
пайплайн:
  optical flow → derotate (ω_cam = R · ω_imu) → остаточный ТРАНСЛЯЦИОННЫЙ поток
    → управляющие сигналы → PID → PWM-offset roll/pitch → RC override
```

## 4. Сигналы → оси (решено — фазировка)

Философия: **v0 = только сильные, высоко-SNR оси; слабые (looming, визуальный yaw) — фаза
2 поверх уже работающего ядра.** Так легче писать и отлаживать (изоляция переменной).

| Ось | v0 | фаза 2 |
|---|---|---|
| **боковой снос** | ✅ ДЕМПФЕР (главное): средний горизонт. поток → ROLL | — |
| **продольный** | пилот (сквозной стик) | looming-зануление: дивергенция → PITCH, гейт по confidence |
| **yaw** | пилот (сквозной), derotation защищает боковой сигнал | визуальное удержание курса |
| **поток** | sparse LK + аффинный фит поля | — |

Обоснование решений (детали — обсуждение + FAQ_vins):
- **боковой → ROLL** — крепкая, высоко-SNR ось; ядро v0;
- **продольный → пилоту в v0**: looming — наихудшей обусловленности (FAQ_vins п.9: ноль в
  центре, низкий SNR, вырождается в дальнем поле) + риск тряски pitch (pitch двигает обзор
  → обратная связь). looming-алгоритм в фазе 2 ТОТ ЖЕ, но ложится на доказанное ядро +
  переиспускает готовый confidence-гейт. Грубую низко-авторитетную версию в v0 НЕ делаем
  (шумный pitch-контур уходит в обратную связь — лучше ноль, чем грубый looming);
- **yaw → пилоту в v0**: derotation по гироскопу УЖЕ снимает yaw-вращательный поток из
  бокового сигнала → визуальный yaw-hold для работы демпфера НЕ нужен (это отдельная
  плюшка, FAQ_vins п.7, требует развязки bas-relief п.8). При активном yaw треки рвутся →
  confidence падает → §7-гейтинг приглушает (это и нужно).

Целевой сценарий v0 — **A (пролёт между препятствиями)**: боковой снос критичен (увод на
кусты), вперёд рулит пилот. Сценарий B (подход к стене, продольный критичен) — это и есть
фаза 2 (looming становится основным контуром).

## 5. IMU-вход ✅

- **только гироскоп** (gz-IMU, FLU, `/gz_imu/data_flu`), **только для derotation**;
  акселерометр НЕ нужен (мы scale-free, attitude не оцениваем);
- `extrinsicRotation` из `sim.yaml` (R камера↔IMU) — перевод ω в фрейм камеры; нужна
  только ротация (рычаг на ω не влияет — твёрдое тело, одна ω везде);
- `extrinsicTranslation` [0.15,0,0.05] — ОПЦ. компенсация рычага `v_рычаг = ω×r` (камера
  смещена от IMU → при вращении едет по дуге → фейковая трансляция); или гейтинг по ω;
- фрейм-цепочка: `gz-IMU (FRD) → imu_frd_to_flu.py → FLU → extrinsicRotation → optical`,
  та же `extrinsicRotation`, что у VINS (согласованность с триангуляцией).

## 6. Стик = setpoint ✅

- стик задаёт **желаемый поток** (scale-free); `error = измеренный − заданный`, PID → error→0;
- центр стика → desired=0 → зануляем всё (держим, гасим ветер); отклонён → desired=X → летим;
- демпфер борется ТОЛЬКО с ветром (некомандованной скоростью), команды проходят
  (FAQ_vins п.12 Q2 — стиль LOITER/FLOWHOLD, но в flow-единицах);
- бонус: у близких препятствий тот же стик = меньшая реальная скорость (само-торможение).

## 7. Безопасность ✅

- **детектор качества потока** (мало фич / темно / aperture) → **плавный fade-out в чистый
  ALT_HOLD** (ручной), а не лить мусор в override (аналог failureDetection VINS, но
  доброкачественный — нода простаивает, не дивергирует);
- **гейтинг при большом ω** (активный поворот), чтобы рычаг и ошибка derotation не дёргали
  демпфер (реюз паттерна из alt_hold_bootstrap.py: yaw не льётся одновременно с translate).

## 8. Реюз существующего кода ✅

- `src/lab/alt_hold_bootstrap.py`: тракт RC override (20 Гц, `_wall_publish`/`tick`),
  ALT_HOLD, arm/climb/land, PID-блок `--gz-hold` (~277–330 — меняется ТОЛЬКО источник
  скорости: поток вместо gz-истины), паттерн yaw-гейтинга (~351–356);
- `src/vins/.../config/sim.yaml`: extrinsic (R + t);
- `/gz_imu/data_flu` + `imu_frd_to_flu.py`: FLU-гироскоп;
- `src/lab/capture_scene.sh`: атомарный прогон для приёмки.

## 9. Приёмка / тест-план ✅

- **оракул истины** — истинная поза Gazebo (`/model/iris_cam/odometry`): сравнить дрейф
  позиции с демпфером и без него;
- **ветер**: `SIM_WIND_*` → измерить, насколько демпфер срезает скорость сноса;
- **проброс стиков**: отклонить → летит в нужную сторону; центр → гасит;
- **fade-out**: убрать текстуру/затемнить → плавный возврат в ALT_HOLD без рывка.

---

## Решено (1–3, см. §4)

1. **Источник потока**: ✅ **sparse LK + аффинный фит** (агрегаты: средний поток +
   дивергенция + curl; реалтайм на 10 Гц; число треков = confidence; свой LK, независимый
   от VINS — переживает ребут VINS). dense — перебор ради агрегатов, тяжело на CPU-боксе.
2. **Продольная ось**: ✅ **пилоту в v0**, looming-зануление — фаза 2 (тот же алгоритм
   поверх доказанного ядра + готовый confidence-гейт).
3. **Yaw**: ✅ **сквозной стик в v0** (derotation защищает боковой сигнал), визуальный
   yaw-hold — фаза 2.

## Решено (4–5)

4. **Топик/частота камеры**: ✅ **`/image_mono`** (8-бит grayscale, вход VINS) — LK всё
   равно работает по grayscale, тот же источник что VINS, минимум оверхеда. Частота —
   камер-rate (~10 Гц в sim, Найквист 5 Гц лимитирует полосу демпфера; на боевой быстрее).
   Поток считается между соседними кадрами; derotation интегрирует гироскоп 250 Гц за
   интервал кадра.
5. **Инжект**: ✅ **RC override** (`/mavros/rc/override`, каналы 1-2 roll/pitch), реюз
   тракта alt_hold_bootstrap.py. Альтернативы отброшены: `VISION_SPEED_ESTIMATE`→EKF3 =
   путь (A), требует метрики (мы scale-free); `SET_ATTITUDE_TARGET` обходит ALT_HOLD и его
   логику стиков. RC override поверх ALT_HOLD — проверенный в стеке путь.

## Скелет v0

`src/lab/flow_damp_node.py` — standalone ROS2-нода (см. файл). v0-контур:
`/image_mono` + `/gz_imu/data_flu` → sparse LK → derotate (ω_cam=R·ω_imu, extrinsicRotation)
→ аффинный фит → боковой поток → PID → ROLL-offset; pitch/yaw/throttle сквозные с
`/mavros/rc/in`; стик roll = setpoint бокового потока; confidence (число треков) → fade-out
в чистый ALT_HOLD. Pitch/yaw-ветки — заглушки-хуки под фазу 2 (looming, визуальный yaw).
TODO в коде: тюнинг гейнов/порогов + валидация знаков (extrinsic-направление, знаки
rot-flow) — связано с оракулом estimate_extrinsic (todo4).

## Трекер фич — свой, НЕ VINS feature_tracker

Трекер фич = два вызова OpenCV прямо в `FlowEstimator` (`src/lab/flow_estimator.py`):
- детектор: `cv2.goodFeaturesToTrack` (Shi-Tomasi углы), метод `_detect()`, параметры
  `self._feat` (maxCorners=200, qualityLevel=0.01, minDistance=8);
- трекинг между кадрами: `cv2.calcOpticalFlowPyrLK` (разреженный пирамидальный LK), в
  `process()`, параметры `self._lk` (winSize=21, maxLevel=3).
Фичи передетектируются каждый кадр (дёшево для скелета), поток = prev_pts→next_pts.
Внешних нод/зависимостей нет.

**Почему свой, а не VINS-овский feature_tracker** (решение 1, §4): у VINS-Mono есть C++-нода
`feature_tracker` — она уже гоняет LK по `/image_mono` и публикует фичи с их скоростями
(по сути готовый оптический поток), реюз был бы бесплатным по вычислениям. НО демпфер
должен ЖИТЬ, когда VINS ребутится (а он срывается каждые ~25 с, todo4). Тап feature_tracker
→ демпфер падает вместе с VINS → теряем главный смысл (выживать там, где VINS срывается).
Поэтому свой ИЗОЛИРОВАННЫЙ LK: техника та же, но независимая от жизненного цикла VINS.

Нюанс (на будущее): если жать CPU — feature_tracker VINS публикует нормализованные
undistorted-точки с УЖЕ посчитанными скоростями (это и есть поток). Но вернётся связанность
с VINS. Для v0 независимость важнее экономии — оставляем свой LK.

## Хвост параллельной задачи — сверка extrinsic

Прогнать VINS с `estimate_extrinsic=2` (без приора) или `=1` (уточнение от текущего) →
сравнить сошедшуюся матрицу с `sim.yaml`. Хенд-расчёт (под допущением optical-frame) дал
ТРАНСПОНИРОВАННУЮ матрицу относительно конвенции VINS RIC=R_imu_cam — но это может
объясняться маппингом Gazebo camera_link→optical, которого хенд-расчёт не учёл; VINS-init
работает → грубо неверной матрица не выглядит. `estimate_extrinsic` = решающий оракул.
Заодно проверит гипотезу, что срыв ~25 с (todo4) — не от кривого extrinsic.
Боевой `dummy_13_7.yaml` — identity-ротация (вероятный placeholder, нужна Kalibr перед
боевым полётом; касается и VINS, и демпфера).

---

# СТАТУС v0 — Шаги 1–3 ЗАКРЫТЫ ✅ (ветка nn2_c3_vins_althold_4)

**FLOW-DAMP v0 валидирован в симуляции: боковой демпфер режет снос ×6 по оси ROLL.**
Ниже — что сделано по каждому шагу + остаток (Шаг 4 / фаза 2 / продольный конфаунд).

## Код (актуально)
- `src/lab/flow_estimator.py` — `FlowEstimator` (numpy+cv2, БЕЗ ROS): sparse LK → derotate
  (ω_cam=R·ω_imu) → боковой поток + диагностика. Дефолты `R`+`rotflow_sign=+1` ПОДТВЕРЖДЕНЫ
  (Шаг 1). Шарится между демпфером и оффлайн-чеком.
- `src/lab/alt_hold_bootstrap.py` — демпфер живёт здесь веткой **`--flow-hold`** (вариант a,
  Шаг 2): реюз hold-only-каркаса + wall-publisher'а 20 Гц. Колбэк `_on_flow_image` (зрение+PID
  раз на кадр) → `flow_roll_off` → ветка `S_EXCITE/flow` (ROLL, pitch/yaw центр) → `_wall_publish`.
  Флаги `--flow-kp/ki/kd/imax/max/conf-*/rsign/osign/*-topic`. `flow_damp_node.py` — устаревший
  standalone (оставлен как боевой вариант фазы 2; в sim не используется).
- `src/lab/flow_derotation_check.py` — оффлайн-валидация знака по bag; добавлен `--omega-max`
  (band-pass по |ω|, отсекает слом LK на быстром вращении).
- gz-hold получил **`--gz-yaw`/`--gz-yaw-period`** — наложенный yaw поверх удержания позиции
  (диагностический чистый-вращение режим, использован в Шаге 1).

## Шаг 1 — знак derotation ✅ ЗАКРЫТ
**Итог: `R` (матрица sim.yaml) + `rotflow_sign=+1` — это текущие дефолты, менять не нужно.**
`flow_derotation_check` на чистом bag'е (gz-hold держит позицию e≈0.1 м + наложенный yaw →
поток rotation-доминирован): **остаток 0.55× baseline на 1189 кадрах**, неверный знак ×2.24,
`Rᵀ` ×1.34. Ключевой приём: дрейф ALT_HOLD доминирует поток, поэтому «чистое вращение» нужно
ловить под gz-hold (`BS_GZHOLD=1 BS_GZ_YAW=80`), а не в свободном bag'е (там 0.76–0.97×).
Ветра в стеке НЕТ (SIM_WIND_SPD=0, нет `<wind>` в SDF) — снос это собственный дрейф ALT_HOLD.

## Шаг 2 — конфликт override ✅ ЗАКРЫТ выбором варианта (a)
Демпфер свёрнут в `alt_hold_bootstrap.py` веткой `--flow-hold` → публишер ОДИН (конфликт
растворён). Унаследован проверенный wall-publisher 20 Гц: override не протухает на низком RTF
и не обрывается при плохом потоке (fade-out: confidence→blend→0, сталл кадров→ROLL центр, но
публикация ИДЁТ). Гейт mode/armed неявный — ветка живёт только после ARM+CLIMB в ALT_HOLD.

## Шаг 3 — валидация демпфера ✅ ЗАКРЫТ (A/B по истинной позе)
Атомарные прогоны `liftland` + `BS_FLOWHOLD=1` (damped) vs без флага (baseline); оракул —
`/model/iris_cam/odometry`. Сравнение боковой (ROLL, демпфер) vs продольной (pitch, не
управляется = встроенный baseline), окно «на текстуре» (первые ~15 с воздуха):

| ось | BASELINE | DAMPED |
|---|---|---|
| продольная (pitch, общий режим) | 2.44–2.81 м/с | 2.47 м/с |
| **боковая (roll, демпфер)** | **~3.0 м/с** | **0.51 м/с** |

→ **демпфер режет боковой снос ×6** (3.0→0.51 м/с). Продольные оси совпали → forward-снос
не зависит от демпфера (не смещает сравнение). Per-sample-yaw проекция подтвердила (yaw-смаз
не виноват). **`osign=+1` верный** (подавляет, не разгоняет). Команда: `liftland`+`BS_FLOWHOLD=1`
`TOPICS_EXTRA="/gz_imu/data_flu /model/iris_cam/odometry"`; анализ дрейфа — в scratchpad.

## Остаток
- **Тюнинг гейнов** (Шаг 3 был на дефолтах `kp8/ki2`): можно подкрутить `kp/ki/kd`,
  `conf_min/full`, `flow_max` для меньшего остаточного дрейфа. Дефолты уже дают ×6.
- **`--flow-osign` строго**: знак выверен по A/B (подавляет), но не оракулом — ок для v0.
- **Продольный forward-снос — ОТКРЫТ**: причина не установлена (AHRS/EKF-lean без опорной
  скорости, та же болезнь, что лечил gz-hold). Демпфер на pitch не влияет (v0 = только ROLL).
  Всплывёт в фазе 2 (looming→PITCH) или при настоящем position-hold.
- **Фаза 2**: looming-зануление (продольный) + визуальный yaw-hold — поверх доказанного ядра.
- **Шаг 4 (вторичное)**: оракул `estimate_extrinsic=2` как независимая сверка Шага 1 +
  гипотеза срыва VINS ~25 с (todo4).
- **Стик=setpoint**: в sim desired=0 (нет пульта); реальный `k_stick*(стик−центр)` — боевой борт.

---

# Тюнинг и предел сигнала (кампания калибровки, ветка nn2_c3_vins_althold_4)

Полный журнал прогонов и методика — `tune_results.md` + `HowToFlow_PID_synth.md`.
Кратко, что установлено эмпирически (гибридный харнес gz-hold-pitch + флоу-roll):

- **`ki=0`, `osign=+1`** — зафиксированы (ki>0 = пружина+windup раскачивает; osign=−1 =
  положительная ОС, разгон до 33 м/с). См. `FAQ_pid.md`.
- **`kp` вверх + временное сглаживание потока** (медиана lateral по N кадрам,
  `--flow-smooth`): kp4=2.76 → kp16+smooth5=1.58 → **kp32+smooth5=0.62 м/с** RMS боковой.
  Сглаживание режет БЕЛЫЙ шум LK как √N (дрейф 39.8→13.5 м).
- **Жёсткий предел — ГЛУБИНА сцены.** Мягкий боковой челнок (крен ±4°, изолированная
  трансляция) → `s≈0.43 px/(м/с)` → эффективная глубина `D=f·dt/s≈50 м`. При v=1.5 м/с
  сигнал `s·v≈0.6px` тонет в шумовом полу LK ~1.6px → ПОКАДРОВО демпфер слеп к v≲3.75 м/с.
  Это НЕ крен/детектор/тюнинг — дальнее поле (`FAQ_vins §10`). Замкнутый контур+сглаживание
  вытягивают устойчивую скорость НИЖЕ покадрового пола усреднением. Рычаг для скачка —
  **близкая текстура** (лететь ниже / у объекта; `s ∝ 1/D`).

## Динамические гейны (kp / smooth на лету) — концепт для боевого борта

`kp` и `smooth_n` можно менять В ПОЛЁТЕ. Реализация: ROS2-параметры +
`add_on_set_parameters_callback` (читать из мутабельного `self.flow_kp`, не из `self.a`),
либо подписка на топик, либо gain-scheduling в коде. **Бампов нет:** при `ki=0` смена `kp` —
чистый множитель; смена `smooth_n` — ресайз медианного буфера `_lat_buf` (мелкий транзиент).

Где это по-настоящему полезно (не «ещё одна ручка»):

1. **Gain scheduling против глубины.** `s ∝ 1/D` гуляет (близко/далеко в кадре) → эффективное
   усиление `c=|k|·kp·s` плавает. Оценивая `D`/силу сигнала онлайн (confidence, looming-
   дивергенция, NN2-карта) → масштабировать **`kp ∝ 1/s`** → постоянное демпфирование
   независимо от сцены (далеко — kp вверх, близко — вниз).
2. **Адаптивное сглаживание: порыв vs дрейф.** Сглаживание = компромисс шум↔лаг: тяжёлое
   давит шум (нужно на медленном дрейфе), но добавляет задержку (плохо для быстрого порыва).
   Динамически: тихо → тяжёлая медиана; детектим всплеск потока (**порыв**) → сбрасываем
   сглаживание для быстрого отклика.

**Оговорки:** для (1) нужен онлайн-оценщик глубины/сигнала; для (2) — детектор порыва
(транзиент потока над фоном). Без них динамика — просто ещё один тюнинг-параметр.
**Связь с боевым бортом:** `nav_pkg` уже имеет looming (глубина) и NN2 (карта места) —
именно они кормят gain-scheduling (сцена/глубина → масштаб kp), когда те слои заведутся.
