# `env.default` / `.env` — профиль стека и прогонов

Профиль — строки `KEY=VALUE` — живёт в ДВУХ файлах рядом с `docker-compose.yml`:

| Файл | В git | Роль |
|---|---|---|
| `env.default` | ✅ | **ЭТАЛОН проекта**: боевые дефолты (LV=2, SF-мастер, fine-мир) едут на все боксы |
| `.env` | ❌ (`.gitignore`) | **рабочая копия бокса**: её читают compose и freefly_lv; машинные правки — тут |

`.env` при отсутствии создаётся копией `env.default` **автоматически**:
таргет `ensure-env` прицеплен к `make build`/`up`/`restart-all`/`fresh-start`,
тот же посев делает `freefly_lv.sh` перед чтением дефолтов. Свежий клон летит
боевым профилем без ручной возни; существующий `.env` НИКОГДА не перетирается.

**Куда писать правку:**

- меняешь дефолт ПРОЕКТА (для всех боксов) → `env.default` **и** свой `.env`
  (автопосев существующий `.env` не обновляет);
- подгоняешь под конкретную машину (пути, GPU, свой мир/ветер) → только `.env`.

## Кто читает `.env`

1. **docker compose** — классически: интерполирует `${KEY:-дефолт}` в
   `docker-compose*.yml` (пути bind mount, env контейнеров). Значение доезжает
   до контейнера только при его **СОЗДАНИИ** (`fresh-start`; `restart-all` env
   не перечитывает). `freefly_lv.sh`/`capture_scene.sh` делают fresh-start
   сами — для прогонов серии ничего специально перезапускать не надо.
2. **`src/lab/freefly_lv.sh`** — как слой дефолтов ПРОФИЛЯ ПОЛЁТА (с 2026-08-26):
   в начале скрипт проходит `.env` и экспортирует строки `KEY=VALUE`, **только
   если переменная не задана снаружи**. Так голый `bash src/lab/freefly_lv.sh`
   летит боевым профилем, а любой ключ переопределяется как раньше:

   ```
   env снаружи (LV=1 bash ...)  >  docker/sim/.env  >  дефолт скрипта (LV=1)
   ```

   Скрипт печатает, что взял: `freefly_lv: дефолты бокса из docker/sim/.env:
   ...` — в логе прогона видно, какие ключи пришли из файла, а какие
   переопределены снаружи.

## Ключи эталона (env.default; сейчас = этот ноутбук)

| Ключ | Значение | Кто ест | Зачем |
|---|---|---|---|
| `VINS_SRC` | `/home/andriy/VINS-MONO-ROS2` | compose | путь к форку VINS-MONO-ROS2 (ветка `1317_debug`) на хосте → bind mount в nav как `/root/sim_ws/src/vins_oss`. ⚠️ машинно-зависим (GCE-бокс: `/root/VINS-MONO-ROS2`) |
| `CUDA_ARCH_BIN` | `8.9` | compose (build) | арка CUDA для сборки OpenCV в образе nav (RTX 4050 = Ada); одна арка — быстрее сборка. ⚠️ машинно-зависим (под GPU бокса) |
| `WORLD` | `/root/worlds/mili_fortress_fine.sdf` | compose → simulator | мир по умолчанию: мелкая фактура земли (grass_plane_fine, texel 3.7 мм) — зрение живёт у земли. Базовый мир — закомментировать строку |
| `LV` | `2` | freefly_lv.sh | профиль «GPS отсутствует с бута» (модель боевого борта) |
| `BS_SF_MASTER` | `1` | freefly_lv.sh → нода | схема «SF-мастер»: SF (CH7) = мастер сырых стиков, SC (CH6) = потолок лесенки демпфер/VinsHold/LOITER. Нужен микс SF→CH7 в EdgeTX |
| `BS_LAND_JOY` | `b1` (эталон env.default с 2026-09-02; дефолт ноды `b0`) | freefly_lv.sh → нода | где в `/joy` кнопка SA (мягкая посадка): `b<i>`/`a<i>`; зависит от микшера пульта — мерить `src/lab/joystick/js_probe.py` |
| `BS_LOITER_TRACK` | `0` (эталон env.default: путь 1 выключен явно; дефолт ноды `0` — КАНДИДАТ, полётом не доказан) | freefly_lv.sh → нода | ярус LOITER, путь 1: стики roll/pitch = уставки скорости В ОСЯХ МИРА (TrackHold) — yaw вращает нос, не траекторию; крена виража atan(v·ω/g) нет по построению (разбор eagle/4 2026-09-01). Парный параметр — `LOIT_OPTIONS 0` в sitl-extra.parm |
| `BS_LOITER_BANK_MAX` | `8` (эталон env.default с 2026-09-02; дефолт ноды `0`; ДОКАЗАН реплеем ab_bank8: крен ≤ 7° против 13–20.8° eagle/4) | freefly_lv.sh → нода |
| `BS_VINS_KD_ERR` | `1` (только в .env бокса; дефолт ноды `0`; доказан ЧАСТИЧНО реплеем ab_kderr: докат 9–11→5–7 м, σθ на 5/15 м ×1.5–1.8 лучше, 30 м — остаток) | freefly_lv.sh → нода | VinsHold: D-член на ОШИБКЕ скорости (v − v_уставки) вместо абсолютной v — лечит звон ~1 Гц и долг уставки 9–12 м при полёте по прямой (серия eagle 2026-09-02, разбор `doc/tmp/eagle/eagle.txt`); на висении закон бит-в-бит прежний |
| `BS_VINS_I_LATCH` | `1` (только в .env бокса; дефолт ноды `0`; ДОКАЗАН полётом ab_ilatch: откат после стопа 2.5–3.1 → 1.6–1.9 м/с, возврат 7.5–9.1 → 3.6–5.3 м) | freefly_lv.sh → нода | VinsHold: защёлка трима (аналог `_TRIM_LATCH` станции) — И-член заморожен от живого стика до «гвоздя» (борт встал, \|v\| < 0.3); в движении ошибка позиции = лаг слежения, не ветер — интегрировать её значит копить мусорный трим до капа и ловить перелёт на отпускании |
| `BS_GZ_KD` | `80` (только в .env бокса; дефолт ноды `120`; ДОКАЗАН полётом ab_kd80: σθ на 30 м 2.2–2.8 → 1.29°, рост качания с высотой исчез) | freefly_lv.sh → нода | пункт 3 серии eagle: kd Gz/Vins-hold — шум VINS-скорости ∝ высоте (σ 0.07→0.13 м/с) перекачивался kd=120 в дёрганье наклона; свип по идентифицированному планту (a=0.0125 PWM→м/с², τ=0.26 с) выбрал 80. ⚠️ общая ручка с gz-hold (сим-костыль), его висение с 80 не перемеряно |
| `BS_VINS_PIN_STOP` | `1` (только в .env бокса; дефолт ноды `0`; ДОКАЗАН полётом ab_pin: возврат после стопа −3.6..−5.3 → +0.1 м) | freefly_lv.sh → нода |
| `BS_VINS_PREDICT` | `1` (только в .env бокса; дефолт ноды `0`; полёт ab_pred — эффект МАЛЫЙ, ~75% пилы от kd·скорость, не kp·позу; оставлен как микро-плюс) | freefly_lv.sh → нода |
| `BS_VINS_VSMOOTH` | `0` — ОТКАЧЕНО (полёт ab_vsmooth: пилу команды срезало 8→5 PWM/тик, но σθ борта 1.44→2.15° на 30 м — лаг D-члена съел демпфирование; дефолт ноды 0) | freefly_lv.sh → нода | VinsHold: ФНЧ vins-скорости для D-члена, τ с — пила команды оказалась косметической (борт фильтрует инерцией), а звон контура от сглаживания растёт; путь тупиковый, ручка/стенд оставлены в коде |

⚠️ **Реплей СТАРЫХ сценариев** (без ключа `"sf"`, записи с 6 осями) под
`BS_SF_MASTER=1` летит целиком на сырых стиках — запускать явно:
`BS_SF_MASTER=0 BS_PILOT=replay ... bash src/lab/freefly_lv.sh`.

## Что ещё сюда можно класть

Любой ключ, который хочется сделать постоянным дефолтом (бокса — в `.env`,
проекта — в `env.default`):

- **Ключи compose** (см. `${...}` в `docker-compose*.yml`): `SPAWN_POSE`
  (постоянная точка старта — шесть чисел или имя пресета `spawn_save.py`, см.
  `spawn.md`), `WIND_SPD` / `WIND_DIR_DEG` / `WIND_FACTOR`, `CAMERA_W`/`CAMERA_H`,
  `VISION_POSE_SOURCE`. Применяются на fresh-start.
- **Ключи freefly_lv** — всё, что скрипт принимает через env: `BS_*`
  (пробрасываются в контейнер автоматически, см. `capture_scene.sh`), `WIND_SPD`,
  `RES`, `GDRIVE_UP`, `MP4`, `KEEP_BAG`, `HUD_MP4`, `TOPICS_EXTRA`...

Правила разбора в freefly_lv: пустые строки и `#`-комментарии пропускаются,
ключ — валидный идентификатор (`A-Za-z0-9_`, не с цифры), парные кавычки вокруг
значения снимаются (как делает compose). Значение с пробелами (например,
`SPAWN_POSE="x y z r p y"` или `TOPICS_EXTRA="..."`) — в кавычках.

## Грабли

- **`restart-all` env не перечитывает** — ключи compose доезжают только при
  пересоздании контейнера (`fresh-start`). Скрипты серии делают его сами.
- **Обновлённый `env.default` на старом боксе сам не применится** — автопосев
  существующий `.env` не трогает. Перенести строку в `.env` руками (или
  удалить `.env` и дать пересеяться — потеряв локальные правки).
- **После автопосева на новом боксе** проверить два машинно-зависимых ключа:
  `VINS_SRC` (где лежит форк) и `CUDA_ARCH_BIN` (арка GPU) — баннер посева
  об этом напоминает.
- Ключи, которые НЕ ест ни compose, ни freefly_lv (например, `make`-переменные
  типа `CPU`), из `.env` никуда не доедут — их по-прежнему давать в команде.
