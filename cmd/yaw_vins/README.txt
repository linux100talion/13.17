cmd/yaw_vins — КУРС EKF ОТ VINS (EK3_SRC1_YAW=6), поверх возврата по треку
===========================================================================

Зачем создан
------------
Сегодня курс полётнику даёт компас, а кадр VINS рождается с курсом ПЕРВОГО кадра камеры
(yaw у монокуляра ненаблюдаем). Разность рам (Δyaw) якорь снимает один раз при созревании
и дальше держит замороженной — то есть уход курса ловится КОСВЕННО, через метровый расход
позиции, а магнитные помехи на борту войдут в навигацию напрямую. Приращения курса у обоих
идут от ОДНОГО гироскопа (VINS кормится тем же /mavros/imu/data), значит меняем не источник
приращений, а ИСТОЧНИК КОРРЕКЦИИ ДРЕЙФА: компас (абсолютен, но шумит и врёт над железом) →
визуальные фичи (гладко и без помех, но без абсолюта). На коротких временах это точнее —
ровно то, что нам сейчас нужно. Механика, риски и раскладка рам — src/nav/frames.md.

Что меняет
----------
Ручки стека — БИТ В БИТ cmd/rth_track (возврат по своему треку в GUIDED). Отличие одно:
профиль mission/yaw_vins (= mission/baseline + BS_EKF_YAW_SRC=vins).
  ВПЕРЁД: в момент латча возврата (rth=ready) нода ставит EK3_SRC1_YAW=6. Рама к этому
          мгновению зрелая, спокойная (BS_RTH_HOME_SETTLE) и с устойчивым Δyaw
          (BS_RTH_DYAW_TOL), борт висит в круге на демпфере — позиционного контура в
          петле нет. СКАЧКА КУРСА БЫТЬ НЕ ДОЛЖНО: мы публикуем ориентацию, уже
          повёрнутую якорем (yaw_VINS + Δyaw = yaw_AHRS), то есть отдаём полётнику его
          же текущий курс.
  НАЗАД:  BS_EKF_YAW_FALLBACK=1 — при пропаже опоры (перерождение VINS / не sane /
          закрытый мост / протухший поток) возвращаем EK3_SRC1_YAW=1.
          ⚠️ Откат ЖЁСТКИЙ: EKF доберёт накопленное расхождение с компасом разом.
          Мягкий доворот перед переключением — следующий шаг, его этот прогон и мерит.

Как летать
----------
  bash cmd/yaw_vins/yaw_vins.sh
  1) взлёт, повисеть, пока в ЛЕВОМ НИЖНЕМ углу FPV не загорится ЗЕЛЁНОЕ «RTH READY» —
     в этот же момент переключается курс (в статусе yawsrc=vins);
  2) дальше как в cmd/rth_track: уйти ЛОМАНОЙ 20–30 м, вернуться кнопкой SD;
  3) ОТДЕЛЬНО измерить рывок отката — спровоцировать перерождение VINS в полёте:
       docker exec p1317_nav bash -lc 'source /opt/ros/humble/setup.bash;
         ros2 topic pub --once /restart std_msgs/msg/Bool "data: true"'
     Делать это НА ВЫСОТЕ 5+ м и подальше от деревьев: борт в этот момент падает на
     ярус 0 (демпфер), а курс возвращается к компасу скачком.

Чем судить
----------
  python3 src/lab/rth_check.py docker/sim/output/joystick/<RUN>          # возврат как обычно
  поля статуса в bag: yawsrc= (что реально стоит), brd= (Δyaw якоря), brdn= (текущая
  разность курсов), плюс истина Gazebo — она здесь единственный судья курса.
ЗАЧЁТ:
  - yawsrc=vins появился ровно на латче, без скачка курса (проверить по /mavros/imu/data
    против истины: разрыв > 2–3° в этот миг = провал);
  - brd= СТОИТ весь полёт (при YAW=6 пересчёт Δyaw обязан давать то же число — это
    рассуждение из frames.md, здесь оно проверяется);
  - курс EKF против истины Gazebo не уходит быстрее, чем при компасе (сравнивать с
    131451/133201 — те же профили, только yaw от компаса);
  - возврат не хуже предыдущих: промах по истине 3.5–4.2 м, след 0.3–0.5 м по медиане.
ПРОВАЛ и куда смотреть:
  - yawsrc остался compass → в логе «отказ — ретраю» (сервис параметров) или латча не
    было (rth≠ready: смотреть brdn — не устоялся ли Δyaw);
  - борт «повело» сразу после латча → скачок курса всё-таки был: смотреть /mavros/imu/data
    и brd в первые секунды;
  - уход курса быстрее компасного → цена больше выигрыша, кандидат отклоняется;
  - рывок при откате большой → делать мягкий доворот (frames.md).

⚠️ В симе компас ИДЕАЛЬНЫЙ, поэтому этот прогон мерит только ЦЕНУ (дрейф курса VINS).
Выигрыш виден лишь с магнитной помехой: повторить с SIM_MAG_ALY / SIM_MAG_ALY_HGT в
eeprom (BS_FCU_PARAMS) — отдельный прогон.

Осталось
--------
  - мягкий откат (доворот публикуемого курса к компасу за N секунд перед переключением);
  - прогон со спавном НЕ на восток (SPAWN_POSE): при спавне на восток Δyaw ≈ 0 и вся
    конструкция с поворотом кадра неотличима от её отсутствия.

Результат
---------
ПОПЫТКА 1 — lv2_joy_20260909_154759 и _155443: ПРОГОН НЕ СОСТОЯЛСЯ, курс не переключался,
дом не латчился, и виноват был НОВЫЙ КРИТЕРИЙ ЗРЕЛОСТИ, а не сама идея.

Цепочка (обе записи одинаковы):
  СЕДЬМОГО ПОЛЯ /nn1/bridge НЕ СУЩЕСТВОВАЛО В КОДЕ ВООБЩЕ (строка «closed ripe 0 1 0 -» —
  шесть полей): читателя написали (ros_telemetry → dyaw_now, brdn= в HUD, чек зрелости),
  писателя в ray_tracer — нет → brdn= пустое всю запись
  → мой чек устойчивости Δyaw при отсутствии числа возвращал «не зрел»
  → зрелости не было НИКОГДА → /vins/bridge_ok=False → мост закрыт весь полёт (brw=ripe)
  → rth=lost:timeout на 60-й секунде, yawsrc=compass до конца
  → EKF без подтяжки потерял позицию (ekf=0 с 28-й секунды, zekf=--)
  → ВЫСОТА ПЕРЦЕПЦИИ ЗАМЁРЗЛА на 0.6 м, пока борт летал на 1.3–2.9 м; канал считал
    себя годным (ipm=1 ipmf=0) и врал масштабом — это и есть «DpHold работал странно,
    гвозди не ставил».

ТРИ ПРАВКИ (2026-09-09):
  0. СЕДЬМОЕ ПОЛЕ ТЕПЕРЬ ПУБЛИКУЕТСЯ (ray_tracer): Δyaw СЕЙЧАС = «курс AHRS − курс VINS»
     каждый кадр, до всякого латча; шестое поле — снимок той же разности в момент шва,
     разность седьмого и шестого = уход курса с латча. Формат строки заперт офлайн-тестом
     src/nav/test/test_bridge_gate.py §9 — читатель и писатель больше не разъедутся;
  1. чек Δyaw ДЕГРАДИРУЕТ В «НЕ МЕШАЮ»: числа нет (старый ray_tracer, голый Orin,
     молчащий IMU) → зрелость не блокируется. Новый чек имеет право ОТЛОЖИТЬ зрелость
     при живом источнике, который реально гуляет, и не имеет права молча запретить полёт;
  2. ВЫСОТА ПЕРЦЕПЦИИ НЕ ПРОТУХАЕТ МОЛЧА (BS_PERC_ALT_STALE=2 с): источник замолчал —
     отдаём None, гейт земли IPM честно закрывается (palt=--, красный ALT GATE), вместо
     полёта по замёрзшему числу. Это отдельный старый дефект, вскрытый этим прогоном.

⚠️ ПЕРЕД ПОВТОРОМ: убедиться, что nav_pkg реально пересобрался —
  docker exec p1317_nav grep -c dnow \
    /root/sim_ws/install/nav_pkg/lib/python3.10/site-packages/nav_pkg/nn1/ray_tracer.py
должно быть НЕ 0 (после make nav-rebuild от 2026-09-09 install бит-в-бит равен исходнику;
на стоящем стеке то же читается из volume:
  docker run --rm -v sim_nav_colcon_install:/i busybox grep -c dnow \
    /i/nav_pkg/lib/python3.10/site-packages/nav_pkg/nn1/ray_tracer.py).
В ЖИВОМ ПОЛЁТЕ проверять по HUD: brdn= должно быть ЧИСЛОМ, а не «--».

ПОПЫТКА 2 — lv2_joy_20260909_163714 (штатный спавн) и _164742 (SPAWN_POSE=diagonal, курс
117.7°): ОБА ВЕРНУЛИСЬ И СЕЛИ, курс переключился (yawsrc=vins на 39-й и 35-й секунде),
brd= стоял всю запись — и ровно поэтому пара прогонов оказалась ценной: она РАЗДЕЛИЛА
«работает» и «выглядит работающим, потому что Δyaw≈0».

  прогон            Δyaw якоря   ошибка курса compass→vins   дрейф EKF   промах по истине
  163714 (восток)      +0.4°        2.72° → 0.82° (лучше!)     0.87 м        3.68 м
  164742 (диагональ)  +93.3°        5.47° → 8.73° (хуже)       7.63 м        5.64 м
  (ошибка курса — медиана |курс EKF − истина Gazebo|, дрейф — на момент касания)

На востоке курс от VINS ОКАЗАЛСЯ ТОЧНЕЕ КОМПАСА (0.82° против 2.72°) — это первый замер
в пользу кандидата. На диагонали он вдвое хуже, дрейф вырос в 9 раз, и причина найдена.

ПЕРВОПРИЧИНА — НЕСОГЛАСОВАННЫЙ QoS, а не сама идея:
  ray_tracer подписывался на /mavros/imu/data и /mavros/global_position/rel_alt с
  дефолтным RELIABLE, а MAVROS публикует их BEST_EFFORT. Такая пара НЕ СОГЛАСУЕТСЯ —
  подписка не получает НИЧЕГО, молча: ни ошибки, ни варна. Значит att_yaw был None
  ВСЕГДА, с самого первого полёта этой ветки. Следствия:
    - седьмое поле /nn1/bridge публиковалось, но как '-' → brdn=-- → критерий зрелости
      по Δyaw слеп (спасал fail-open из 15f8154, иначе полёты бы не состоялись);
    - latch_yaw() на первом открытии моста НЕ ВЫЗЫВАЛСЯ НИКОГДА: ветка «нет курса AHRS»
      делает anchor.reset() → 2 с в EKF шёл СЫРОЙ VINS. Это ровно тот дефект, который
      чинил 5c1d562 («на открытии отдаём раму с ПОВОРОТОМ») — фикс молча деградировал
      обратно. На востоке безвредно (кадр почти совпадает), на диагонали EKF получил
      раму, повёрнутую на ~118°: в логе «мост ОТКРЫТ впервые ... якорь тождественен
      (курса AHRS нет)», через 2.03 с «кадр VINS заякорен: Δyaw=+93.3°», дрейф скакнул
      до 6.1 м и дальше только рос (12.6 м на 60-й с). Потом жёсткая подтяжка увела
      якорь с +93.3° на +100.1° — при EK3_SRC1_YAW=6 это 6.8° ступеньки прямо в курс EKF;
    - засечка NN1 выходила по `R_enu_body is None` — то есть не работала бы вовсе.

ПРАВКИ: qos_profile_sensor_data на ВСЕ входы MAVROS + сторож молчащих входов в
ray_tracer (через 30 с после старта ошибка в лог с указанием на QoS). Живая проверка
мисматчей по всему стеку: сравнить Reliability публикатора и подписчика в
`ros2 topic info -v <топик>` — на 2026-09-09 после правки не осталось ни одного.

ПОВТОРИТЬ обе точки спавна после пересборки nav. Ожидание: на диагонали дрейф и ошибка
курса должны сойтись к восточным числам; если нет — виноват уже сам EK3_SRC1_YAW=6.

ПОПЫТКА 3 — lv2_joy_20260909_171337 (восток, уже с фиксом QoS): «RTH Lost: Bridge closed».
Фикс QoS ПОДТВЕРЖДЁН, вскрыт следующий слой.

Подтвердилось: brdn= стал ЧИСЛОМ, и в логе вместо «якорь тождественен (курса AHRS нет)»
теперь «мост ОТКРЫТ впервые: 2 с отдаю VINS от своего начала (Δyaw=-2.5°, t=0)» — то
есть latch_yaw наконец работает, кадр отдаётся С ПОВОРОТОМ.

Провалилось: возврат умер через 2 с после READY. ПЕТЛЯ ИЗ ТРЁХ ЗВЕНЬЕВ, которую и
завёл впервые заработавший критерий Δyaw:

  борт пошёл в разворот → brdn дрогнул больше допуска 3° → зрелость СНЯТА
  → /vins/bridge_ok=False → BridgeGate закрыл мост (brw=ripe)
  → RthReadiness видит закрытый мост ПОСЛЕ латча → LOST терминально (rth=lost:bridge)

Тайминг: латч дома 52.2 с, ЗАПРЕТ 54.2 с; мост закрывался трижды (54, 80, 140 с wall).
Реплей rth_ready_replay.py по этому же bag воспроизводит цепочку тик-в-тик.

ДВА ДЕФЕКТА, оба исправлены:
  1. ЗРЕЛОСТЬ ТЕПЕРЬ ЗАЩЁЛКИВАЕТСЯ. «Доказал ли VINS себя» — вопрос односторонний;
     за здоровьем ПОТОКА следит BridgeGate (|v|>12, перерождение, шторм подтяжек,
     /vins/sane), а не счётчик зрелости. Снять доказанную зрелость может только
     перерождение VINS. Это разрывает петлю в первом же звене;
  2. ЧЕК Δyaw МОЛЧИТ НА ВИРАЖЕ (BS_RTH_DYAW_WZ=15 °/с). Курс AHRS идёт от FCU, курс
     VINS — из одометрии 10 Гц: на развороте их разность гуляет РАЗНИЦЕЙ ЗАДЕРЖЕК,
     а не уходом кадра. Замер по этому bag: размах brdn 5.1° при 41.6 °/с против
     2.4° при 9.5 °/с; выброс, закрывший мост, случился на 38 °/с. Родня — ipm_wz_gate.

⚠️ Реплей по bag доказать фикс НЕ МОЖЕТ: состояние моста там ЗАПИСАННЫЙ ВХОД, а не
следствие — петля видна, разрыв не виден. Доказательство — офлайн-тесты
test_rth_ready.py §10ж (защёлка: рывок Δyaw и обмелевший поток не снимают зрелость,
перерождение снимает) и §10з (вираж 40 °/с не мешает, 5 °/с по-прежнему блокирует).

ПОВТОРИТЬ обе точки спавна. Судить: brdn= число, мост НЕ закрывается по ripe после
первого открытия, rth= доходит до ready и там остаётся, дальше — дрейф и ошибка курса.

ПОПЫТКА 4 — lv2_joy_20260909_172825 (восток) и _173415 (diagonal). Петля зрелости
починена, восток стал ЛУЧШИМ прогоном кампании, диагональ вскрыла третий слой.

  прогон            дрейф EKF   промах по истине   след возврата (медиана)
  172825 (восток)     0.19 м         3.39 м              0.13 м
  173415 (диагональ) 13.09 м         9.63 м              2.23 м

Восток: brd=-3.6° всю запись, НИ ОДНОЙ жёсткой подтяжки, мост по ripe не закрывался.
Конструкция работает.

Диагональ — ЯКОРЬ СПАРИВАЛ ПОЗЫ ПО ПРИХОДУ, А НЕ ПО ВРЕМЕНИ. Секунды из bag:

  приход  штамп  курс VINS
   58.98  73.03   -80.4
   60.95  73.13   -81.2     ← Δприход 1.96 с, Δштамп 0.10 с

VINS ВСТАЛ НА 1.96 с по реальному времени при НЕПРЕРЫВНЫХ штампах — это не дыра в
данных и не перерождение, а подвис счёта; гейт моста смотрит на штампы и совершенно
правильно ничего не заметил (reb не менялся, мост остался открыт). А FrameAnchor брал
СВЕЖУЮ позу EKF и ДВУХСЕКУНДНУЮ позу VINS. Борт разворачивался 10 °/с → пара
разъехалась на ω·лаг = 20°, жёсткая подтяжка сработала на первом же сообщении после
подвиса (61.0 с) и защёлкнула Δyaw=+76.5° вместо верных +96° — на весь полёт:

  t 36-58 с   brd +95.9°   истинный поворот кадра +96°    ошибка ~0
  t 61.0 с    brd +76.5°   истинный поворот кадра +97°    ошибка -21°
  t 109 с     brd +69.0°   истинный поворот кадра +97°    ошибка -28°

Ошибка угла поворачивает ВСЮ карту вокруг якоря: 20° на 45 м ≈ 15 м — ровно
наблюдаемый дрейф. При EK3_SRC1_YAW=6 те же 20° ушли и в курс полётника.
На восточном спавне механизм тот же, но подтяжка ни разу не сработала — потому и
не было видно четыре прогона подряд.

ПРАВКИ (2 и 3 из трёх предложенных):
  2. ЖЁСТКАЯ ПОДТЯЖКА ПРАВИТ ТОЛЬКО ТРАНСЛЯЦИЮ. Расход, который её будит, измерен в
     метрах — выводить из него угол мы не обязаны, а цена ошибки несимметрична.
     Угол задаётся только при рождении рамы (первый латч, latch_yaw, перерождение);
  3. ЯКОРЬ НЕ ДВИГАЕТСЯ НА ПРОТУХШЕЙ ОДОМЕТРИИ (anchor_stale_sec=0.3 с): возраст =
     наши часы минус штамп сообщения. Отстало — якорь стоит, поза в EKF идёт как шла,
     в лог варн (не чаще раза в 5 с).
Тесты: test_frame_anchor.py §4 (подтяжка не трогает угол, сходится трансляцией) и
§4б (регресс 173415: протухшая пара с 20° лага угол не портит).

ОСТАЛОСЬ (пункт 1, следующим шагом): спаривать позы ПО ШТАМПУ — буфер поз EKF и
выборка под штамп одометрии VINS, как AttitudeBuffer в канале IPM. Тогда лаг вообще
перестанет попадать в якорь, а не только не будет защёлкиваться.
